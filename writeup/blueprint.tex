\documentclass[11pt]{article}
\usepackage{amsmath,amssymb}
\usepackage[margin=1in]{geometry}

\title{Blueprint for the Ternary Quartic Rank-4 Proof}
\date{2026-04-09}

\begin{document}
\maketitle

\section*{Status}
This blueprint is not complete yet. The abstract SOCP component, orthogonal
mixing of the four factor coordinates, and all currently identified
representative-level rank-$4$ algebraic branches are now formalized. The
remaining gap is the normalization/classification step that reduces an
arbitrary admissible rank-$4$ factor to those representatives, followed by the
final theorem assembly in \texttt{TernaryQuarticProof.lean}.

\medskip
\noindent
One additional obstruction is now explicit. Orthogonal mixing acts on the
\emph{coefficient-space} preimages of the four coordinates, not directly on the
affine-linear polynomial space. So even when
$W \cap \mathbb{R}[x_1,x_2]_{\le 1}$ contains the lines of $1$, $x_1$, or
$x_2$, orthogonal mixing alone need not place those exact polynomials among the
coordinates. This is why an affine variable-change transport layer is needed in
addition to factor-coordinate mixing.

\section*{Formalized Step}
Let $r(u,p) = \sigma(u) - p$. The following statement is already formalized in
\texttt{TernaryQuarticProof/Certificate.lean}.

\medskip
\noindent
\textbf{Lemma.} Assume:
\begin{enumerate}
\item $u$ is an admissible rank-4 point;
\item $u$ is a SOCP for $p$ with respect to a positive-definite bilinear form
$B$;
\item $r(u,p)$ is orthogonal to $\operatorname{im}(A_{u_{\mathrm{img}}})$ for
some admissible factor $u_{\mathrm{img}}$;
\item there is a decomposition
\[
  p = q_{\mathrm{img}} + \sum_{w \in W} \sigma(w),
\]
where $q_{\mathrm{img}} \in \operatorname{im}(A_{u_{\mathrm{img}}})$ and every
$w \in W$ is an admissible kernel direction for $A_u$.
\end{enumerate}
Then $r(u,p) = 0$.

\medskip
\noindent
\textbf{Proof.}
The image term pairs to zero by the first-order condition. Each kernel-square
term pairs nonnegatively with the residual by the second-order condition,
because $A_u(w)=0$ removes the positive $2\langle A_u(w), A_u(w)\rangle$ part
of the Hessian expression. Summing these inequalities gives
$\langle p, r(u,p)\rangle \ge 0$. On the other hand, the first-order condition
at $v=u$ gives $\langle \sigma(u), r(u,p)\rangle = 0$, hence
\[
  \langle r(u,p), r(u,p)\rangle
  = \langle \sigma(u)-p, r(u,p)\rangle
  = - \langle p, r(u,p)\rangle \le 0.
\]
Positive-definiteness implies $\langle r(u,p), r(u,p)\rangle \ge 0$, so the
objective is zero and therefore $r(u,p)=0$.

\section*{Additional Formalized Reductions}
Two special cases are now formalized.

\medskip
\noindent
\textbf{Image case.} If $p \in \operatorname{im}(A_u)$ with an admissible
preimage, then $r(u,p)=0$ directly from the first-order condition and positive
definiteness.

\medskip
\noindent
\textbf{Constant-relation case.} If there exists a nonzero constant vector
$c \in \mathbb{R}^4$ with $\sum_i c_i u_i = 0$, then $r(u,p)=0$ for every SOS
quartic $p$.

\medskip
\noindent
\textbf{Proof sketch.}
For each quadratic summand $q^2$ in an SOS decomposition of $p$, define the
admissible direction $w_i = c_i q$. Then
\[
  A_u(w) = q \sum_i c_i u_i = 0,
  \qquad
  \sigma(w) = \Bigl(\sum_i c_i^2\Bigr) q^2.
\]
Since $\sum_i c_i^2 > 0$, the SOCP inequality in the direction $w$ implies
$\langle q^2, r(u,p)\rangle \ge 0$. Summing over the SOS decomposition yields
$\langle p, r(u,p)\rangle \ge 0$, and the abstract SOCP argument finishes as
before.

\medskip
\noindent
\textbf{Orthogonal factor-coordinate invariance.} Let $M \in \mathbb{R}^{4
\times 4}$ satisfy $M^\top M = M M^\top = I$, and define
\[
  (\operatorname{mix}_M u)_i = \sum_j M_{ij} u_j.
\]
Then the following are now formalized in
\texttt{TernaryQuarticProof/FactorTransform.lean}:
\begin{enumerate}
\item admissibility is preserved by $\operatorname{mix}_M$;
\item $A_{\operatorname{mix}_M u}(v) = A_u(\operatorname{mix}_{M^\top} v)$;
\item $\sigma(\operatorname{mix}_M u) = \sigma(u)$ and hence
$r(p,\operatorname{mix}_M u)=r(p,u)$;
\item FOCP and SOCP are preserved under orthogonal mixing.
\end{enumerate}
Therefore the eventual factor-space case analysis may be carried out up to
orthogonal recombination of the four coordinates.

\medskip
\noindent
\textbf{Formalized surjective constant branch.} The constant
$\dim(W \cap \mathbb{R}[x_1,x_2]_{\le 1}) = 1$ model
\[
  u_{\mathrm{const}} = (1, x_1^2, x_1x_2, x_2^2)
\]
is now fully formalized in
\texttt{TernaryQuarticProof/RepresentativeSurjective.lean}. The key lemma is
that every quartic monomial of degree at most $4$ lies in
$\operatorname{im}(A_{u_{\mathrm{const}}})$, hence every quartic polynomial does
as well by linearity. Therefore every SOCP at this representative has zero
residual by the already-formalized image case. The same file now also proves a
basis-invariant upgrade: if an admissible factor contains $1$ and admits three
independent homogeneous quadratic relations whose coefficient matrix in the
basis $(x_1^2,x_1x_2,x_2^2)$ has nonzero determinant, then Lean reconstructs
the exact monomial relations and closes the branch abstractly. This
reconstruction is also transported across affine equivalences.

\medskip
\noindent
\textbf{Formalized surjective mixed-affine representative.} The model
\[
  u_{\mathrm{ma},15} = (1, x_1, x_1^2, x_2^2)
\]
is now formalized as well in
\texttt{TernaryQuarticProof/RepresentativeMixedAffine.lean}. Its proof is the
same style as the other surjective branches: every quartic monomial is hit
directly, either through the constant coordinate for total degree at most $2$,
through $x_1^2$ for monomials with $x_1$-degree at least $2$, or through
$x_2^2$ for the remaining quartic monomials. So every SOCP at this
representative has zero residual by the abstract image certificate.

\medskip
\noindent
\textbf{Formalized codimension-one image reduction.} The mixed-affine
rank-$14$ representative
\[
  u_{\mathrm{ma},14} = (1, x_1, x_1x_2, x_2^2)
\]
now has a full representative-level formalization in
\texttt{TernaryQuarticProof/RepresentativeMixedAffine.lean}: if a quartic
polynomial has zero $x_1^4$ coefficient, then it lies in
$\operatorname{im}(A_{u_{\mathrm{ma},14}})$. The proof is again
monomial-by-monomial. Every quartic monomial except $x_1^4$ is represented by
one of the four coordinates, and then support induction upgrades the monomial
statement to all quartics with vanishing $x_1^4$ coefficient. The same file now
also proves that every SOCP at this representative has zero residual. The extra
step uses the six-monomial quadratic normal form to show that the $x_1^4$
coefficient of a quadratic square is exactly the square of its $x_1^2$
coefficient, and then matches that coefficient with the explicit kernel square
\[
  \sigma(0,0,-c x_1x_2, c x_1^2) = c^2(x_1^2x_2^2 + x_1^4).
\]

\medskip
\noindent
\textbf{Formalized codimension-two image reduction.} The mixed-affine
rank-$13$ representative
\[
  u_{\mathrm{ma},13} = (1, x_1, x_1^2, x_1x_2)
\]
is now also fully formalized in the same file. The image theorem proves that a
quartic lies in $\operatorname{im}(A_{u_{\mathrm{ma},13}})$ whenever its
$x_2^3$ and $x_2^4$ coefficients vanish. The key coefficient identities for a
quadratic square are
\[
  [x_2^3](q^2) = 2\,[x_2](q)\,[x_2^2](q), \qquad
  [x_2^4](q^2) = [x_2^2](q)^2,
\]
and the explicit kernel line
\[
  w_{b,c} = (0,0,-x_2(b+c x_2),x_1(b+c x_2))
\]
matches both missing quotient classes simultaneously. Hence every SOCP at this
representative also has zero residual.

\medskip
\noindent
\textbf{Formalized span-three branch.} The representative
\[
  u_{\mathrm{span3}} = (1, x_1, x_2, h),
\]
with arbitrary quadratic homogeneous $h$, is still formalized directly in
\texttt{TernaryQuarticProof/RepresentativeSpanThree.lean}. But the file now
proves a stronger abstract theorem: for any admissible rank-$4$ factor $u$, if
there exist coefficient vectors $c_0,c_1,c_2 \in \mathbb{R}^4$ with
\[
  \sum_i c_{0,i}u_i = 1,\qquad
  \sum_i c_{1,i}u_i = x_1,\qquad
  \sum_i c_{2,i}u_i = x_2,
\]
then every SOCP at $u$ has zero residual. Thus the whole
$\dim(W \cap \mathbb{R}[x_1,x_2]_{\le 1})=3$ branch is now solved without any
exact orthogonal normalization to a rigid representative.

Writing a quadratic factor as
\[
  q = q_{\le 1} + q_{\mathrm{hom}}, \qquad
  q_{\mathrm{hom}} = x_1 a + x_2 b,
\]
with $a,b$ linear, the proof first shows that every cubic lies in
$\operatorname{im}(A_u)$ from the span relations above. It then builds the raw
kernel direction
\[
  w_q^{\mathrm{raw}}
  =
  \operatorname{rel}(-c_0,q_{\mathrm{hom}})
  + \operatorname{rel}(c_1,a)
  + \operatorname{rel}(c_2,b),
\]
which satisfies $A_u(w_q^{\mathrm{raw}})=0$. Its quartic part is
\[
  \sigma(w_q^{\mathrm{raw}})_{\deg 4}
  =
  \Bigl(\sum_i c_{0,i}^2\Bigr) q_{\mathrm{hom}}^2,
\]
so after scaling by
\[
  \alpha = \Bigl(\sqrt{\sum_i c_{0,i}^2}\Bigr)^{-1}
\]
one gets a kernel vector $w_q=\alpha\, w_q^{\mathrm{raw}}$ for which
\[
  q^2 - \sigma(w_q)
\]
has total degree at most $3$. Summing these cubic image terms over an SOS
decomposition of $p$ and using the indexed-family certificate in
\texttt{Certificate.lean} yields $r(u,p)=0$ for every SOCP in this branch.

\section*{Normalization Target}
The main unresolved issue is no longer the representative algebra above. It is
the remaining normalization/classification step for the branches not already
covered by the abstract span-three theorem, namely the independent
constant-containing $\dim = 2$ configurations that should reduce, after affine
variable changes and orthogonal mixing of the four coordinates, to one of the
already-formalized low-affine or mixed-affine representatives.

\medskip
\noindent
The affine-normalization layer is now split across
\texttt{TernaryQuarticProof/AffineTransform.lean} and
\texttt{TernaryQuarticProof/AffineSocpTransform.lean}. The first file handles
the raw affine substitution $x \mapsto A x + b$: the induced algebra
homomorphism preserves the quadratic and quartic degree bounds and commutes
with the maps $A_u(\cdot)$, $\sigma$, and the residual after applying the
substitution coordinatewise. The second file upgrades the invertible case to an
actual transport statement: after moving the bilinear form by the inverse
equivalence, affine changes of variables preserve positive-definiteness,
admissibility, SOS quarticity, FOCP, SOCP, and the condition $r(u,p)=0$.

What still remains is the classification lemma producing the specific affine
and orthogonal normalization data for those remaining rank-$4$ factors. Once
that data exists, the representative theorems can be pulled back through these
two transport layers.

\medskip
\noindent
That pullback step is now also packaged explicitly in
\texttt{TernaryQuarticProof/RepresentativeTransport.lean}. The main theorem
there says: if, after applying an affine inverse on the polynomial side and an
orthogonal mixing on the factor side, the SOCP point becomes an already-solved
representative $u_{\mathrm{rep}}$, then the original point has zero residual.
So the remaining normalization problem is now sharply isolated to one kind of
statement:
\[
  \text{produce exact data with }
  \operatorname{mix}(M^\top,\; e^{-1}\!\cdot u) = u_{\mathrm{rep}}.
\]

\section*{Correction To The Previous Case Split}
The earlier five-case list is not complete. The constant-containing
$\dim(W \cap \mathbb{R}[x_1,x_2]_{\le 1}) = 2$ branch splits further.

\medskip
\noindent
Representative mixed-affine models recorded in the Julia script are:
\[
  (1,x_1,x_1^2,x_1x_2),\qquad
  (1,x_1,x_1x_2,x_2^2),\qquad
  (1,x_1,x_1^2,x_2^2).
\]
Their image ranks are respectively $13$, $14$, and $15$, with missing quotient
monomials
\[
  \{x_2^3,x_2^4\},\qquad \{x_1^4\},\qquad \varnothing.
\]
Moreover the first two mixed-affine representatives also admit explicit kernel
directions:
\[
  u=(1,x_1,x_1^2,x_1x_2),\qquad
  w_\ell=(0,0,-x_2\ell,x_1\ell),\qquad \ell=c+d x_2,
\]
for which
\[
  A_u(w_\ell)=0,\qquad
  \sigma(w_\ell)=(x_1^2+x_2^2)\ell^2 \equiv x_2^2\ell^2
  \equiv 2cd\,x_2^3 + d^2 x_2^4
  \pmod{\operatorname{im}(A_u)}.
\]
Hence this family generates the two missing quotient classes. For
\[
  u=(1,x_1,x_1x_2,x_2^2),
\]
the direction
\[
  w=(0,0,-x_1x_2,x_1^2)
\]
satisfies
\[
  A_u(w)=0,\qquad \sigma(w)=x_1^2x_2^2+x_1^4 \equiv x_1^4
  \pmod{\operatorname{im}(A_u)}.
\]
So the mixed-affine algebra itself now looks tractable; the old statement that
the proof was complete up to normalization was too optimistic because the
classification step still has to produce one of the formalized mixed-affine
plane types, not just a single concrete representative.

\medskip
\noindent
The current candidate normalization for the constant-containing
$\dim = 2$ branch is the following. After choosing coordinates so that
\[
  W \cap \mathbb{R}[x_1,x_2]_{\le 1} = \operatorname{span}(1,x_1),
\]
the remaining quadratic plane is a $2$-dimensional subspace of
\[
  \operatorname{span}(x_1^2,x_1x_2,x_2^2),
\]
hence the kernel of a nonzero binary quadratic
\[
  q = a x_1^2 + b x_1x_2 + c x_2^2
\]
in the dual $3$-space. Under shears and rescalings preserving the line
$\mathbb{R}x_1$, the quadratic $q$ should reduce to one of
\[
  x_2^2,\qquad x_1^2,\qquad x_1x_2,\qquad x_1^2 \pm x_2^2.
\]
The numerical sweep now recorded in
\texttt{julia/tq\_r4\_exploration\_2026\_04\_09.jl} matches this exactly:
\[
  x_2^2 \leadsto \operatorname{rank} 13,\qquad
  x_1^2 \leadsto \operatorname{rank} 14,
\]
while
\[
  x_1x_2,\qquad x_1^2 \pm x_2^2,\qquad x_1^2 + x_1x_2,
\]
and the tested generic examples all have image rank $15$, with numerical
errors again near $10^{-10}$ or smaller. So the only nonsurjective
mixed-affine representatives appear to be
\[
  (1,x_1,x_1^2,x_1x_2),\qquad (1,x_1,x_1x_2,x_2^2).
\]
The written reduction is therefore no longer mysterious: it should be the
binary-quadratic completion-of-squares argument above, implemented inside the
subgroup of affine changes preserving the line $\mathbb{R}x_1$.

\medskip
\noindent
The first algebraic half of this reduction is now formalized in
\texttt{TernaryQuarticProof/MixedAffineNormalization.lean}. That file defines
the shear $x_2 \mapsto t x_1 + x_2$ and the scaling $x_2 \mapsto s x_2$ as
explicit affine polynomial equivalences, and proves the key formulas
\[
  a x_1^2 + b x_1x_2 + c x_2^2
  \;\longmapsto\;
  \Bigl(a - \frac{b^2}{4c}\Bigr) x_1^2 + c x_2^2
  \qquad (c \neq 0),
\]
and
\[
  a x_1^2 + b x_1x_2
  \;\longmapsto\;
  b x_1x_2
  \qquad (b \neq 0),
\]
together with the diagonal scaling formula. In this round, the same file now
packages the sign cases as explicit normal-form equalities:
\[
  a x_1^2 + c x_2^2 \leadsto a(x_1^2+x_2^2)
  \qquad \text{when } a/c>0,
\]
and
\[
  a x_1^2 + c x_2^2 \leadsto a(x_1^2-x_2^2)
  \qquad \text{when } -a/c>0,
\]
after the scaling \(x_2 \mapsto \sqrt{a/c}\,x_2\) or
\(x_2 \mapsto \sqrt{-a/c}\,x_2\). It also combines these with the shear step,
so the cross-killed diagonal form now normalizes directly to the sum-square or
split-diagonal annihilator whenever the corresponding ratio is positive. The
same Lean file now packages the whole coefficient case split as a single theorem
\texttt{mixedAffineAnnihilator\_normal\_form\_cases}. So the remaining work is
no longer to discover the completion-of-squares algebra, but to connect these
explicit affine moves to a general rank-$4$ mixed-affine factor and then feed
the result into the already-formalized representative theorems.

\medskip
\noindent
There is one useful refinement of that plan. After the shear step, the
surjective non-rank-one case naturally appears as a split diagonal annihilator
\[
  x_1^2 - x_2^2,
\]
whose kernel plane is
\[
  \operatorname{span}(x_1x_2,\ x_1^2+x_2^2).
\]
This plane is not one of the two canonical surjective models already stated
above, but it is linearly equivalent to
\[
  \operatorname{span}(x_1^2,x_2^2)
\]
via the fixed change of variables
\[
  (x_1,x_2) \longmapsto (x_1+x_2,\ x_1-x_2).
\]
Under that map,
\[
  x_1x_2 \longmapsto x_1^2-x_2^2,
  \qquad
  x_1^2+x_2^2 \longmapsto 2(x_1^2+x_2^2),
\]
so any invertible basis of
\[
  \operatorname{span}(x_1x_2,\ x_1^2+x_2^2)
\]
transports to an invertible basis of
\[
  \operatorname{span}(x_1^2,x_2^2).
\]
Therefore the split diagonal case can be discharged by a fixed transport lemma,
and the remaining mixed-affine normalization can stay inside the explicit
translation-shear-scaling formulas already formalized.

\medskip
\noindent
The current plan for that last connection is to work in explicit quadratic
coordinates. Every quadratic polynomial can be written uniquely as
\[
  a_{00} + a_{10} x_1 + a_{01} x_2 + a_{20} x_1^2 + a_{11} x_1x_2 + a_{02} x_2^2.
\]
Inside the affine subgroup preserving the line $\mathbb{R}x_1$, the three basic
moves needed for the mixed-affine branch are:
\[
  x_2 \mapsto x_2 + t,\qquad
  x_2 \mapsto s x_1 + x_2,\qquad
  x_2 \mapsto s x_2.
\]
Their effect on the six coefficients is completely explicit. In particular, the
translation controls the affine tail $(a_{00},a_{10},a_{01})$, while the
shear-and-scale formulas act on the homogeneous triple
\[
  (a_{20},a_{11},a_{02})
\]
by the same completion-of-squares identities already proved in
\texttt{MixedAffineNormalization.lean}. The new file
\texttt{QuadraticCoordinateForm.lean} now also packages this at the level of
arbitrary quadratics: it defines the translation equivalence $x_2 \mapsto x_2+t$
inside the transport framework and proves the generic coefficient identities
\[
  \operatorname{coeff}_{x_2}\bigl(q(x_1,x_2+t)\bigr)
  =
  \operatorname{coeff}_{x_2}(q) + 2 t\, \operatorname{coeff}_{x_2^2}(q),
\]
\[
  \operatorname{coeff}_{x_1x_2}\bigl(q(x_1+t x_2,x_2)\bigr)
  =
  \operatorname{coeff}_{x_1x_2}(q) + 2 t\, \operatorname{coeff}_{x_2^2}(q),
\]
together with the corresponding $x_1^2$, $x_1x_2$, and $x_2^2$ formulas under
translation, shear, and scaling. So the remaining normalization problem
can be reframed as follows: extract from a general constant-containing
$\dim = 2$ factor the relevant coefficient data, kill the unwanted affine tail,
read off the binary-quadratic annihilator of the resulting homogeneous
$2$-plane, and then apply the already-formalized rank-$13$, rank-$14$, or
rank-$15$ plane theorem.

\medskip
\noindent
At this point the missing paper-level step is no longer ``find the right
normalization formulas'' but ``apply the already-formalized formulas with the
right chosen parameters.'' Concretely, for a quadratic
\[
  q = a_{00} + a_{10} x_1 + a_{01} x_2 + a_{20} x_1^2 + a_{11} x_1x_2 + a_{02} x_2^2,
\]
the intended normalization now runs as follows:
\[
  t_{\mathrm{tr}} = -\frac{a_{01}}{2 a_{02}}
  \qquad (a_{02} \neq 0)
\]
kills the $x_2$ coefficient under translation $x_2 \mapsto x_2+t_{\mathrm{tr}}$,
while
\[
  t_{\mathrm{sh}} = -\frac{a_{11}}{2 a_{02}}
  \qquad (a_{02} \neq 0)
\]
kills the $x_1x_2$ coefficient under the shear $x_2 \mapsto x_2+t_{\mathrm{sh}}x_1$,
leaving
\[
  a_{20} - \frac{a_{11}^2}{4 a_{02}}
\]
as the new $x_1^2$ coefficient. In the degenerate case $a_{02}=0$ but
$a_{11}\neq 0$, the parameter
\[
  t_{\mathrm{sh}} = -\frac{a_{20}}{a_{11}}
\]
instead kills the $x_1^2$ coefficient and reduces directly to the pure-cross
orbit. So the remaining formal gap is to convert these scalar coefficient
choices into relation data for the mixed-affine plane theorems.

\section*{Current Candidate Blueprint}
At the level of a paper proof, the present candidate argument is:

\begin{enumerate}
\item If the four coordinates of $u$ are linearly dependent over $\mathbb{R}$,
apply the already-formalized constant-relation lemma.
\item Otherwise let $W = \operatorname{span}(u_1,u_2,u_3,u_4)$ and classify by
the dimension of $W \cap \mathbb{R}[x_1,x_2]_{\le 1}$.
\item If $\dim(W \cap \mathbb{R}[x_1,x_2]_{\le 1}) = 3$, choose coefficient
  vectors spanning $1,x_1,x_2$ inside $W$ and apply the abstract theorem
  \texttt{residual\_eq\_zero\_of\_contains\_aff1}. No exact representative
  normalization is needed in this branch.
\item Otherwise use orthogonal changes in the factor coordinates together with
  affine changes of variables in $(x_1,x_2)$ to reduce the remaining stable
  branches to the following models:
  \[
    (x_1,x_2,x_1^2,x_1x_2),\quad
    (x_1,x_2,x_1^2,x_2^2),\quad
    (1,x_1^2,x_1x_2,x_2^2),\quad
    (x_1,x_1^2,x_1x_2,x_2^2).
  \]
\item In these stable models, show directly that every quadratic square lies in
  \[
    \operatorname{im}(A_u) + \operatorname{cone}(\sigma(\ker A_u))
  \]
  using explicit kernel families:
  \begin{itemize}
  \item $(x_1,x_2,x_1^2,x_1x_2)$:
    \[
      w_c=(-2x_2,x_1,0,1),\qquad
      w_q=(-x_2^2,x_1x_2,0,0);
    \]
  \item $(x_1,x_2,x_1^2,x_2^2)$:
    \[
      w=(-x_1,0,1,0);
    \]
  \item $(1,x_1^2,x_1x_2,x_2^2)$: surjective image, no kernel term needed;
  \item $(x_1,x_1^2,x_1x_2,x_2^2)$:
    \[
      w_\ell=(-x_2\ell,0,\ell,0),\qquad \ell=c+d x_2.
    \]
  \end{itemize}
\item The model
  \[
    (1,x_1^2,x_1x_2,x_2^2)
  \]
  is already formalized: its image is all quartics.
\item Treat the mixed-affine branch by separating the two nonsurjective models
  \[
    (1,x_1,x_1^2,x_1x_2),\qquad
    (1,x_1,x_1x_2,x_2^2),
  \]
  from the surjective rank-$15$ orbits. The representative quotient generators
  are now explicit: the first model uses the family
  \[
    w_\ell=(0,0,-x_2\ell,x_1\ell),\qquad \ell=c+d x_2,
  \]
  the second uses
  \[
    w=(0,0,-x_1x_2,x_1^2),
  \]
  and the hyperbolic rank-$15$ model
  \[
    (1,x_1,x_1^2,x_2^2)
  \]
  is formalized as surjective. The definite rank-$15$ orbit
  \[
    \operatorname{span}(x_1x_2,\ x_1^2-x_2^2)
  \]
  is now formalized as surjective at the plane level as well. The remaining
  mixed-affine gap is no longer the annihilator case split itself, since
  \texttt{MixedAffineNormalization.lean} now packages that as
  \texttt{mixedAffineAnnihilator\_normal\_form\_cases}. What is still missing is
  the written proof that every constant-containing $\dim = 2$ factor supplies
  the coefficient data and relation vectors needed to invoke that theorem and
  then land in one of these formalized plane types.
\item A smaller replacement for exact representative transport is now plausible
  in this branch: prove basis-invariant mixed-affine plane theorems. Instead of
  forcing the last two coordinates to be exactly
  \[
    (x_1^2,x_1x_2)\quad\text{or}\quad(x_1x_2,x_2^2),
  \]
  one can work with an arbitrary orthonormal coefficient-space basis of the
  quadratic plane
  \[
    x_1 \cdot \operatorname{span}(x_1,x_2)
    \qquad\text{or}\qquad
    x_2 \cdot \operatorname{span}(x_1,x_2),
  \]
  and build the kernel family directly from the syzygy
  \[
    (x_1 a)(b \ell) - (x_1 b)(a \ell)=0
  \]
  or its $x_2$-analogue. In these plane-type formulations, the image part
  depends only on the plane itself, while orthonormality restores the clean
  $\sigma = (a^2+b^2)\ell^2$ identity used in the representative proofs. The
  current Lean support for this approach is now in place: the new file
  \texttt{RepresentativeMixedAffinePlane.lean} proves that any explicit scalar
  relation $\sum c_i u_i=r$ yields an admissible image witness for $rq$, that
  two orthonormal coefficient-space relation directions satisfy
  \[
    \sigma\bigl(\mathrm{rel}_c(p)+\mathrm{rel}_d(q)\bigr)=p^2+q^2.
  \]
  More importantly, it now abstracts the whole image half of the rank-$14$
  mixed-affine proof: if a factor admits explicit relations for
  \[
    1,\qquad x_1,\qquad x_1x_2,\qquad x_2^2,
  \]
  then every quartic with zero $x_1^4$ coefficient lies in its admissible
  image. The certificate half is now abstracted too: if those same relations
  are accompanied by an orthonormal coefficient-space pair for the
  $(x_1x_2,x_2^2)$ directions, then the formalized kernel
  \[
    w=\mathrm{rel}_{-c_2}(t x_1x_2)+\mathrm{rel}_{c_3}(t x_1^2)
  \]
  matches the missing $x_1^4$ coefficient and forces zero residual for every
  SOCP. So the missing step is no longer the monomial image or kernel algebra
  itself, but the extraction of the correct canonical relation data from an
  arbitrary factor.
\item The rank-$13$ mixed-affine branch is now abstracted in the same style.
  The new plane-level theorem only assumes explicit scalar relations for
  \[
    1,\qquad x_1^2,\qquad x_1x_2,
  \]
  plus an orthonormal coefficient-space pair for the last two directions. Under
  those hypotheses, every quartic with vanishing $x_2^3$ and $x_2^4$
  coefficients lies in the admissible image, and the formalized kernel
  \[
    w=\mathrm{rel}_{-c_2}(x_2\ell)+\mathrm{rel}_{c_3}(x_1\ell),
    \qquad \ell=b+c x_2,
  \]
  matches the missing $(x_2^3,x_2^4)$ classes and forces zero residual for
  every SOCP. So both nonsurjective mixed-affine representatives are now
  available as basis-invariant plane certificates rather than only as concrete
  coordinate models.
\item The old stronger-basis attempt has now split into two clean formalized
  layers. First, \texttt{RepresentativeMixedAffinePlane.lean} now proves exact
  surjective plane theorems for any invertible basis of
  \[
    \operatorname{span}(x_1^2,x_2^2)
    \qquad\text{and}\qquad
    \operatorname{span}(x_1x_2,\ x_1^2-x_2^2),
  \]
  together with algebra-equivalence transport wrappers. Second, for the two
  nonsurjective planes
  \[
    \operatorname{span}(x_1x_2,\ x_2^2)
    \qquad\text{and}\qquad
    \operatorname{span}(x_1^2,\ x_1x_2),
  \]
  the file first proved orthogonal in-plane basis reductions back to the exact
  rank-$14$ and rank-$13$ certificates, and now those same nonsurjective
  theorems have been strengthened to allow orthogonal but not necessarily unit
  column data, again wrapped through arbitrary polynomial algebra
  equivalences. So the remaining gap has tightened again: one no longer needs
  to normalize the column lengths inside the mixed-affine plane, but one still
  needs the normalization/classification step to produce from a general
  mixed-affine factor either an invertible surjective plane basis or an
  orthonormal relation basis whose polynomial images lie in one of the two
  canonical nonsurjective planes. The intended bridge is now an explicit
  six-coefficient quadratic normal form plus formulas for the
  $x_2$-translation, $x_2$-shear, and $x_2$-scaling subgroup preserving
  $\mathbb{R}x_1$.

\medskip
\noindent
The sharper nonsurjective-plane picture has now partially been formalized. For
the rank-$14$ plane \(\operatorname{span}(x_1x_2,x_2^2)\), the Lean file now
proves the stronger determinant-based theorem:
\begin{itemize}
\item any orthonormal relation basis with polynomial images
  \[
    x_2(a x_1 + b x_2),\qquad x_2(c x_1 + d x_2),
  \]
  and
  \[
    \det\!\begin{pmatrix}a&b\\ c&d\end{pmatrix}\neq 0
  \]
  already
  forces zero residual;
\item the proof reconstructs the exact \(x_1x_2\) and \(x_2^2\) relations by
  the inverse \(2\times 2\) matrix and uses the kernel syzygy
  \[
    -x_1(c x_1+d x_2)\cdot x_2(a x_1+b x_2)
    +x_1(a x_1+b x_2)\cdot x_2(c x_1+d x_2)=0
  \]
  whose square still misses only the \(x_1^4\) coefficient, now scaled by
  \(a^2+c^2\neq 0\).
\end{itemize}
So the rank-$14$ branch no longer needs any extra polynomial-column
orthogonality inside the plane. The analogous rank-$13$ strengthening is now
formalized as well:
\begin{itemize}
\item for the rank-$13$ plane \(\operatorname{span}(x_1^2,x_1x_2)\), any
  orthonormal relation basis with polynomial images
  \[
    x_1(a x_1+b x_2),\qquad x_1(c x_1+d x_2)
  \]
  and
  \[
    \det\!\begin{pmatrix}a&b\\ c&d\end{pmatrix}\neq 0
  \]
  already forces zero residual;
\item the proof uses the kernel
  \[
    \mathrm{rel}_{-c_2}\bigl((c x_1+d x_2)(\beta+\gamma x_2)\bigr)
    + \mathrm{rel}_{c_3}\bigl((a x_1+b x_2)(\beta+\gamma x_2)\bigr),
  \]
  because
  \[
    -x_1(a x_1+b x_2)\cdot (c x_1+d x_2)(\beta+\gamma x_2)
    +x_1(c x_1+d x_2)\cdot (a x_1+b x_2)(\beta+\gamma x_2)=0.
  \]
  Its square still affects only the pure \(x_2^3\) and \(x_2^4\)
  coefficients modulo the already-formalized image, now scaled by
  \(b^2+d^2\neq 0\).
\end{itemize}
So neither nonsurjective mixed-affine plane now needs an extra polynomial-column
orthogonality condition. The normalization only has to produce the canonical
plane type and a nonzero determinant inside that plane, not a special
orthogonal basis of polynomial columns. Both determinant-based theorems are
also wrapped through the affine transport layer, so once a normalized factor
lands in either canonical plane type, the result pulls back directly to the
original SOCP point.
\item Apply the formalized admissible image-plus-cone certificate to conclude
$\sigma(u)=p$.
\end{enumerate}

\noindent
The main obstacle to a finished Lean proof is now precise: complete the
normalization/classification of the constant-containing $\dim = 2$ branch, then
assemble the final theorem from the already-formalized representative and
abstract branch theorems.

\medskip
\noindent
That six-monomial normal form is now formalized in
\texttt{TernaryQuarticProof/QuadraticNormalForm.lean}. It introduces the six
quadratic monomial exponents
\[
  1,\ x_1,\ x_2,\ x_1^2,\ x_1x_2,\ x_2^2
\]
as explicit \texttt{Fin 2 ->0 Nat} indices, proves that every support monomial of
an \texttt{IsQuadratic} polynomial is one of them, and concludes that every
quadratic is the sum of its six monomial-coefficient terms. This is still a
proof-serving finite-support lemma rather than a general coefficient API.

\medskip
\noindent
The mixed-affine analysis is now complete at the representative-or-plane level:
the rank-$13$ and rank-$14$ nonsurjective models are formalized both as concrete
representatives and as abstract plane certificates, now in a form that also
accepts non-unit orthogonal in-plane coefficient data and transports directly
across affine algebra equivalences, and the surjective rank-$15$ branch is
formalized for both real plane orbits
\[
  \operatorname{span}(x_1^2,x_2^2)
  \qquad\text{and}\qquad
  \operatorname{span}(x_1x_2,\ x_1^2-x_2^2).
\]
The new bridge layer is also formalized now: using the six-coefficient
quadratic normal form, Lean proves that a homogeneous quadratic satisfying one
of the canonical coefficient conditions
\[
  \operatorname{coeff}_{x_1x_2}=0,\qquad
  \operatorname{coeff}_{x_1^2}=0,\qquad
  \operatorname{coeff}_{x_2^2}=0,\qquad
  \operatorname{coeff}_{x_1^2}\pm \operatorname{coeff}_{x_2^2}=0
\]
already has the corresponding canonical plane expansion. The mixed-affine plane
file now wraps those coefficient lemmas into generic relation theorems, so once
normalization produces homogeneous quadratics in one of those coefficient
orbits, the existing rank-$13$, rank-$14$, or rank-$15$ plane certificates
apply directly.
So the remaining obstacle is no longer a missing mixed-affine representative
calculation. One correction is needed here: the polynomial identity
\texttt{mixedAffineAnnihilator\_normal\_form\_cases} describes the covariant
action of shears and rescalings on the annihilator polynomial
\(a x_1^2 + b x_1x_2 + c x_2^2\), while the actual classification of the
mixed-affine branch is governed by the dual action on the homogeneous
coefficient functionals
\[
  (\operatorname{coeff}_{x_1^2},\ \operatorname{coeff}_{x_1x_2},\
  \operatorname{coeff}_{x_2^2}).
\]
That dual action is now formalized in
\texttt{QuadraticCoordinateForm.lean}, and
\texttt{RepresentativeMixedAffinePlane.lean} already uses it in the two new
transport wrappers
\texttt{residual\_eq\_zero\_of\_equiv\_relations\_const\_homQuadratics\_crossAnnihilator}
and
\texttt{residual\_eq\_zero\_of\_equiv\_relations\_const\_x0\_homQuadratics\_annihilator\_disc\_zero}.
The same file now also handles the purely diagonal dual relation
\(a\,\operatorname{coeff}_{x_1^2} + d\,\operatorname{coeff}_{x_2^2}=0\) by a
single \(x_2\)-scaling, via the wrappers
\texttt{residual\_eq\_zero\_of\_equiv\_relations\_const\_homQuadratics\_diagAnnihilator\_sum}
and
\texttt{residual\_eq\_zero\_of\_equiv\_relations\_const\_homQuadratics\_diagAnnihilator\_diff},
which reduce directly to the already-formalized sum-square and split-diagonal
plane theorems.
So the remaining obstacle is now the written and formal extraction statement
showing that every constant-containing
$\dim(W \cap \mathbb{R}[x_1,x_2]_{\le 1}) = 2$ factor reduces to one of these
already-formalized plane types, together with the exact dual relation,
sign, and determinant data needed to invoke the transport theorems.

\medskip
\noindent
The low-affine representative analysis is now complete too in
\texttt{TernaryQuarticProof/RepresentativeLowAffine.lean}. The three stable
models
\[
  (x_1,x_2,x_1^2,x_2^2),\qquad
  (x_1,x_2,x_1^2,x_1x_2),\qquad
  (x_1,x_1^2,x_1x_2,x_2^2)
\]
are all formalized at the representative level. The first two use explicit
codimension-one or codimension-two image-plus-kernel corrections at the
constant and quartic-end classes, while the third uses the affine kernel line
\[
  w_\ell = (-x_2\ell,0,\ell,0), \qquad \ell = c + d x_2,
\]
to match the missing quotient classes of $1$ and $x_2$. So the only remaining
representative-level model outside the mixed-affine normalization problem is
the generic
\[
  (1,x_1,x_2,h)
\]
branch.

\medskip
\noindent
This low-affine upgrade needed one correction. The original writeup treated
only two \(\dim=2\) no-constant branches as canonical, but the exploration log
already contained the additional rank-\(14\) definite plane
\[
  \operatorname{span}(x_1x_2,\ x_1^2-x_2^2),
\]
for example through the probe
\[
  (x_1,x_2,x_1^2-x_2^2,x_1x_2),
\]
which numerically has image rank \(14\). The file
\texttt{TernaryQuarticProof/RepresentativeLowAffine.lean} no longer stops at
the exact representatives
\[
  (x_1,x_2,x_1^2,x_2^2)
  \qquad\text{and}\qquad
  (x_1,x_2,x_1^2,x_1x_2).
\]
It now proves basis-invariant plane theorems from explicit relations to
\(x_1,x_2\) together with any invertible basis of the canonical quadratic
planes
\[
  \operatorname{span}(x_1^2,x_2^2)
  \qquad\text{and}\qquad
  \operatorname{span}(x_1^2,x_1x_2).
\]
In addition, the new exact-relation theorem
\texttt{residual\_eq\_zero\_of\_relations\_x0\_x1\_x0x1\_diffsq}
and its determinant plane wrapper
\texttt{residual\_eq\_zero\_of\_relations\_x0\_x1\_x0x1\_diffsqPlane}
now formalize the definite rank-\(14\) orbit
\[
  \operatorname{span}(x_1x_2,\ x_1^2-x_2^2)
\]
as well. The split rank-\(14\) orbit
\[
  \operatorname{span}(x_1x_2,\ x_1^2+x_2^2)
\]
is now packaged by the fixed transport theorem
\texttt{residual\_eq\_zero\_of\_relations\_x0\_x1\_x0x1\_sumsqPlane},
which sends it to the already-formalized coprime plane
\(\operatorname{span}(x_1^2,x_2^2)\). So the final normalization step no
longer needs exact representative equalities in the low-affine \(\dim=2\)
cases either: it only has to recover the linear part and the resulting plane
type.

\medskip
\noindent
The linear low-affine model has now also been lifted through the same
\(3\times 3\) homogeneous reconstruction. The same file no longer uses only
the rigid representative
\[
  (x_1,x_1^2,x_1x_2,x_2^2),
\]
but proves the exact-relation theorem for any admissible factor $u$ with
coefficient vectors satisfying
\[
  \sum_i c_{0,i}u_i = x_1,\qquad
  \sum_i c_{1,i}u_i = x_1^2,\qquad
  \sum_i c_{2,i}u_i = x_1x_2,\qquad
  \sum_i c_{3,i}u_i = x_2^2.
\]
The image reduction is now abstract at that level, and the kernel-line
argument has been lifted to the same exact relation data. The new
\texttt{exact\_relations\_of\_homQuadBasis\_det} lemma now reconstructs those
exact canonical relations from any independent homogeneous quadratic triple, so
the linear low-affine branch also has a basis-invariant homogeneous-basis
theorem and an affine-equivalence transport wrapper.

\section*{Pending Step}
To finish the theorem, it remains to formalize the remaining normalization and
classification data, especially for the constant-containing mixed-affine
$\dim=2$ branch, and then assemble the root declaration in
\texttt{TernaryQuarticProof.lean}. The admissible image-plus-cone certificate
and the representative/plane-level no-spurious-SOCP theorems are already in
place.

\section*{Planned File Map}
\begin{itemize}
\item \texttt{TernaryQuarticProof/Socp.lean}: abstract SOCP consequences.
\item \texttt{TernaryQuarticProof/Certificate.lean}: admissible transformed
image-plus-cone certificate.
\item \texttt{TernaryQuarticProof/FactorTransform.lean}: orthogonal
factor-coordinate invariance.
\item \texttt{TernaryQuarticProof/QuadraticNormalForm.lean}: finite-support
normal form for two-variable quadratics;
\item one future algebra module for the remaining representative image and
image-plus-kernel decompositions;
\item \texttt{TernaryQuarticProof.lean}: final theorem assembly.
\end{itemize}

\end{document}
