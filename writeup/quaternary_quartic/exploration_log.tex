\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage[T1]{fontenc}
\usepackage{hyperref}

\title{Quaternary Quartic Rank-7 Exploration Log}
\author{}
\date{}

\begin{document}
\maketitle

\section*{2026-05-04: Rank-deficient branch formalized}

\paragraph{Starting state.}
The Lean root theorem
\texttt{QuaternaryQuartic.quaternaryQuartic\_rankSeven\_no\_spurious\_socp}
was still proved through the scaffold axiom
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_placeholder}.
The full verification harness therefore built the files but failed at
\texttt{\#print axioms}, as expected.

\paragraph{Largest gap selected.}
The main blueprint splits first on whether the seven quadratic coordinates
\(u_i\) are linearly dependent.  The rank-deficient branch is independent of
the low-rank catalecticant support theorem and can be closed directly from the
SOCP inequalities and the SOS certificate.  This made it the largest remaining
gap that could be reduced without first formalizing the apolar Hilbert-function
argument.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Certificate.lean}.  It defines:
\[
\texttt{relationDirection}\ c\ q=(i\mapsto c_i q),\qquad
\texttt{relationPoly}\ u\ c=\sum_i c_i u_i,
\]
plus the supporting admissibility, \(A\)-image, and \(\sigma\)-formula lemmas.
The main new theorem is
\[
\texttt{residual\_eq\_zero\_of\_constant\_relation}.
\]
It proves that if there is a nonzero scalar relation
\(\sum_i c_i u_i=0\), then any SOS/SOCP point has
\(\texttt{residual}\ p\ u=0\).

\paragraph{Proof idea.}
For each SOS summand \(q_j^2\), use the kernel direction
\(w_i=c_i q_j\).  Then \(A_u(w)=0\) and
\[
\sigma(w)=\left(\sum_i c_i^2\right)q_j^2.
\]
The Hessian inequality gives
\(\left(\sum_i c_i^2\right)B(q_j^2,R)\ge0\), and the scalar is positive
because \(c\ne0\).  Summing over the SOS certificate gives \(B(p,R)\ge0\).
FOCP at \(v=u\) gives \(B(\sigma(u),R)=0\), hence the objective equals
\(-B(p,R)\le0\).  Positive-definiteness gives the opposite inequality, so the
objective is zero and the residual vanishes.

\paragraph{Verification.}
The new module and root proof file typecheck:
\[
\texttt{lake env lean QuaternaryQuarticProof/Certificate.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}.
\]
The full harness command available in the repository is
\texttt{./scripts/verify\_quaternary\_quartic.sh}; the user-provided
\texttt{verify\_quarternary\_quartic.sh} path does not exist.  The full harness
still fails only because the root theorem depends on the existing placeholder
axiom.

\paragraph{Remaining blockers.}
The largest remaining gap is the full-rank branch.  The next proof-serving
steps are:
\begin{enumerate}
\item formalize the catalecticant kernel \(K_\lambda\) and prove
  \(L_u\subseteq K_\lambda\) from FOCP;
\item express full rank of the seven coordinates as
  \(\ker(\texttt{relationPolyLin}\ u)=\bot\), yielding \(\dim L_u=7\);
\item formalize the low-rank catalecticant support theorem and the rank
  \(1,2,3\) syzygy-to-negative-curvature constructions.
\end{enumerate}

No Julia numerical claims were used in this round.

\section*{2026-05-04: Catalecticant setup and negative SOS summand}

\paragraph{Largest gap selected.}
After the rank-deficient branch, the next full-rank prerequisite in the
blueprint is the residual functional and catalecticant kernel setup.  This
precedes the low-rank support theorem and is needed in every \(k=1,2,3\)
case.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Catalecticant.lean} and wired it through
\texttt{QuaternaryQuarticProof/Basic.lean}.  The new module defines:
\[
\texttt{quadSubmodule},\qquad
\texttt{spanU}\ u=\operatorname{span}\{u_i\},\qquad
\texttt{catalecticantKernel}\ B\ p\ u.
\]
The catalecticant kernel is the submodule of quadratic polynomials \(q\) such
that
\[
  B(qr,\texttt{residual}\ p\ u)=0
  \quad\text{for every quadratic }r.
\]

\paragraph{Formalized lemmas.}
The theorem
\texttt{spanU\_le\_catalecticantKernel} proves the blueprint lemma
\(L_u\subseteq K_\lambda\) from FOCP, by testing the single-coordinate
direction \(v=\texttt{Pi.single}\ i\ r\).

The theorem \texttt{target\_dot\_residual\_negative} proves that if
\(\texttt{residual}\ p\ u\ne0\), then
\[
  B(p,\texttt{residual}\ p\ u)<0.
\]
The theorem \texttt{exists\_negative\_sos\_summand} then extracts a quadratic
SOS summand \(q\) with
\[
  B(q^2,\texttt{residual}\ p\ u)<0.
\]
The combined theorem
\texttt{exists\_negative\_sos\_summand\_of\_nonzero\_residual} packages these
two steps for later use.

\paragraph{Verification.}
The new module and root proof file typecheck:
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}.
\]

\paragraph{Remaining blockers.}
The largest remaining gap is now the finite-dimensional full-rank bridge:
connect \(\ker(\texttt{relationPolyLin}\ u)=\bot\) with
\(\dim(\texttt{spanU}\ u)=7\), restrict the catalecticant quotient to the
10-dimensional quadratic submodule, and prove or import the low-rank
support/syzygy constructions without axioms.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Full-rank span dimension bridge}

\paragraph{Largest gap selected.}
The full-rank branch needs a formal way to turn ``the seven coordinates have
no scalar relation'' into the dimension statement \(\dim L_u=7\).  This is the
input used with \(L_u\subseteq K_\lambda\) to force catalecticant rank at most
three.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Catalecticant.lean} with:
\[
\texttt{spanU\_le\_quadSubmodule},\qquad
\texttt{linearIndependent\_u\_of\_relationPolyLin\_ker\_eq\_bot},\qquad
\texttt{finrank\_spanU\_eq\_seven\_of\_relationPolyLin\_ker\_eq\_bot}.
\]
The last theorem proves
\[
  \ker(\texttt{relationPolyLin}\ u)=\bot
  \Longrightarrow
  \operatorname{finrank}_{\mathbb R}(\texttt{spanU}\ u)=7.
\]

\paragraph{Verification.}
The modified catalecticant module and root proof file typecheck:
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}.
\]

\paragraph{Remaining blockers.}
The next largest gap is to make the rank bound precise on the quadratic
submodule: define the restricted catalecticant map
\[
  \texttt{quadSubmodule}\to(\texttt{quadSubmodule}\to_{\mathbb R}\mathbb R)
\]
and prove that \(L_u\subseteq K_\lambda\) and \(\dim L_u=7\) imply rank at most
three.  After that, the main remaining mathematical input is the low-rank
support theorem and the rank \(1,2,3\) syzygy constructions.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Quadratic submodule tied to mathlib degree filtration}

\paragraph{Largest gap selected.}
The rank bound requires finite-dimensional control of the quadratic submodule.
Rather than maintain a standalone hand-built submodule, the proof should reuse
mathlib's existing total-degree filtration for multivariate polynomials.

\paragraph{Lean progress.}
Added
\[
\texttt{quadSubmodule\_eq\_restrictTotalDegree}
\]
showing that \texttt{quadSubmodule} is exactly
\[
  \texttt{MvPolynomial.restrictTotalDegree (Fin 3) }\mathbb R\texttt{ 2}.
\]
Also added the finite-module instance
\[
\texttt{instModuleFiniteQuadSubmodule}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}
\]
both typecheck after this change.

\paragraph{Remaining blockers.}
The exact numerical dimension \(\dim(\texttt{quadSubmodule})=10\) is still
open.  The next likely route is to use the basis of
\texttt{MvPolynomial.restrictTotalDegree} indexed by exponent vectors of total
degree at most two, then count those exponent vectors for \(\texttt{Fin 3}\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Quadratic dimension proved}

\paragraph{Largest gap selected.}
The immediate blocker for the catalecticant rank bound was the exact dimension
of the quadratic submodule.  The blueprint needs
\(\dim \Quad=10\), and the previous round had only connected \(\Quad\) to
mathlib's total-degree filtration.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_quadSubmodule\_eq\_ten} :
  \operatorname{finrank}_{\mathbb R}(\texttt{quadSubmodule})=10.
\]
The proof uses \texttt{MvPolynomial.basisRestrictSupport} for the
total-degree-bounded monomial basis.  The indexing subtype
\[
\{s:\texttt{Fin 3}\to_0\mathbb N : \sum_i s_i\le2\}
\]
is counted by an equivalence with bounded triples
\[
\{f:\texttt{Fin 3}\to\texttt{Fin 3} : \sum_i f_i\le2\}.
\]
The final finite cardinal computation is closed by \texttt{decide}, not
\texttt{native\_decide}, so it introduces no extra axiom.

\paragraph{Verification.}
The module builds, and
\[
\texttt{\#print axioms QuaternaryQuartic.finrank\_quadSubmodule\_eq\_ten}
\]
reports only
\[
\texttt{[propext, Classical.choice, Quot.sound]}.
\]

\paragraph{Remaining blockers.}
The largest remaining gap is the restricted catalecticant rank inequality:
define the linear map \(\Quad\to\Quad^\ast\), identify its kernel with the
quadratic catalecticant kernel, and prove that the full-rank branch gives
rank at most \(10-7=3\).  After that, the main mathematical burden remains
the low-rank support theorem and the rank \(1,2,3\) syzygy constructions.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Restricted catalecticant rank bound}

\paragraph{Largest gap selected.}
After proving \(\dim \Quad=10\), the remaining finite-dimensional bridge was
to define the actual restricted catalecticant map and prove the full-rank
rank bound \(k\le3\).

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Catalecticant.lean} with:
\[
\texttt{spanUQuad},\quad
\texttt{catalecticantMap},\quad
\texttt{spanUQuad\_le\_ker\_catalecticantMap}.
\]
The map is
\[
  \Quad\to\Quad^\ast,\qquad
  q\mapsto(r\mapsto B(qr,\texttt{residual}\ p\ u)).
\]
The inclusion theorem proves that FOCP places the seven-dimensional span of
the coordinates inside this kernel.

The main new theorem is
\[
\texttt{catalecticantMap\_rank\_le\_three\_of\_relationPolyLin\_ker\_eq\_bot}.
\]
It combines:
\[
\dim \Quad=10,\qquad
\dim L_u=7,\qquad
L_u\subseteq\ker C_\lambda,
\]
with rank-nullity to conclude
\[
  \operatorname{finrank}(\operatorname{range} C_\lambda)\le3.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks after the change.

\paragraph{Remaining blockers.}
The largest remaining mathematical gap is now the low-rank structural input:
for catalecticant rank \(1,2,3\), produce the support reductions and syzygies
from the blueprint.  The reusable negative-curvature construction for a
syzygy is also still pending in Lean.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Main split reduced to full rank}

\paragraph{Largest gap selected.}
The blueprint's main proof first splits on whether the seven coordinates have
a nonzero scalar relation.  The rank-deficient certificate was formalized
earlier, but the reusable main split had not yet been packaged.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Certificate.lean} with:
\[
\texttt{residual\_eq\_zero\_of\_relationPolyLin\_ker\_ne\_bot}
\]
and
\[
\texttt{residual\_eq\_zero\_or\_relationPolyLin\_ker\_eq\_bot}.
\]
The second theorem proves that any admissible SOS/SOCP point either already
has zero residual or has full scalar rank
\[
\ker(\texttt{relationPolyLin}\ u)=\bot.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Certificate.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The root theorem can now reduce to the full-rank branch.  The unresolved
content is entirely in that branch: use the rank-\(\le3\) catalecticant bound,
the negative SOS summand, and the low-rank support/syzygy mechanisms to
construct a negative-curvature direction.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Negative-curvature contradiction isolated}

\paragraph{Largest gap selected.}
All rank \(1,2,3\) syzygy arguments end the same way: construct an
admissible direction \(v\) with \(A_u(v)=0\) and
\[
  B(\sigma(v),\texttt{residual}\ p\ u)<0.
\]
The SOCP contradiction from this data had not yet been isolated.

\paragraph{Lean progress.}
Added
\[
\texttt{false\_of\_negative\_curvature\_direction}.
\]
It proves directly from \(\texttt{IsSOCP}\) that such a direction is
impossible.  The proof unfolds the Hessian term, uses \(A_u(v)=0\) to remove
the \(2B(A_u(v),A_u(v))\) term, and contradicts the asserted negativity.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Certificate.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The pending syzygy lemma now only needs to construct an admissible direction,
prove \(A_u(v)=0\), and prove the negative \(\sigma(v)\)-pairing.  The final
low-rank case work remains the main mathematical gap.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Syzygy direction algebra}

\paragraph{Largest gap selected.}
The low-rank cases in the blueprint all produce product identities of the form
\[
\sum_j m_jn_j=sq.
\]
To use these identities in Lean, the proof needs a reusable construction of
the corresponding rank-seven direction and a formal computation of its
\(A_u\)-image.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Syzygy.lean} and imported it through the
proof root chain.  The file defines
\[
\texttt{syzygyDirection}\ \alpha\ n\ \beta\ q
\]
with coordinates
\[
v_i=\sum_j \alpha_{j,i}n_j-\beta_i q.
\]
It proves:
\[
\texttt{syzygyDirection\_admissible}
\]
when the \(n_j\) and \(q\) are quadratic, and
\[
\texttt{A\_syzygyDirection}.
\]
The latter is the formal identity
\[
A_u(v)
=\sum_j \texttt{relationPoly}\ u\ \alpha_j\cdot n_j
  -\texttt{relationPoly}\ u\ \beta\cdot q.
\]
The corollary \texttt{A\_syzygyDirection\_eq\_zero} closes the \(A_u(v)=0\)
goal from the product identity.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The next syzygy step is the residual-functional computation:
under \(n_j\in K_\lambda\), prove that all terms involving the \(n_j\)'s vanish
and only \(\sum_i\beta_i^2 B(q^2,R)\) remains.  This is the remaining generic
negative-curvature mechanism before the rank \(1,2,3\) structural cases.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Generic syzygy-to-negative-curvature lemma}

\paragraph{Largest gap selected.}
After formalizing the syzygy direction and its \(A_u\)-image, the remaining
generic Hessian work was the residual-functional calculation.  This is the
part of Lemma~\textup{``Syzygy gives negative curvature''} that is independent
of the low-rank structural cases.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Syzygy.lean} with:
\[
\texttt{dot\_sigma\_syzygyDirection}
\]
and
\[
\texttt{false\_of\_syzygy\_negative\_curvature}.
\]
The first theorem proves that if every \(n_j\) lies in the catalecticant
kernel, then
\[
B(\sigma(v),R)
=\left(\sum_i\beta_i^2\right)B(q^2,R)
\]
for the syzygy direction \(v_i=\sum_j\alpha_{j,i}n_j-\beta_iq\).
The second theorem combines this formula with the product identity
\[
\sum_j \texttt{relationPoly}\ u\ \alpha_j\cdot n_j
=\texttt{relationPoly}\ u\ \beta\cdot q,
\]
\(\beta\ne0\), and \(B(q^2,R)<0\) to contradict SOCP.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The reusable SOCP/Hessian mechanism is now formalized.  The largest remaining
gap is the low-rank structural theorem and the rank \(1,2,3\) constructions
that produce the required \(\alpha,n,\beta,q\) data.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Catalecticant rank trichotomy}

\paragraph{Largest gap selected.}
The blueprint uses the facts \(k\le3\) and \(k\ne0\) to split into the
rank \(1,2,3\) cases.  The upper bound was formalized earlier; the nonzero
rank conclusion from a negative square was still missing.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Catalecticant.lean} with:
\[
\texttt{catalecticantMap\_rank\_pos\_of\_negative\_square}
\]
and
\[
\texttt{catalecticantMap\_rank\_eq\_one\_or\_two\_or\_three}.
\]
The first theorem proves that if some quadratic \(q\) satisfies
\[
B(q^2,\texttt{residual}\ p\ u)<0,
\]
then the restricted catalecticant map has nonzero range.  The second combines
this with the full-rank \(k\le3\) theorem to produce the exact Lean case split
\[
k=1\ \vee\ k=2\ \vee\ k=3.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The main full-rank proof can now enter the same three cases as the blueprint.
The remaining gap is to prove the rank-specific structural/syzygy existence
lemmas for \(k=1,2,3\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Full theorem reduced to structural syzygy certificates}

\paragraph{Largest gap selected.}
The generic syzygy contradiction still needed to be connected back to the SOS
argument that supplies a negative square.  This bridge should be independent
of the eventual rank \(1,2,3\) structural proofs.

\paragraph{Lean progress.}
Added
\[
\texttt{residual\_eq\_zero\_of\_syzygy\_certificates\_for\_negative\_squares}.
\]
It proves that if every quadratic \(q\) with
\[
B(q^2,\texttt{residual}\ p\ u)<0
\]
admits syzygy data \((\alpha,n,\beta)\) with \(n_j\in K_\lambda\),
\[
\sum_j \texttt{relationPoly}\ u\ \alpha_j\cdot n_j
=\texttt{relationPoly}\ u\ \beta\cdot q,
\qquad \beta\ne0,
\]
then \(\texttt{residual}\ p\ u=0\).  The proof argues by contradiction,
extracts a negative SOS summand, invokes the certificate hypothesis, and
applies the generic syzygy negative-curvature theorem.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The main remaining theorem can be stated informally as: in catalecticant ranks
\(1,2,3\), the low-rank structure from the blueprint supplies the syzygy
certificate for every negative square.  This is now the dominant open Lean
gap.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Grassmann intersection lemmas}

\paragraph{Largest gap selected.}
The remaining rank \(1,2,3\) structural cases repeatedly use the finite
dimensional inequality
\[
\dim(U\cap V)\ge \dim U+\dim V-\dim W
\]
for subspaces \(U,V\subseteq W\), and then conclude that an intersection is
nonzero when the sum of dimensions is larger than the ambient dimension.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Dimension.lean} with:
\[
\texttt{finrank\_add\_le\_finrank\_add\_finrank\_inf\_of\_le}
\]
and
\[
\texttt{inf\_ne\_bot\_of\_finrank\_lt\_add}.
\]
The first is the Grassmann lower-bound inequality in rearranged form.  The
second packages the common nonzero-intersection conclusion.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The dimension-intersection tool is now available for the incidence arguments
inside \(\operatorname{Sym}^2A\) and \(\operatorname{Sym}^2B\).  The remaining
dominant work is still the low-rank support theorem and the concrete
rank-specific syzygy existence proofs.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Nonzero intersection witnesses}

\paragraph{Largest gap selected.}
The Grassmann inequality itself was available, but the rank-one and rank-two
incidence arguments need an actual nonzero element of an intersection.  In the
blueprint this element is later named \(s\) and becomes the nonzero factor in
the syzygy identity.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Dimension.lean} with:
\[
\texttt{exists\_mem\_inf\_ne\_zero\_of\_finrank\_lt\_add}
\]
and
\[
\texttt{exists\_mem\_inf\_ne\_zero\_of\_finrank\_eq\_and\_lt\_add}.
\]
These lemmas package the common step:
if \(s,t\subseteq w\) and \(\dim w < \dim s+\dim t\), then there exists
\[
0\ne x\in s\cap t.
\]
The second version accepts lower bounds on \(\dim s,\dim t\) and an exact
ambient dimension, matching the arithmetic form used in the rank-specific
blueprint cases.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The next incidence step is to instantiate these lemmas inside the concrete
product subspaces \(MA\), \(MB\), \(\operatorname{Sym}^2A\), and
\(\operatorname{Sym}^2B\).  The low-rank support theorem remains the largest
mathematical input still unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Main theorem reduced to three rank-case certificates}

\paragraph{Largest gap selected.}
After isolating the generic syzygy contradiction, the proof still needed a
single theorem matching the blueprint's final structure: split off the
rank-deficient case, obtain the catalecticant rank trichotomy in the full-rank
case, and dispatch each rank by a structural certificate.

\paragraph{Lean progress.}
Added the predicate
\[
\texttt{HasSyzygyCertificate}\ B\ p\ u\ q
\]
for the syzygy data needed by the generic curvature argument.  Rephrased the
previous negative-square theorem in terms of this predicate.

Added
\[
\texttt{residual\_eq\_zero\_of\_rank\_case\_syzygy\_certificates}.
\]
This theorem proves \(\texttt{residual}\ p\ u=0\) from three assumptions:
for every negative quadratic square \(q\), if the catalecticant range rank is
respectively \(1\), \(2\), or \(3\), then \(\texttt{HasSyzygyCertificate}\)
holds for \(q\).  Internally it uses the rank-deficient split, the full-rank
catalecticant rank trichotomy, and the generic syzygy negative-curvature
theorem.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The root theorem is now reduced to the three low-rank certificate-existence
lemmas:
\[
k=1,\qquad k=2,\qquad k=3.
\]
These are exactly the structural support/syzygy constructions from the
blueprint.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three kernel equality}

\paragraph{Largest gap selected.}
The rank-three case in the blueprint starts from
\[
\dim K=7=\dim L,\qquad L\subseteq K,
\]
and concludes \(L=K\).  This equality is needed to treat all product terms
coming from the low-rank support reduction as elements of the coordinate span.

\paragraph{Lean progress.}
Added
\[
\texttt{spanUQuad\_eq\_ker\_catalecticantMap\_of\_rank\_three}.
\]
It proves that in the full-rank coordinate case, if the restricted
catalecticant range has finrank \(3\), then
\[
\texttt{spanUQuad}\ hu
=\ker(\texttt{catalecticantMap}\ B\ p\ u).
\]
The proof combines the existing inclusion \(L\subseteq K\), the full-rank
dimension \(\dim L=7\), rank-nullity, and \(\dim\Quad=10\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three case still needs the low-rank support theorem to produce the
linear form \(z\) and the supported negative quadratic \(q_W\), but the
dimension equality \(L=K\) is now available in Lean.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Kernel dimensions in ranks one, two, and three}

\paragraph{Largest gap selected.}
The rank-specific blueprint arguments use the numerical identities
\[
\dim K=9,\qquad \dim K=8,\qquad \dim K=7
\]
in catalecticant ranks \(1,2,3\), respectively.  Only the rank-three equality
was implicit in the previous \(L=K\) lemma.

\paragraph{Lean progress.}
Added:
\[
\texttt{finrank\_ker\_catalecticantMap\_eq\_nine\_of\_rank\_one},
\]
\[
\texttt{finrank\_ker\_catalecticantMap\_eq\_eight\_of\_rank\_two},
\]
and
\[
\texttt{finrank\_ker\_catalecticantMap\_eq\_seven\_of\_rank\_three}.
\]
Each follows from rank-nullity and \(\dim\Quad=10\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one and rank-two incidence arguments can now use the formal ambient
kernel dimensions \(9\) and \(8\).  The remaining unformalized content is
still the low-rank support theorem and the concrete product-subspace
dimension computations.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Coordinate-span coefficient extraction}

\paragraph{Largest gap selected.}
The structural low-rank arguments produce quadratics known to lie in the
coordinate span \(L_u\).  The syzygy machinery, however, needs explicit
coefficient vectors \(\alpha,\beta:\texttt{Fin 7}\to\mathbb R\) so those
quadratics can be written as \(\texttt{relationPoly}\ u\ \alpha\) and
\(\texttt{relationPoly}\ u\ \beta\).

\paragraph{Lean progress.}
Added:
\[
\texttt{exists\_relationPoly\_eq\_of\_mem\_spanUQuad}
\]
and
\[
\texttt{exists\_relationPoly\_eq\_of\_mem\_spanU}.
\]
These convert membership in \(\texttt{spanUQuad}\) or \(\texttt{spanU}\) into
explicit scalar coefficient vectors.  This is needed to feed rank-specific
span membership conclusions into \(\texttt{HasSyzygyCertificate}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The low-rank structural cases can now move from membership statements
\(m_j,s\in L_u\) to the coefficient data required by the generic syzygy
certificate.  The remaining work is to formalize the support-reduction and
product-subspace incidence arguments themselves.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product identity converted to syzygy certificate}

\paragraph{Largest gap selected.}
The rank-specific blueprint arguments naturally produce geometric data:
\[
m_j\in L,\qquad n_j\in K,\qquad 0\ne s\in L,\qquad
\sum_j m_jn_j=sq.
\]
The generic Lean endpoint, however, expects explicit coefficient vectors in
\texttt{HasSyzygyCertificate}.  The conversion from geometric product data to
coefficient data was still missing.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_product\_identity}.
\]
Given finite product data \(m_j,n_j,s,q\), with \(m_j,s\in\texttt{spanUQuad}\),
\(n_j\in\texttt{catalecticantKernel}\), \(s\ne0\), and
\[
\sum_j m_jn_j=sq,
\]
the theorem extracts coefficient vectors for the \(m_j\)'s and \(s\) using the
span-coefficient lemmas, proves the coefficient vector for \(s\) is nonzero,
and returns \(\texttt{HasSyzygyCertificate}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-specific low-rank proofs can now aim to produce only the natural
product identity data.  The remaining work is the low-rank support theorem and
the concrete incidence/product-space constructions that create these
\(m_j,n_j,s,q\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three kernel products give certificates}

\paragraph{Largest gap selected.}
In the rank-three case the blueprint uses \(K=L\) to turn kernel product data
into a coordinate-span syzygy.  The equality \(K=L\) was formalized, but the
certificate-producing bridge had not yet been connected.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_rank\_three\_kernel\_product\_identity}.
\]
Assuming full scalar rank, FOCP, and catalecticant range rank \(3\), this
theorem takes finite product data
\[
m_j\in\ker C_\lambda,\qquad n_j\in K_\lambda,\qquad
0\ne s\in\ker C_\lambda,\qquad \sum_j m_jn_j=sq,
\]
and returns \(\texttt{HasSyzygyCertificate}\).  Internally it uses the
rank-three equality
\[
\texttt{spanUQuad}=\ker(\texttt{catalecticantMap})
\]
and the general product-identity-to-certificate theorem.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three case is now reduced to producing the product data from the
low-rank support theorem: the formal analogue of choosing \(z\in A\), writing
the supported negative quadratic \(q_W\) as \(\sum_j \ell_jm_j\), and setting
\[
m_j=z\ell_j,\qquad n_j=zm_j,\qquad s=z^2.
\]

No Julia numerical claims were used in this round.

\section*{2026-05-04: Restricted kernel identified with polynomial kernel}

\paragraph{Largest gap selected.}
The low-rank structural arguments naturally switch between quadratics as
elements of \(\Quad\) and quadratics as polynomials satisfying the
catalecticant-kernel condition.  The Lean development had both notions:
\(\ker(\texttt{catalecticantMap})\) in \texttt{quadSubmodule} and
\(\texttt{catalecticantKernel}\) as a polynomial submodule, but no bridge.

\paragraph{Lean progress.}
Added
\[
\texttt{mem\_ker\_catalecticantMap\_iff}.
\]
It proves, for \(q:\texttt{quadSubmodule}\),
\[
q\in\ker(\texttt{catalecticantMap}\ B\ p\ u)
\iff
q.1\in\texttt{catalecticantKernel}\ B\ p\ u.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This removes a coercion barrier for the rank \(1,2,3\) structural proofs.  The
remaining work is still the support theorem and construction of the product
identity data.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Affine-linear submodule formalized}

\paragraph{Largest gap selected.}
The low-rank support theorem in the blueprint is stated on the
four-dimensional affine-linear space \(\Lin\), with products of linear forms
landing in the quadratic space \(\Quad\).  The Lean development already had
\(\Quad\), but not \(\Lin\).

\paragraph{Lean progress.}
Added \texttt{linSubmodule}, the submodule of polynomials of total degree at
most one, and proved
\[
\texttt{finrank\_linSubmodule\_eq\_four}.
\]
The proof parallels the quadratic dimension proof, using the monomial basis of
\texttt{MvPolynomial.restrictTotalDegree} and counting exponent vectors of
total degree at most one in three variables.

Also added
\[
\texttt{mul\_lin\_lin\_mem\_quad},
\]
which proves that products of affine-linear polynomials are quadratic.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The formal environment now has the finite-dimensional spaces \(\Lin\) and
\(\Quad\) used by the support theorem.  The remaining work is to formalize the
low-rank support reduction and the rank-specific product-space constructions.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product subspaces of linear forms}

\paragraph{Largest gap selected.}
The low-rank structural theorem and the rank-one/rank-two incidence arguments
use product subspaces such as \(AE\), \(xA\), \(\operatorname{Sym}^2A\), and
\(MB\).  The Lean development had \(\Lin\) and \(\Quad\), but not the
operation sending two subspaces of \(\Lin\) to the span of their products in
\(\Quad\).

\paragraph{Lean progress.}
Added:
\[
\texttt{linProduct} : \Lin\to\Lin\to\Quad
\]
and
\[
\texttt{linProductSubmodule}\ U\ V\le\Quad.
\]
The latter is the span of all products \(uv\) with \(u\in U\) and \(v\in V\).
Also added membership and monotonicity lemmas:
\[
\texttt{linProduct\_mem\_linProductSubmodule},\qquad
\texttt{linProductSubmodule\_mono},
\]
plus the generator criterion
\[
\texttt{linProductSubmodule\_le\_ker\_catalecticantMap\_of\_generators},
\]
which turns pointwise catalecticant-kernel membership of all products
\(uv\) into inclusion of the whole product span in the kernel.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The product spaces used in the blueprint can now be represented in Lean.  The
next missing pieces are their concrete dimension computations and the
low-rank support theorem that asserts \(AE\subseteq K\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three negative certificate wrapper}

\paragraph{Largest gap selected.}
The rank-three support-product constructor produced
\texttt{HasSyzygyCertificate} for a quadratic \(q\), but the flexible
rank-case reduction expects the bundled existential containing the quadratic
degree bound and negativity of \(\lambda(q^2)\).

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_support\_product\_decomposition}.
\]
This wraps
\texttt{hasSyzygyCertificate\_of\_support\_product\_decomposition} with
\(q\in\Quad\) and a hypothesis \(B(q^2,R)<0\), returning the exact
existential consumed by
\texttt{residual\_eq\_zero\_of\_rank\_case\_exists\_negative\_syzygy\_certificate}.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three endpoint now matches the global rank-split interface.  The
unformalized parts are still the low-rank support theorem and the construction
that moves the initially negative square to the supported \(WW\)-quadratic.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three support product decomposition}

\paragraph{Largest gap selected.}
The rank-three branch uses a different product identity from the rank-one and
rank-two branches.  If \(q\in WW\), \(zW\subseteq L\cap K\), and
\(z^2\in L\setminus\{0\}\), then a finite decomposition
\(q=\sum \ell_jm_j\) gives
\[
\sum (z\ell_j)(zm_j)=z^2 q.
\]
This needed its own certificate constructor.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_support\_product\_decomposition}.
\]
The lemma extracts a finitely supported representation of
\(q\in\texttt{linProductSubmodule}\ W\ W\), multiplies both factors by the
fixed \(z\), and builds the \texttt{HasSyzygyCertificate} from the resulting
identity.  Its hypotheses are exactly the rank-three endpoint data:
\[
zW\subseteq L,\qquad zW\subseteq K,\qquad z^2\in L\setminus\{0\}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This packages the rank-three finite decomposition once the low-rank support
theorem supplies \(z\) and \(W\), and once the negative square is reduced to a
quadratic in \(WW\).  The support theorem and the rank-specific decomposition
of the negative square remain unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Dimension-to-certificate wrapper}

\paragraph{Largest gap selected.}
After packaging a concrete incidence witness \(s\in S\cap MA\), the next
rank-case gap was to package the Grassmann dimension step that produces such
an \(s\).  This is the common endpoint of the rank-one and rank-two incidence
arguments.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimage\_product\_dimension}.
\]
The lemma assumes \(S\subseteq L\), \(xA\subseteq K\), \(S\le W\),
\[
\texttt{linProductSubmodule}\,
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\,A \le W,
\]
nontriviality of the preimage subspace, and numerical lower bounds whose sum
exceeds \(\dim W\).  It then extracts a nonzero
\[
s\in S\cap
\texttt{linProductSubmodule}\,
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\,A
\]
and returns the negative-square syzygy-certificate existential for \(x^2\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The finite-dimensional rank-one/rank-two endpoint is now mostly packaged.
The remaining high-level gaps are the structural support theorem, the binary
negative-pure-square theorem, and the concrete rank-case subspace
instantiations.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Incidence witness to negative certificate}

\paragraph{Largest gap selected.}
The rank-one and rank-two cases now have enough subspace plumbing to produce
an incidence witness \(0\neq s\in M A\), where
\[
M=\{a\in A:xa\in S\}.
\]
The next gap was to package this witness directly into the flexible rank-case
output: a negative quadratic square together with a syzygy certificate.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_preimage\_product\_incidence}.
\]
If \(S\subseteq L\), \(xA\subseteq K\), \(s\in S\setminus\{0\}\), and
\[
s\in \texttt{linProductSubmodule}\,
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\,A,
\]
then Lean constructs a \texttt{HasSyzygyCertificate} for \(x^2\).
Also added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimage\_product\_incidence},
\]
which combines that certificate with a negativity hypothesis
\(\lambda(x^4)<0\) to produce the exact existential used by
\texttt{residual\_eq\_zero\_of\_rank\_case\_exists\_negative\_syzygy\_certificate}.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one/rank-two branches can now end once their subspace hypotheses and
negative pure square are available.  The largest remaining gaps are the
low-rank support theorem, the binary negative-pure-square theorem, and the
rank-specific instantiation of \(S,A,x\) and the ambient symmetric-square
subspaces.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Preimages as linear subspaces}

\paragraph{Largest gap selected.}
The dimension argument naturally produces a nonzero preimage inside the type
\(A\), but the product-space and syzygy lemmas need \(M\) as an actual
subspace of \(\Lin\).  The formal proof needed a reusable way to view
\(\{a\in A:xa\in P\}\) as a submodule of \(\Lin\).

\paragraph{Lean progress.}
Added
\[
\texttt{linProductLeftPreimageWithin}\ x\ A\ P
  = A\cap\texttt{linProductLeftPreimage}\ x\ P
\]
as a submodule of \(\Lin\), with membership and projection lemmas.  Proved
\[
\texttt{linProductLeftPreimageWithin\_ne\_bot\_of\_on\_ne\_bot}
\]
and the packaged Grassmann criterion
\[
\texttt{linProductLeftPreimageWithin\_ne\_bot\_of\_finrank\_lt\_add}.
\]
This lets the rank cases obtain a nonzero \(M\le\Lin\) directly from the
dimension inequality for \(P\cap xA\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one/rank-two incidence plumbing now has compatible subspace types.
The remaining work is still to instantiate these lemmas with the blueprint's
rank-specific \(P,N,A,B,W\), and to formalize the low-rank support and binary
negative-pure-square structural statements.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Nonzero product preimages from Grassmann}

\paragraph{Largest gap selected.}
The rank-one and rank-two incidence proofs need to pass from a nonzero
intersection element in \(P\cap xA\) to a nonzero element of
\[
M=\{a\in A:xa\in P\}.
\]
This is the step that makes the product-space lower bounds applicable to the
preimage subspace \(M\).

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_mem\_linProductLeftPreimageOn\_ne\_zero\_of\_mem\_inf\_range\_ne\_zero},
\]
which extracts a nonzero preimage from a nonzero element of
\(P\cap\operatorname{range}(x\cdot-)\).  Also added
\[
\texttt{linProductLeftPreimageOn\_ne\_bot\_of\_inf\_range\_ne\_bot}
\]
and the packaged Grassmann criterion
\[
\texttt{linProductLeftPreimageOn\_ne\_bot\_of\_finrank\_lt\_add}.
\]
The last lemma says that if \(P\) and \(\operatorname{range}(x\cdot A)\) sit
inside an ambient quadratic subspace \(W\), \(x\neq0\), and the lower bounds
on \(\dim P\) and \(\dim A\) add to more than \(\dim W\), then the preimage
subspace \(M\) is nonzero.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This closes another finite-dimensional incidence bridge.  The structural
gaps are still the low-rank support theorem and the binary negative-pure-square
theorem, plus the final rank-specific instantiation of the subspaces.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Preimage-intersection dimension comparison}

\paragraph{Largest gap selected.}
The rank-one and rank-two lower bounds on \(M=\{a\in A:xa\in P\}\) use
\(\dim M=\dim(P\cap xA)\), relying on injectivity of multiplication by a
nonzero \(x\).

\paragraph{Lean progress.}
Defined
\[
\texttt{linProductLeftPreimageOnToInfRange},
\]
the canonical linear map from the preimage subspace
\(\{a\in A:xa\in P\}\) to
\[
P\cap \operatorname{range}(\texttt{linProductLeftMapOn}\ x\ A).
\]
For \(x\neq0\), proved this map is bijective and recorded the finrank equality
\[
\texttt{finrank\_linProductLeftPreimageOn\_eq\_inf\_range\_of\_ne\_zero}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This supplies the dimension comparison for the blueprint's preimage subspaces
\(M\).  The remaining rank-case work is to instantiate \(P\), \(A\), \(B\),
and the ambient symmetric-square subspaces, and to formalize the low-rank
support and binary negative-pure-square structural inputs.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product preimage subspaces}

\paragraph{Largest gap selected.}
The rank-one and rank-two incidence constructions define spaces such as
\(M=\{a\in A:xa\in P\}\).  The Lean development had product spans, but not
named preimage subspaces for multiplication by a fixed linear form.

\paragraph{Lean progress.}
Added
\[
\texttt{symSquareSubmodule}\ U=\texttt{linProductSubmodule}\ U\ U
\]
and the fixed-factor preimage subspaces
\[
\texttt{linProductLeftPreimage}\ x\ P\le\Lin,\qquad
\texttt{linProductLeftPreimageOn}\ x\ A\ P\le A.
\]
Both are implemented as comaps of the multiplication linear maps, with
membership simp lemmas stating that membership is exactly the condition
\(xb\in P\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
These definitions make the blueprint's \(M\) and \(N\) subspaces directly
expressible.  The next missing piece is the dimension comparison between such
preimages and the corresponding intersections \(P\cap xA\), followed by the
low-rank support and negative pure-square theorems.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Flexible negative-square reduction}

\paragraph{Largest gap selected.}
The previous rank-case reduction required a syzygy certificate for the exact
negative SOS summand selected from \(p\).  The blueprint's rank-one and
rank-two cases instead use that summand only to prove the existence of a
negative pure square \(x^2\), and then build the syzygy certificate for
\(x^2\).  The formal reduction therefore needed to allow the certified
negative square to differ from the initial SOS summand.

\paragraph{Lean progress.}
Added
\[
\texttt{residual\_eq\_zero\_of\_exists\_negative\_syzygy\_certificate},
\]
which proves \(R=0\) from the existence, under \(R\neq0\), of any quadratic
\(q\) with \(\lambda(q^2)<0\) and a
\texttt{HasSyzygyCertificate} for \(q\).  Also added the rank-split version
\[
\texttt{residual\_eq\_zero\_of\_rank\_case\_exists\_negative\_syzygy\_certificate}.
\]
This theorem still uses the original negative SOS summand to prove the
catalecticant rank is \(1\), \(2\), or \(3\), but each rank branch may now
return a different certified negative square.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The formal reduction now matches the blueprint's rank-one and rank-two flow.
The unresolved pieces are the rank-specific construction of a negative pure
square and the structural low-rank support theorem that supplies the
subspaces used by the product-incidence machinery.

No Julia numerical claims were used in this round.

\section*{2026-05-04: From product incidence to syzygy certificate}

\paragraph{Largest gap selected.}
The incidence lemmas can now produce a nonzero \(s\in N\cap MA\), but the
negative-curvature mechanism requires an explicit finite product identity.
The remaining bridge is to turn membership \(s\in MA\) into the finite
syzygy certificate used by \texttt{HasSyzygyCertificate}.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_mem\_linProductSubmodule}.
\]
Given a fixed affine-linear form \(x\), subspaces \(M,A\le\Lin\), and
\[
xM\subseteq L,\qquad xA\subseteq K,\qquad s\in L\cap MA,\qquad s\neq0,
\]
the lemma constructs a \texttt{HasSyzygyCertificate} for
\((xx)\).  The proof extracts a finitely supported linear-combination
representation of \(s\in MA\) from
\texttt{Finsupp.mem\_span\_range\_iff\_exists\_finsupp} and rewrites
\[
\sum_j (xm_j)(x a_j)=s\,x^2.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one and rank-two cases now have a direct path from their incidence
witness \(s\) to a syzygy certificate.  What remains is to formalize the
subspace setup that supplies \(xM\subseteq L\), \(xA\subseteq K\), and
\(s\in L\cap MA\), plus the structural low-rank support and binary
negative-pure-square results.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product incidence intersection}

\paragraph{Largest gap selected.}
The rank-one and rank-two arguments require a nonzero element in intersections
such as \(N\cap MA\) and \(N\cap MB\).  After the product-space dimension
lower bounds, the remaining formal step is a Grassmann dimension argument
inside a chosen ambient quadratic subspace.

\paragraph{Lean progress.}
Imported the general Grassmann lemmas from \texttt{Dimension.lean} into
\texttt{Catalecticant.lean} and proved
\[
\texttt{exists\_mem\_inf\_linProductSubmodule\_ne\_zero\_of\_finrank\_lt\_add}.
\]
The lemma says that if \(N\le W\), \(MA\le W\), \(M\neq0\), and the lower
bounds on \(\dim N\) and \(\dim A\) add to more than \(\dim W\), then there is
a nonzero quadratic in \(N\cap MA\).  This packages the blueprint's final
incidence step for both \(N\cap MA\) and \(N\cap MB\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The finite-dimensional intersection mechanism is now available, but the rank
cases still need definitions tying the blueprint's \(P\), \(M\), \(N\), and
ambient \(\operatorname{Sym}^2A\) or \(\operatorname{Sym}^2B\) to concrete
Lean submodules.  The low-rank support theorem and the binary negative
pure-square theorem remain unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product-space lower bounds}

\paragraph{Largest gap selected.}
After proving that multiplication by a nonzero linear form is injective, the
rank-one and rank-two incidence arguments still need the multi-generator
consequence: if \(M\neq0\), then the product space \(MA\) has dimension at
least \(\dim A\).

\paragraph{Lean progress.}
Proved
\[
\texttt{range\_linProductLeftMapOn\_le\_linProductSubmodule},
\]
which embeds the image of multiplication by a chosen \(m\in M\) into the full
product span \(MA\).  Combining this with injectivity gives
\[
\texttt{finrank\_right\_le\_finrank\_linProductSubmodule\_of\_mem\_ne\_zero}
\]
and the subspace-level version
\[
\texttt{finrank\_right\_le\_finrank\_linProductSubmodule\_of\_left\_ne\_bot}.
\]
Also added the symmetric form
\[
\texttt{finrank\_left\_le\_finrank\_linProductSubmodule\_of\_right\_ne\_bot}.
\]
These are the formal dimension lower bounds behind the blueprint claims
\(\dim(MA)\ge2\) and \(\dim(MB)\ge3\) once \(M\neq0\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The next finite-dimensional incidence step is to combine these product-space
lower bounds with the existing Grassmann intersection lemma to produce a
nonzero element of \(N\cap MA\) or \(N\cap MB\).  The low-rank support theorem
and binary negative-pure-square theorem remain the main structural gaps.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Multiplication by a nonzero linear form}

\paragraph{Largest gap selected.}
The rank-one and rank-two incidence arguments both use the dimension claim
\(\dim(xA)=\dim A\) for a nonzero affine-linear form \(x\).  This is one of
the first product-space dimension facts needed after introducing
\texttt{linProductSubmodule}.

\paragraph{Lean progress.}
Defined the linear maps
\[
\texttt{linProductLeftMap}\ x:\Lin\to\Quad,\qquad
\texttt{linProductLeftMapOn}\ x\ A:A\to\Quad
\]
given by multiplication by \(x\).  Proved
\[
\texttt{linProductLeftMap\_ker\_eq\_bot\_of\_ne\_zero}
\]
and its restricted-subspace version, using that
\(\mathbb R[X_1,X_2,X_3]\) has no zero divisors.  Also proved
\[
\texttt{finrank\_range\_linProductLeftMapOn\_eq},
\]
which states that multiplication by a nonzero affine-linear form preserves
the dimension of any subspace \(A\le\Lin\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This establishes the injective multiplication input used for \(xA\) and
\(MB\).  The remaining incidence work still needs dimension lower bounds for
the multi-generator product spaces \(MA\) and \(MB\), plus the low-rank support
and binary negative-pure-square theorems.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Linear annihilator for support reduction}

\paragraph{Largest gap selected.}
The next structural blocker is the low-rank apolar support theorem, especially
the bridge from its linear annihilator \(A\subseteq\Lin\) to the kernel
inclusion \(A\Lin\subseteq K\).

\paragraph{Lean progress.}
Added product algebra lemmas for \texttt{linProduct}: additivity and scalar
compatibility in each factor, zero-factor simplifications, commutativity, and
\[
\texttt{linProductSubmodule\_comm}.
\]
Defined
\[
\texttt{linearAnnihilator}\ B\ p\ u\le\Lin,
\]
the subspace of affine-linear forms \(a\) such that \(ae\) is in
\(\ker(\texttt{catalecticantMap}\ B\ p\ u)\) for every \(e\in\Lin\).
Also proved
\[
\texttt{linProductSubmodule\_linearAnnihilator\_top\_le\_ker}
\]
and the more flexible
\[
\texttt{linProductSubmodule\_le\_ker\_of\_le\_linearAnnihilator},
\]
which turn a subspace inclusion \(A\le\texttt{linearAnnihilator}\) into the
formal version of \(A\Lin\subseteq K\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This round formalizes the target object of the support theorem, but not the
Macaulay/apolar Hilbert-function argument proving that a large enough
\(\texttt{linearAnnihilator}\) exists at catalecticant rank at most three.
The rank-one and rank-two arguments still need their finite-dimensional
incidence and negative pure-square lemmas.

No Julia numerical claims were used in this round.

\end{document}
