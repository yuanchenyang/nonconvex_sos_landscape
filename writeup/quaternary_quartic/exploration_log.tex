\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage[T1]{fontenc}
\usepackage{hyperref}

\title{Quaternary Quartic Rank-7 Exploration Log}
\author{}
\date{}

\begin{document}
\maketitle

\section*{2026-05-04: Rank-deficient branch formalized}

\paragraph{Starting state.}
The Lean root theorem
\texttt{QuaternaryQuartic.quaternaryQuartic\_rankSeven\_no\_spurious\_socp}
was still proved through the scaffold axiom
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_placeholder}.
The full verification harness therefore built the files but failed at
\texttt{\#print axioms}, as expected.

\paragraph{Largest gap selected.}
The main blueprint splits first on whether the seven quadratic coordinates
\(u_i\) are linearly dependent.  The rank-deficient branch is independent of
the low-rank catalecticant support theorem and can be closed directly from the
SOCP inequalities and the SOS certificate.  This made it the largest remaining
gap that could be reduced without first formalizing the apolar Hilbert-function
argument.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Certificate.lean}.  It defines:
\[
\texttt{relationDirection}\ c\ q=(i\mapsto c_i q),\qquad
\texttt{relationPoly}\ u\ c=\sum_i c_i u_i,
\]
plus the supporting admissibility, \(A\)-image, and \(\sigma\)-formula lemmas.
The main new theorem is
\[
\texttt{residual\_eq\_zero\_of\_constant\_relation}.
\]
It proves that if there is a nonzero scalar relation
\(\sum_i c_i u_i=0\), then any SOS/SOCP point has
\(\texttt{residual}\ p\ u=0\).

\paragraph{Proof idea.}
For each SOS summand \(q_j^2\), use the kernel direction
\(w_i=c_i q_j\).  Then \(A_u(w)=0\) and
\[
\sigma(w)=\left(\sum_i c_i^2\right)q_j^2.
\]
The Hessian inequality gives
\(\left(\sum_i c_i^2\right)B(q_j^2,R)\ge0\), and the scalar is positive
because \(c\ne0\).  Summing over the SOS certificate gives \(B(p,R)\ge0\).
FOCP at \(v=u\) gives \(B(\sigma(u),R)=0\), hence the objective equals
\(-B(p,R)\le0\).  Positive-definiteness gives the opposite inequality, so the
objective is zero and the residual vanishes.

\paragraph{Verification.}
The new module and root proof file typecheck:
\[
\texttt{lake env lean QuaternaryQuarticProof/Certificate.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}.
\]
The full harness command available in the repository is
\texttt{./scripts/verify\_quaternary\_quartic.sh}; the user-provided
\texttt{verify\_quarternary\_quartic.sh} path does not exist.  The full harness
still fails only because the root theorem depends on the existing placeholder
axiom.

\paragraph{Remaining blockers.}
The largest remaining gap is the full-rank branch.  The next proof-serving
steps are:
\begin{enumerate}
\item formalize the catalecticant kernel \(K_\lambda\) and prove
  \(L_u\subseteq K_\lambda\) from FOCP;
\item express full rank of the seven coordinates as
  \(\ker(\texttt{relationPolyLin}\ u)=\bot\), yielding \(\dim L_u=7\);
\item formalize the low-rank catalecticant support theorem and the rank
  \(1,2,3\) syzygy-to-negative-curvature constructions.
\end{enumerate}

No Julia numerical claims were used in this round.

\section*{2026-05-04: Catalecticant setup and negative SOS summand}

\paragraph{Largest gap selected.}
After the rank-deficient branch, the next full-rank prerequisite in the
blueprint is the residual functional and catalecticant kernel setup.  This
precedes the low-rank support theorem and is needed in every \(k=1,2,3\)
case.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Catalecticant.lean} and wired it through
\texttt{QuaternaryQuarticProof/Basic.lean}.  The new module defines:
\[
\texttt{quadSubmodule},\qquad
\texttt{spanU}\ u=\operatorname{span}\{u_i\},\qquad
\texttt{catalecticantKernel}\ B\ p\ u.
\]
The catalecticant kernel is the submodule of quadratic polynomials \(q\) such
that
\[
  B(qr,\texttt{residual}\ p\ u)=0
  \quad\text{for every quadratic }r.
\]

\paragraph{Formalized lemmas.}
The theorem
\texttt{spanU\_le\_catalecticantKernel} proves the blueprint lemma
\(L_u\subseteq K_\lambda\) from FOCP, by testing the single-coordinate
direction \(v=\texttt{Pi.single}\ i\ r\).

The theorem \texttt{target\_dot\_residual\_negative} proves that if
\(\texttt{residual}\ p\ u\ne0\), then
\[
  B(p,\texttt{residual}\ p\ u)<0.
\]
The theorem \texttt{exists\_negative\_sos\_summand} then extracts a quadratic
SOS summand \(q\) with
\[
  B(q^2,\texttt{residual}\ p\ u)<0.
\]
The combined theorem
\texttt{exists\_negative\_sos\_summand\_of\_nonzero\_residual} packages these
two steps for later use.

\paragraph{Verification.}
The new module and root proof file typecheck:
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}.
\]

\paragraph{Remaining blockers.}
The largest remaining gap is now the finite-dimensional full-rank bridge:
connect \(\ker(\texttt{relationPolyLin}\ u)=\bot\) with
\(\dim(\texttt{spanU}\ u)=7\), restrict the catalecticant quotient to the
10-dimensional quadratic submodule, and prove or import the low-rank
support/syzygy constructions without axioms.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Full-rank span dimension bridge}

\paragraph{Largest gap selected.}
The full-rank branch needs a formal way to turn ``the seven coordinates have
no scalar relation'' into the dimension statement \(\dim L_u=7\).  This is the
input used with \(L_u\subseteq K_\lambda\) to force catalecticant rank at most
three.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Catalecticant.lean} with:
\[
\texttt{spanU\_le\_quadSubmodule},\qquad
\texttt{linearIndependent\_u\_of\_relationPolyLin\_ker\_eq\_bot},\qquad
\texttt{finrank\_spanU\_eq\_seven\_of\_relationPolyLin\_ker\_eq\_bot}.
\]
The last theorem proves
\[
  \ker(\texttt{relationPolyLin}\ u)=\bot
  \Longrightarrow
  \operatorname{finrank}_{\mathbb R}(\texttt{spanU}\ u)=7.
\]

\paragraph{Verification.}
The modified catalecticant module and root proof file typecheck:
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}.
\]

\paragraph{Remaining blockers.}
The next largest gap is to make the rank bound precise on the quadratic
submodule: define the restricted catalecticant map
\[
  \texttt{quadSubmodule}\to(\texttt{quadSubmodule}\to_{\mathbb R}\mathbb R)
\]
and prove that \(L_u\subseteq K_\lambda\) and \(\dim L_u=7\) imply rank at most
three.  After that, the main remaining mathematical input is the low-rank
support theorem and the rank \(1,2,3\) syzygy constructions.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Quadratic submodule tied to mathlib degree filtration}

\paragraph{Largest gap selected.}
The rank bound requires finite-dimensional control of the quadratic submodule.
Rather than maintain a standalone hand-built submodule, the proof should reuse
mathlib's existing total-degree filtration for multivariate polynomials.

\paragraph{Lean progress.}
Added
\[
\texttt{quadSubmodule\_eq\_restrictTotalDegree}
\]
showing that \texttt{quadSubmodule} is exactly
\[
  \texttt{MvPolynomial.restrictTotalDegree (Fin 3) }\mathbb R\texttt{ 2}.
\]
Also added the finite-module instance
\[
\texttt{instModuleFiniteQuadSubmodule}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean},\qquad
\texttt{lake env lean QuaternaryQuarticProof.lean}
\]
both typecheck after this change.

\paragraph{Remaining blockers.}
The exact numerical dimension \(\dim(\texttt{quadSubmodule})=10\) is still
open.  The next likely route is to use the basis of
\texttt{MvPolynomial.restrictTotalDegree} indexed by exponent vectors of total
degree at most two, then count those exponent vectors for \(\texttt{Fin 3}\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Quadratic dimension proved}

\paragraph{Largest gap selected.}
The immediate blocker for the catalecticant rank bound was the exact dimension
of the quadratic submodule.  The blueprint needs
\(\dim \Quad=10\), and the previous round had only connected \(\Quad\) to
mathlib's total-degree filtration.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_quadSubmodule\_eq\_ten} :
  \operatorname{finrank}_{\mathbb R}(\texttt{quadSubmodule})=10.
\]
The proof uses \texttt{MvPolynomial.basisRestrictSupport} for the
total-degree-bounded monomial basis.  The indexing subtype
\[
\{s:\texttt{Fin 3}\to_0\mathbb N : \sum_i s_i\le2\}
\]
is counted by an equivalence with bounded triples
\[
\{f:\texttt{Fin 3}\to\texttt{Fin 3} : \sum_i f_i\le2\}.
\]
The final finite cardinal computation is closed by \texttt{decide}, not
\texttt{native\_decide}, so it introduces no extra axiom.

\paragraph{Verification.}
The module builds, and
\[
\texttt{\#print axioms QuaternaryQuartic.finrank\_quadSubmodule\_eq\_ten}
\]
reports only
\[
\texttt{[propext, Classical.choice, Quot.sound]}.
\]

\paragraph{Remaining blockers.}
The largest remaining gap is the restricted catalecticant rank inequality:
define the linear map \(\Quad\to\Quad^\ast\), identify its kernel with the
quadratic catalecticant kernel, and prove that the full-rank branch gives
rank at most \(10-7=3\).  After that, the main mathematical burden remains
the low-rank support theorem and the rank \(1,2,3\) syzygy constructions.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Restricted catalecticant rank bound}

\paragraph{Largest gap selected.}
After proving \(\dim \Quad=10\), the remaining finite-dimensional bridge was
to define the actual restricted catalecticant map and prove the full-rank
rank bound \(k\le3\).

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Catalecticant.lean} with:
\[
\texttt{spanUQuad},\quad
\texttt{catalecticantMap},\quad
\texttt{spanUQuad\_le\_ker\_catalecticantMap}.
\]
The map is
\[
  \Quad\to\Quad^\ast,\qquad
  q\mapsto(r\mapsto B(qr,\texttt{residual}\ p\ u)).
\]
The inclusion theorem proves that FOCP places the seven-dimensional span of
the coordinates inside this kernel.

The main new theorem is
\[
\texttt{catalecticantMap\_rank\_le\_three\_of\_relationPolyLin\_ker\_eq\_bot}.
\]
It combines:
\[
\dim \Quad=10,\qquad
\dim L_u=7,\qquad
L_u\subseteq\ker C_\lambda,
\]
with rank-nullity to conclude
\[
  \operatorname{finrank}(\operatorname{range} C_\lambda)\le3.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks after the change.

\paragraph{Remaining blockers.}
The largest remaining mathematical gap is now the low-rank structural input:
for catalecticant rank \(1,2,3\), produce the support reductions and syzygies
from the blueprint.  The reusable negative-curvature construction for a
syzygy is also still pending in Lean.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Main split reduced to full rank}

\paragraph{Largest gap selected.}
The blueprint's main proof first splits on whether the seven coordinates have
a nonzero scalar relation.  The rank-deficient certificate was formalized
earlier, but the reusable main split had not yet been packaged.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Certificate.lean} with:
\[
\texttt{residual\_eq\_zero\_of\_relationPolyLin\_ker\_ne\_bot}
\]
and
\[
\texttt{residual\_eq\_zero\_or\_relationPolyLin\_ker\_eq\_bot}.
\]
The second theorem proves that any admissible SOS/SOCP point either already
has zero residual or has full scalar rank
\[
\ker(\texttt{relationPolyLin}\ u)=\bot.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Certificate.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The root theorem can now reduce to the full-rank branch.  The unresolved
content is entirely in that branch: use the rank-\(\le3\) catalecticant bound,
the negative SOS summand, and the low-rank support/syzygy mechanisms to
construct a negative-curvature direction.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Negative-curvature contradiction isolated}

\paragraph{Largest gap selected.}
All rank \(1,2,3\) syzygy arguments end the same way: construct an
admissible direction \(v\) with \(A_u(v)=0\) and
\[
  B(\sigma(v),\texttt{residual}\ p\ u)<0.
\]
The SOCP contradiction from this data had not yet been isolated.

\paragraph{Lean progress.}
Added
\[
\texttt{false\_of\_negative\_curvature\_direction}.
\]
It proves directly from \(\texttt{IsSOCP}\) that such a direction is
impossible.  The proof unfolds the Hessian term, uses \(A_u(v)=0\) to remove
the \(2B(A_u(v),A_u(v))\) term, and contradicts the asserted negativity.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Certificate.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The pending syzygy lemma now only needs to construct an admissible direction,
prove \(A_u(v)=0\), and prove the negative \(\sigma(v)\)-pairing.  The final
low-rank case work remains the main mathematical gap.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Syzygy direction algebra}

\paragraph{Largest gap selected.}
The low-rank cases in the blueprint all produce product identities of the form
\[
\sum_j m_jn_j=sq.
\]
To use these identities in Lean, the proof needs a reusable construction of
the corresponding rank-seven direction and a formal computation of its
\(A_u\)-image.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Syzygy.lean} and imported it through the
proof root chain.  The file defines
\[
\texttt{syzygyDirection}\ \alpha\ n\ \beta\ q
\]
with coordinates
\[
v_i=\sum_j \alpha_{j,i}n_j-\beta_i q.
\]
It proves:
\[
\texttt{syzygyDirection\_admissible}
\]
when the \(n_j\) and \(q\) are quadratic, and
\[
\texttt{A\_syzygyDirection}.
\]
The latter is the formal identity
\[
A_u(v)
=\sum_j \texttt{relationPoly}\ u\ \alpha_j\cdot n_j
  -\texttt{relationPoly}\ u\ \beta\cdot q.
\]
The corollary \texttt{A\_syzygyDirection\_eq\_zero} closes the \(A_u(v)=0\)
goal from the product identity.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The next syzygy step is the residual-functional computation:
under \(n_j\in K_\lambda\), prove that all terms involving the \(n_j\)'s vanish
and only \(\sum_i\beta_i^2 B(q^2,R)\) remains.  This is the remaining generic
negative-curvature mechanism before the rank \(1,2,3\) structural cases.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Generic syzygy-to-negative-curvature lemma}

\paragraph{Largest gap selected.}
After formalizing the syzygy direction and its \(A_u\)-image, the remaining
generic Hessian work was the residual-functional calculation.  This is the
part of Lemma~\textup{``Syzygy gives negative curvature''} that is independent
of the low-rank structural cases.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Syzygy.lean} with:
\[
\texttt{dot\_sigma\_syzygyDirection}
\]
and
\[
\texttt{false\_of\_syzygy\_negative\_curvature}.
\]
The first theorem proves that if every \(n_j\) lies in the catalecticant
kernel, then
\[
B(\sigma(v),R)
=\left(\sum_i\beta_i^2\right)B(q^2,R)
\]
for the syzygy direction \(v_i=\sum_j\alpha_{j,i}n_j-\beta_iq\).
The second theorem combines this formula with the product identity
\[
\sum_j \texttt{relationPoly}\ u\ \alpha_j\cdot n_j
=\texttt{relationPoly}\ u\ \beta\cdot q,
\]
\(\beta\ne0\), and \(B(q^2,R)<0\) to contradict SOCP.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The reusable SOCP/Hessian mechanism is now formalized.  The largest remaining
gap is the low-rank structural theorem and the rank \(1,2,3\) constructions
that produce the required \(\alpha,n,\beta,q\) data.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Catalecticant rank trichotomy}

\paragraph{Largest gap selected.}
The blueprint uses the facts \(k\le3\) and \(k\ne0\) to split into the
rank \(1,2,3\) cases.  The upper bound was formalized earlier; the nonzero
rank conclusion from a negative square was still missing.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Catalecticant.lean} with:
\[
\texttt{catalecticantMap\_rank\_pos\_of\_negative\_square}
\]
and
\[
\texttt{catalecticantMap\_rank\_eq\_one\_or\_two\_or\_three}.
\]
The first theorem proves that if some quadratic \(q\) satisfies
\[
B(q^2,\texttt{residual}\ p\ u)<0,
\]
then the restricted catalecticant map has nonzero range.  The second combines
this with the full-rank \(k\le3\) theorem to produce the exact Lean case split
\[
k=1\ \vee\ k=2\ \vee\ k=3.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The main full-rank proof can now enter the same three cases as the blueprint.
The remaining gap is to prove the rank-specific structural/syzygy existence
lemmas for \(k=1,2,3\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Full theorem reduced to structural syzygy certificates}

\paragraph{Largest gap selected.}
The generic syzygy contradiction still needed to be connected back to the SOS
argument that supplies a negative square.  This bridge should be independent
of the eventual rank \(1,2,3\) structural proofs.

\paragraph{Lean progress.}
Added
\[
\texttt{residual\_eq\_zero\_of\_syzygy\_certificates\_for\_negative\_squares}.
\]
It proves that if every quadratic \(q\) with
\[
B(q^2,\texttt{residual}\ p\ u)<0
\]
admits syzygy data \((\alpha,n,\beta)\) with \(n_j\in K_\lambda\),
\[
\sum_j \texttt{relationPoly}\ u\ \alpha_j\cdot n_j
=\texttt{relationPoly}\ u\ \beta\cdot q,
\qquad \beta\ne0,
\]
then \(\texttt{residual}\ p\ u=0\).  The proof argues by contradiction,
extracts a negative SOS summand, invokes the certificate hypothesis, and
applies the generic syzygy negative-curvature theorem.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The main remaining theorem can be stated informally as: in catalecticant ranks
\(1,2,3\), the low-rank structure from the blueprint supplies the syzygy
certificate for every negative square.  This is now the dominant open Lean
gap.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Grassmann intersection lemmas}

\paragraph{Largest gap selected.}
The remaining rank \(1,2,3\) structural cases repeatedly use the finite
dimensional inequality
\[
\dim(U\cap V)\ge \dim U+\dim V-\dim W
\]
for subspaces \(U,V\subseteq W\), and then conclude that an intersection is
nonzero when the sum of dimensions is larger than the ambient dimension.

\paragraph{Lean progress.}
Added \texttt{QuaternaryQuarticProof/Dimension.lean} with:
\[
\texttt{finrank\_add\_le\_finrank\_add\_finrank\_inf\_of\_le}
\]
and
\[
\texttt{inf\_ne\_bot\_of\_finrank\_lt\_add}.
\]
The first is the Grassmann lower-bound inequality in rearranged form.  The
second packages the common nonzero-intersection conclusion.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The dimension-intersection tool is now available for the incidence arguments
inside \(\operatorname{Sym}^2A\) and \(\operatorname{Sym}^2B\).  The remaining
dominant work is still the low-rank support theorem and the concrete
rank-specific syzygy existence proofs.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Nonzero intersection witnesses}

\paragraph{Largest gap selected.}
The Grassmann inequality itself was available, but the rank-one and rank-two
incidence arguments need an actual nonzero element of an intersection.  In the
blueprint this element is later named \(s\) and becomes the nonzero factor in
the syzygy identity.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Dimension.lean} with:
\[
\texttt{exists\_mem\_inf\_ne\_zero\_of\_finrank\_lt\_add}
\]
and
\[
\texttt{exists\_mem\_inf\_ne\_zero\_of\_finrank\_eq\_and\_lt\_add}.
\]
These lemmas package the common step:
if \(s,t\subseteq w\) and \(\dim w < \dim s+\dim t\), then there exists
\[
0\ne x\in s\cap t.
\]
The second version accepts lower bounds on \(\dim s,\dim t\) and an exact
ambient dimension, matching the arithmetic form used in the rank-specific
blueprint cases.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The next incidence step is to instantiate these lemmas inside the concrete
product subspaces \(MA\), \(MB\), \(\operatorname{Sym}^2A\), and
\(\operatorname{Sym}^2B\).  The low-rank support theorem remains the largest
mathematical input still unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Main theorem reduced to three rank-case certificates}

\paragraph{Largest gap selected.}
After isolating the generic syzygy contradiction, the proof still needed a
single theorem matching the blueprint's final structure: split off the
rank-deficient case, obtain the catalecticant rank trichotomy in the full-rank
case, and dispatch each rank by a structural certificate.

\paragraph{Lean progress.}
Added the predicate
\[
\texttt{HasSyzygyCertificate}\ B\ p\ u\ q
\]
for the syzygy data needed by the generic curvature argument.  Rephrased the
previous negative-square theorem in terms of this predicate.

Added
\[
\texttt{residual\_eq\_zero\_of\_rank\_case\_syzygy\_certificates}.
\]
This theorem proves \(\texttt{residual}\ p\ u=0\) from three assumptions:
for every negative quadratic square \(q\), if the catalecticant range rank is
respectively \(1\), \(2\), or \(3\), then \(\texttt{HasSyzygyCertificate}\)
holds for \(q\).  Internally it uses the rank-deficient split, the full-rank
catalecticant rank trichotomy, and the generic syzygy negative-curvature
theorem.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The root theorem is now reduced to the three low-rank certificate-existence
lemmas:
\[
k=1,\qquad k=2,\qquad k=3.
\]
These are exactly the structural support/syzygy constructions from the
blueprint.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three kernel equality}

\paragraph{Largest gap selected.}
The rank-three case in the blueprint starts from
\[
\dim K=7=\dim L,\qquad L\subseteq K,
\]
and concludes \(L=K\).  This equality is needed to treat all product terms
coming from the low-rank support reduction as elements of the coordinate span.

\paragraph{Lean progress.}
Added
\[
\texttt{spanUQuad\_eq\_ker\_catalecticantMap\_of\_rank\_three}.
\]
It proves that in the full-rank coordinate case, if the restricted
catalecticant range has finrank \(3\), then
\[
\texttt{spanUQuad}\ hu
=\ker(\texttt{catalecticantMap}\ B\ p\ u).
\]
The proof combines the existing inclusion \(L\subseteq K\), the full-rank
dimension \(\dim L=7\), rank-nullity, and \(\dim\Quad=10\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three case still needs the low-rank support theorem to produce the
linear form \(z\) and the supported negative quadratic \(q_W\), but the
dimension equality \(L=K\) is now available in Lean.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Kernel dimensions in ranks one, two, and three}

\paragraph{Largest gap selected.}
The rank-specific blueprint arguments use the numerical identities
\[
\dim K=9,\qquad \dim K=8,\qquad \dim K=7
\]
in catalecticant ranks \(1,2,3\), respectively.  Only the rank-three equality
was implicit in the previous \(L=K\) lemma.

\paragraph{Lean progress.}
Added:
\[
\texttt{finrank\_ker\_catalecticantMap\_eq\_nine\_of\_rank\_one},
\]
\[
\texttt{finrank\_ker\_catalecticantMap\_eq\_eight\_of\_rank\_two},
\]
and
\[
\texttt{finrank\_ker\_catalecticantMap\_eq\_seven\_of\_rank\_three}.
\]
Each follows from rank-nullity and \(\dim\Quad=10\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one and rank-two incidence arguments can now use the formal ambient
kernel dimensions \(9\) and \(8\).  The remaining unformalized content is
still the low-rank support theorem and the concrete product-subspace
dimension computations.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Coordinate-span coefficient extraction}

\paragraph{Largest gap selected.}
The structural low-rank arguments produce quadratics known to lie in the
coordinate span \(L_u\).  The syzygy machinery, however, needs explicit
coefficient vectors \(\alpha,\beta:\texttt{Fin 7}\to\mathbb R\) so those
quadratics can be written as \(\texttt{relationPoly}\ u\ \alpha\) and
\(\texttt{relationPoly}\ u\ \beta\).

\paragraph{Lean progress.}
Added:
\[
\texttt{exists\_relationPoly\_eq\_of\_mem\_spanUQuad}
\]
and
\[
\texttt{exists\_relationPoly\_eq\_of\_mem\_spanU}.
\]
These convert membership in \(\texttt{spanUQuad}\) or \(\texttt{spanU}\) into
explicit scalar coefficient vectors.  This is needed to feed rank-specific
span membership conclusions into \(\texttt{HasSyzygyCertificate}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The low-rank structural cases can now move from membership statements
\(m_j,s\in L_u\) to the coefficient data required by the generic syzygy
certificate.  The remaining work is to formalize the support-reduction and
product-subspace incidence arguments themselves.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product identity converted to syzygy certificate}

\paragraph{Largest gap selected.}
The rank-specific blueprint arguments naturally produce geometric data:
\[
m_j\in L,\qquad n_j\in K,\qquad 0\ne s\in L,\qquad
\sum_j m_jn_j=sq.
\]
The generic Lean endpoint, however, expects explicit coefficient vectors in
\texttt{HasSyzygyCertificate}.  The conversion from geometric product data to
coefficient data was still missing.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_product\_identity}.
\]
Given finite product data \(m_j,n_j,s,q\), with \(m_j,s\in\texttt{spanUQuad}\),
\(n_j\in\texttt{catalecticantKernel}\), \(s\ne0\), and
\[
\sum_j m_jn_j=sq,
\]
the theorem extracts coefficient vectors for the \(m_j\)'s and \(s\) using the
span-coefficient lemmas, proves the coefficient vector for \(s\) is nonzero,
and returns \(\texttt{HasSyzygyCertificate}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-specific low-rank proofs can now aim to produce only the natural
product identity data.  The remaining work is the low-rank support theorem and
the concrete incidence/product-space constructions that create these
\(m_j,n_j,s,q\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three kernel products give certificates}

\paragraph{Largest gap selected.}
In the rank-three case the blueprint uses \(K=L\) to turn kernel product data
into a coordinate-span syzygy.  The equality \(K=L\) was formalized, but the
certificate-producing bridge had not yet been connected.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_rank\_three\_kernel\_product\_identity}.
\]
Assuming full scalar rank, FOCP, and catalecticant range rank \(3\), this
theorem takes finite product data
\[
m_j\in\ker C_\lambda,\qquad n_j\in K_\lambda,\qquad
0\ne s\in\ker C_\lambda,\qquad \sum_j m_jn_j=sq,
\]
and returns \(\texttt{HasSyzygyCertificate}\).  Internally it uses the
rank-three equality
\[
\texttt{spanUQuad}=\ker(\texttt{catalecticantMap})
\]
and the general product-identity-to-certificate theorem.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three case is now reduced to producing the product data from the
low-rank support theorem: the formal analogue of choosing \(z\in A\), writing
the supported negative quadratic \(q_W\) as \(\sum_j \ell_jm_j\), and setting
\[
m_j=z\ell_j,\qquad n_j=zm_j,\qquad s=z^2.
\]

No Julia numerical claims were used in this round.

\section*{2026-05-04: Restricted kernel identified with polynomial kernel}

\paragraph{Largest gap selected.}
The low-rank structural arguments naturally switch between quadratics as
elements of \(\Quad\) and quadratics as polynomials satisfying the
catalecticant-kernel condition.  The Lean development had both notions:
\(\ker(\texttt{catalecticantMap})\) in \texttt{quadSubmodule} and
\(\texttt{catalecticantKernel}\) as a polynomial submodule, but no bridge.

\paragraph{Lean progress.}
Added
\[
\texttt{mem\_ker\_catalecticantMap\_iff}.
\]
It proves, for \(q:\texttt{quadSubmodule}\),
\[
q\in\ker(\texttt{catalecticantMap}\ B\ p\ u)
\iff
q.1\in\texttt{catalecticantKernel}\ B\ p\ u.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This removes a coercion barrier for the rank \(1,2,3\) structural proofs.  The
remaining work is still the support theorem and construction of the product
identity data.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Affine-linear submodule formalized}

\paragraph{Largest gap selected.}
The low-rank support theorem in the blueprint is stated on the
four-dimensional affine-linear space \(\Lin\), with products of linear forms
landing in the quadratic space \(\Quad\).  The Lean development already had
\(\Quad\), but not \(\Lin\).

\paragraph{Lean progress.}
Added \texttt{linSubmodule}, the submodule of polynomials of total degree at
most one, and proved
\[
\texttt{finrank\_linSubmodule\_eq\_four}.
\]
The proof parallels the quadratic dimension proof, using the monomial basis of
\texttt{MvPolynomial.restrictTotalDegree} and counting exponent vectors of
total degree at most one in three variables.

Also added
\[
\texttt{mul\_lin\_lin\_mem\_quad},
\]
which proves that products of affine-linear polynomials are quadratic.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The formal environment now has the finite-dimensional spaces \(\Lin\) and
\(\Quad\) used by the support theorem.  The remaining work is to formalize the
low-rank support reduction and the rank-specific product-space constructions.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product subspaces of linear forms}

\paragraph{Largest gap selected.}
The low-rank structural theorem and the rank-one/rank-two incidence arguments
use product subspaces such as \(AE\), \(xA\), \(\operatorname{Sym}^2A\), and
\(MB\).  The Lean development had \(\Lin\) and \(\Quad\), but not the
operation sending two subspaces of \(\Lin\) to the span of their products in
\(\Quad\).

\paragraph{Lean progress.}
Added:
\[
\texttt{linProduct} : \Lin\to\Lin\to\Quad
\]
and
\[
\texttt{linProductSubmodule}\ U\ V\le\Quad.
\]
The latter is the span of all products \(uv\) with \(u\in U\) and \(v\in V\).
Also added membership and monotonicity lemmas:
\[
\texttt{linProduct\_mem\_linProductSubmodule},\qquad
\texttt{linProductSubmodule\_mono},
\]
plus the generator criterion
\[
\texttt{linProductSubmodule\_le\_ker\_catalecticantMap\_of\_generators},
\]
which turns pointwise catalecticant-kernel membership of all products
\(uv\) into inclusion of the whole product span in the kernel.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The product spaces used in the blueprint can now be represented in Lean.  The
next missing pieces are their concrete dimension computations and the
low-rank support theorem that asserts \(AE\subseteq K\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three negative certificate wrapper}

\paragraph{Largest gap selected.}
The rank-three support-product constructor produced
\texttt{HasSyzygyCertificate} for a quadratic \(q\), but the flexible
rank-case reduction expects the bundled existential containing the quadratic
degree bound and negativity of \(\lambda(q^2)\).

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_support\_product\_decomposition}.
\]
This wraps
\texttt{hasSyzygyCertificate\_of\_support\_product\_decomposition} with
\(q\in\Quad\) and a hypothesis \(B(q^2,R)<0\), returning the exact
existential consumed by
\texttt{residual\_eq\_zero\_of\_rank\_case\_exists\_negative\_syzygy\_certificate}.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three endpoint now matches the global rank-split interface.  The
unformalized parts are still the low-rank support theorem and the construction
that moves the initially negative square to the supported \(WW\)-quadratic.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three support product decomposition}

\paragraph{Largest gap selected.}
The rank-three branch uses a different product identity from the rank-one and
rank-two branches.  If \(q\in WW\), \(zW\subseteq L\cap K\), and
\(z^2\in L\setminus\{0\}\), then a finite decomposition
\(q=\sum \ell_jm_j\) gives
\[
\sum (z\ell_j)(zm_j)=z^2 q.
\]
This needed its own certificate constructor.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_support\_product\_decomposition}.
\]
The lemma extracts a finitely supported representation of
\(q\in\texttt{linProductSubmodule}\ W\ W\), multiplies both factors by the
fixed \(z\), and builds the \texttt{HasSyzygyCertificate} from the resulting
identity.  Its hypotheses are exactly the rank-three endpoint data:
\[
zW\subseteq L,\qquad zW\subseteq K,\qquad z^2\in L\setminus\{0\}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This packages the rank-three finite decomposition once the low-rank support
theorem supplies \(z\) and \(W\), and once the negative square is reduced to a
quadratic in \(WW\).  The support theorem and the rank-specific decomposition
of the negative square remain unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Dimension-to-certificate wrapper}

\paragraph{Largest gap selected.}
After packaging a concrete incidence witness \(s\in S\cap MA\), the next
rank-case gap was to package the Grassmann dimension step that produces such
an \(s\).  This is the common endpoint of the rank-one and rank-two incidence
arguments.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimage\_product\_dimension}.
\]
The lemma assumes \(S\subseteq L\), \(xA\subseteq K\), \(S\le W\),
\[
\texttt{linProductSubmodule}\,
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\,A \le W,
\]
nontriviality of the preimage subspace, and numerical lower bounds whose sum
exceeds \(\dim W\).  It then extracts a nonzero
\[
s\in S\cap
\texttt{linProductSubmodule}\,
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\,A
\]
and returns the negative-square syzygy-certificate existential for \(x^2\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The finite-dimensional rank-one/rank-two endpoint is now mostly packaged.
The remaining high-level gaps are the structural support theorem, the binary
negative-pure-square theorem, and the concrete rank-case subspace
instantiations.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Incidence witness to negative certificate}

\paragraph{Largest gap selected.}
The rank-one and rank-two cases now have enough subspace plumbing to produce
an incidence witness \(0\neq s\in M A\), where
\[
M=\{a\in A:xa\in S\}.
\]
The next gap was to package this witness directly into the flexible rank-case
output: a negative quadratic square together with a syzygy certificate.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_preimage\_product\_incidence}.
\]
If \(S\subseteq L\), \(xA\subseteq K\), \(s\in S\setminus\{0\}\), and
\[
s\in \texttt{linProductSubmodule}\,
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\,A,
\]
then Lean constructs a \texttt{HasSyzygyCertificate} for \(x^2\).
Also added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimage\_product\_incidence},
\]
which combines that certificate with a negativity hypothesis
\(\lambda(x^4)<0\) to produce the exact existential used by
\texttt{residual\_eq\_zero\_of\_rank\_case\_exists\_negative\_syzygy\_certificate}.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one/rank-two branches can now end once their subspace hypotheses and
negative pure square are available.  The largest remaining gaps are the
low-rank support theorem, the binary negative-pure-square theorem, and the
rank-specific instantiation of \(S,A,x\) and the ambient symmetric-square
subspaces.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Preimages as linear subspaces}

\paragraph{Largest gap selected.}
The dimension argument naturally produces a nonzero preimage inside the type
\(A\), but the product-space and syzygy lemmas need \(M\) as an actual
subspace of \(\Lin\).  The formal proof needed a reusable way to view
\(\{a\in A:xa\in P\}\) as a submodule of \(\Lin\).

\paragraph{Lean progress.}
Added
\[
\texttt{linProductLeftPreimageWithin}\ x\ A\ P
  = A\cap\texttt{linProductLeftPreimage}\ x\ P
\]
as a submodule of \(\Lin\), with membership and projection lemmas.  Proved
\[
\texttt{linProductLeftPreimageWithin\_ne\_bot\_of\_on\_ne\_bot}
\]
and the packaged Grassmann criterion
\[
\texttt{linProductLeftPreimageWithin\_ne\_bot\_of\_finrank\_lt\_add}.
\]
This lets the rank cases obtain a nonzero \(M\le\Lin\) directly from the
dimension inequality for \(P\cap xA\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one/rank-two incidence plumbing now has compatible subspace types.
The remaining work is still to instantiate these lemmas with the blueprint's
rank-specific \(P,N,A,B,W\), and to formalize the low-rank support and binary
negative-pure-square structural statements.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Nonzero product preimages from Grassmann}

\paragraph{Largest gap selected.}
The rank-one and rank-two incidence proofs need to pass from a nonzero
intersection element in \(P\cap xA\) to a nonzero element of
\[
M=\{a\in A:xa\in P\}.
\]
This is the step that makes the product-space lower bounds applicable to the
preimage subspace \(M\).

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_mem\_linProductLeftPreimageOn\_ne\_zero\_of\_mem\_inf\_range\_ne\_zero},
\]
which extracts a nonzero preimage from a nonzero element of
\(P\cap\operatorname{range}(x\cdot-)\).  Also added
\[
\texttt{linProductLeftPreimageOn\_ne\_bot\_of\_inf\_range\_ne\_bot}
\]
and the packaged Grassmann criterion
\[
\texttt{linProductLeftPreimageOn\_ne\_bot\_of\_finrank\_lt\_add}.
\]
The last lemma says that if \(P\) and \(\operatorname{range}(x\cdot A)\) sit
inside an ambient quadratic subspace \(W\), \(x\neq0\), and the lower bounds
on \(\dim P\) and \(\dim A\) add to more than \(\dim W\), then the preimage
subspace \(M\) is nonzero.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This closes another finite-dimensional incidence bridge.  The structural
gaps are still the low-rank support theorem and the binary negative-pure-square
theorem, plus the final rank-specific instantiation of the subspaces.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Preimage-intersection dimension comparison}

\paragraph{Largest gap selected.}
The rank-one and rank-two lower bounds on \(M=\{a\in A:xa\in P\}\) use
\(\dim M=\dim(P\cap xA)\), relying on injectivity of multiplication by a
nonzero \(x\).

\paragraph{Lean progress.}
Defined
\[
\texttt{linProductLeftPreimageOnToInfRange},
\]
the canonical linear map from the preimage subspace
\(\{a\in A:xa\in P\}\) to
\[
P\cap \operatorname{range}(\texttt{linProductLeftMapOn}\ x\ A).
\]
For \(x\neq0\), proved this map is bijective and recorded the finrank equality
\[
\texttt{finrank\_linProductLeftPreimageOn\_eq\_inf\_range\_of\_ne\_zero}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This supplies the dimension comparison for the blueprint's preimage subspaces
\(M\).  The remaining rank-case work is to instantiate \(P\), \(A\), \(B\),
and the ambient symmetric-square subspaces, and to formalize the low-rank
support and binary negative-pure-square structural inputs.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product preimage subspaces}

\paragraph{Largest gap selected.}
The rank-one and rank-two incidence constructions define spaces such as
\(M=\{a\in A:xa\in P\}\).  The Lean development had product spans, but not
named preimage subspaces for multiplication by a fixed linear form.

\paragraph{Lean progress.}
Added
\[
\texttt{symSquareSubmodule}\ U=\texttt{linProductSubmodule}\ U\ U
\]
and the fixed-factor preimage subspaces
\[
\texttt{linProductLeftPreimage}\ x\ P\le\Lin,\qquad
\texttt{linProductLeftPreimageOn}\ x\ A\ P\le A.
\]
Both are implemented as comaps of the multiplication linear maps, with
membership simp lemmas stating that membership is exactly the condition
\(xb\in P\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
These definitions make the blueprint's \(M\) and \(N\) subspaces directly
expressible.  The next missing piece is the dimension comparison between such
preimages and the corresponding intersections \(P\cap xA\), followed by the
low-rank support and negative pure-square theorems.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Flexible negative-square reduction}

\paragraph{Largest gap selected.}
The previous rank-case reduction required a syzygy certificate for the exact
negative SOS summand selected from \(p\).  The blueprint's rank-one and
rank-two cases instead use that summand only to prove the existence of a
negative pure square \(x^2\), and then build the syzygy certificate for
\(x^2\).  The formal reduction therefore needed to allow the certified
negative square to differ from the initial SOS summand.

\paragraph{Lean progress.}
Added
\[
\texttt{residual\_eq\_zero\_of\_exists\_negative\_syzygy\_certificate},
\]
which proves \(R=0\) from the existence, under \(R\neq0\), of any quadratic
\(q\) with \(\lambda(q^2)<0\) and a
\texttt{HasSyzygyCertificate} for \(q\).  Also added the rank-split version
\[
\texttt{residual\_eq\_zero\_of\_rank\_case\_exists\_negative\_syzygy\_certificate}.
\]
This theorem still uses the original negative SOS summand to prove the
catalecticant rank is \(1\), \(2\), or \(3\), but each rank branch may now
return a different certified negative square.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The formal reduction now matches the blueprint's rank-one and rank-two flow.
The unresolved pieces are the rank-specific construction of a negative pure
square and the structural low-rank support theorem that supplies the
subspaces used by the product-incidence machinery.

No Julia numerical claims were used in this round.

\section*{2026-05-04: From product incidence to syzygy certificate}

\paragraph{Largest gap selected.}
The incidence lemmas can now produce a nonzero \(s\in N\cap MA\), but the
negative-curvature mechanism requires an explicit finite product identity.
The remaining bridge is to turn membership \(s\in MA\) into the finite
syzygy certificate used by \texttt{HasSyzygyCertificate}.

\paragraph{Lean progress.}
Added
\[
\texttt{hasSyzygyCertificate\_of\_mem\_linProductSubmodule}.
\]
Given a fixed affine-linear form \(x\), subspaces \(M,A\le\Lin\), and
\[
xM\subseteq L,\qquad xA\subseteq K,\qquad s\in L\cap MA,\qquad s\neq0,
\]
the lemma constructs a \texttt{HasSyzygyCertificate} for
\((xx)\).  The proof extracts a finitely supported linear-combination
representation of \(s\in MA\) from
\texttt{Finsupp.mem\_span\_range\_iff\_exists\_finsupp} and rewrites
\[
\sum_j (xm_j)(x a_j)=s\,x^2.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one and rank-two cases now have a direct path from their incidence
witness \(s\) to a syzygy certificate.  What remains is to formalize the
subspace setup that supplies \(xM\subseteq L\), \(xA\subseteq K\), and
\(s\in L\cap MA\), plus the structural low-rank support and binary
negative-pure-square results.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product incidence intersection}

\paragraph{Largest gap selected.}
The rank-one and rank-two arguments require a nonzero element in intersections
such as \(N\cap MA\) and \(N\cap MB\).  After the product-space dimension
lower bounds, the remaining formal step is a Grassmann dimension argument
inside a chosen ambient quadratic subspace.

\paragraph{Lean progress.}
Imported the general Grassmann lemmas from \texttt{Dimension.lean} into
\texttt{Catalecticant.lean} and proved
\[
\texttt{exists\_mem\_inf\_linProductSubmodule\_ne\_zero\_of\_finrank\_lt\_add}.
\]
The lemma says that if \(N\le W\), \(MA\le W\), \(M\neq0\), and the lower
bounds on \(\dim N\) and \(\dim A\) add to more than \(\dim W\), then there is
a nonzero quadratic in \(N\cap MA\).  This packages the blueprint's final
incidence step for both \(N\cap MA\) and \(N\cap MB\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The finite-dimensional intersection mechanism is now available, but the rank
cases still need definitions tying the blueprint's \(P\), \(M\), \(N\), and
ambient \(\operatorname{Sym}^2A\) or \(\operatorname{Sym}^2B\) to concrete
Lean submodules.  The low-rank support theorem and the binary negative
pure-square theorem remain unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Product-space lower bounds}

\paragraph{Largest gap selected.}
After proving that multiplication by a nonzero linear form is injective, the
rank-one and rank-two incidence arguments still need the multi-generator
consequence: if \(M\neq0\), then the product space \(MA\) has dimension at
least \(\dim A\).

\paragraph{Lean progress.}
Proved
\[
\texttt{range\_linProductLeftMapOn\_le\_linProductSubmodule},
\]
which embeds the image of multiplication by a chosen \(m\in M\) into the full
product span \(MA\).  Combining this with injectivity gives
\[
\texttt{finrank\_right\_le\_finrank\_linProductSubmodule\_of\_mem\_ne\_zero}
\]
and the subspace-level version
\[
\texttt{finrank\_right\_le\_finrank\_linProductSubmodule\_of\_left\_ne\_bot}.
\]
Also added the symmetric form
\[
\texttt{finrank\_left\_le\_finrank\_linProductSubmodule\_of\_right\_ne\_bot}.
\]
These are the formal dimension lower bounds behind the blueprint claims
\(\dim(MA)\ge2\) and \(\dim(MB)\ge3\) once \(M\neq0\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The next finite-dimensional incidence step is to combine these product-space
lower bounds with the existing Grassmann intersection lemma to produce a
nonzero element of \(N\cap MA\) or \(N\cap MB\).  The low-rank support theorem
and binary negative-pure-square theorem remain the main structural gaps.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Multiplication by a nonzero linear form}

\paragraph{Largest gap selected.}
The rank-one and rank-two incidence arguments both use the dimension claim
\(\dim(xA)=\dim A\) for a nonzero affine-linear form \(x\).  This is one of
the first product-space dimension facts needed after introducing
\texttt{linProductSubmodule}.

\paragraph{Lean progress.}
Defined the linear maps
\[
\texttt{linProductLeftMap}\ x:\Lin\to\Quad,\qquad
\texttt{linProductLeftMapOn}\ x\ A:A\to\Quad
\]
given by multiplication by \(x\).  Proved
\[
\texttt{linProductLeftMap\_ker\_eq\_bot\_of\_ne\_zero}
\]
and its restricted-subspace version, using that
\(\mathbb R[X_1,X_2,X_3]\) has no zero divisors.  Also proved
\[
\texttt{finrank\_range\_linProductLeftMapOn\_eq},
\]
which states that multiplication by a nonzero affine-linear form preserves
the dimension of any subspace \(A\le\Lin\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This establishes the injective multiplication input used for \(xA\) and
\(MB\).  The remaining incidence work still needs dimension lower bounds for
the multi-generator product spaces \(MA\) and \(MB\), plus the low-rank support
and binary negative-pure-square theorems.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Linear annihilator for support reduction}

\paragraph{Largest gap selected.}
The next structural blocker is the low-rank apolar support theorem, especially
the bridge from its linear annihilator \(A\subseteq\Lin\) to the kernel
inclusion \(A\Lin\subseteq K\).

\paragraph{Lean progress.}
Added product algebra lemmas for \texttt{linProduct}: additivity and scalar
compatibility in each factor, zero-factor simplifications, commutativity, and
\[
\texttt{linProductSubmodule\_comm}.
\]
Defined
\[
\texttt{linearAnnihilator}\ B\ p\ u\le\Lin,
\]
the subspace of affine-linear forms \(a\) such that \(ae\) is in
\(\ker(\texttt{catalecticantMap}\ B\ p\ u)\) for every \(e\in\Lin\).
Also proved
\[
\texttt{linProductSubmodule\_linearAnnihilator\_top\_le\_ker}
\]
and the more flexible
\[
\texttt{linProductSubmodule\_le\_ker\_of\_le\_linearAnnihilator},
\]
which turn a subspace inclusion \(A\le\texttt{linearAnnihilator}\) into the
formal version of \(A\Lin\subseteq K\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This round formalizes the target object of the support theorem, but not the
Macaulay/apolar Hilbert-function argument proving that a large enough
\(\texttt{linearAnnihilator}\) exists at catalecticant rank at most three.
The rank-one and rank-two arguments still need their finite-dimensional
incidence and negative pure-square lemmas.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Quartic degree subspace}

\paragraph{Largest gap selected.}
The finite-dimensional polynomial-space inventory in the blueprint includes
the quartic degree filtration and the closure fact
\(\Quad\cdot\Quad\subseteq\Quart\).  The Lean proof had \(\Lin\) and
\(\Quad\), but not the bundled quartic subspace.

\paragraph{Lean progress.}
Added
\[
\texttt{quarticSubmodule}\le\Poly
\]
with the membership simp lemma and equality to
\[
\texttt{MvPolynomial.restrictTotalDegree}\ (Fin\ 3)\ \mathbb R\ 4.
\]
Also added the finite-module instance and proved
\[
\texttt{mul\_quad\_quad\_mem\_quartic},\qquad
\texttt{sq\_quad\_mem\_quartic}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The degree-filtration layer now includes \(\Quart\).  The harder remaining
finite-dimensional inventory item is still the proof that products of
affine-linear forms span all of \(\Quad\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-case support data to certificate family}

\paragraph{Largest gap selected.}
The rank-one/rank-two and rank-three support-data predicates were connected
to certificates separately.  The proof still needed one combined bridge from
all branch support data to the named global certificate-family obligation.

\paragraph{Lean progress.}
In \texttt{Basic.lean}, defined
\[
\texttt{HasRankCaseSupportData}\ B\ p\ u\ hu,
\]
which provides \texttt{HasPreimageProductSupportData} in ranks \(1\) and
\(2\), and \texttt{HasRankThreeAnnihilatorSupportData} in rank \(3\).  Proved
\[
\texttt{hasRankCaseNegativeCertificateFamily\_of\_supportData}
\]
and
\[
\texttt{residual\_eq\_zero\_of\_rankCaseSupportData}.
\]
Thus the main theorem is now conditionally reduced to producing
\texttt{HasRankCaseSupportData} in the contradiction setting.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The branch endpoints are now uniformly connected to the global certificate
family.  The remaining mathematical work is the construction of those branch
support data from the low-rank support theorem and binary negative-pure-square
theorem.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Preimage-product support data predicate}

\paragraph{Largest gap selected.}
The rank-one and rank-two branches share the same endpoint: a preimage
subspace \(M=\{a\in A:xa\in S\}\), a nonzero intersection produced by
dimension, and negativity of \(x^4\).  This geometric endpoint needed a named
Lean predicate parallel to the rank-three support-data predicate.

\paragraph{Lean progress.}
Added
\[
\texttt{HasPreimageProductSupportData}\ B\ p\ u\ hu.
\]
It packages the annihilator subspace \(A\), the subspaces \(S,W\le\Quad\),
the inclusion
\[
\texttt{linProductSubmodule}
  (\texttt{linProductLeftPreimageWithin}\ x\ A\ S)\ A\le W,
\]
nontriviality of the preimage, the Grassmann dimension inequalities, and
the negativity \(B(x^4,R)<0\).  Proved
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimageProductSupportData},
\]
which converts this data into the negative syzygy-certificate existential for
the global rank split.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-one/rank-two endpoint is now named and connected to the proof.  The
remaining work is to derive this support data from the binary pure-square
theorem and low-rank support theorem in the concrete rank-one and rank-two
cases.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three support data predicate}

\paragraph{Largest gap selected.}
After the rank-three annihilator endpoint was packaged, the remaining
rank-three geometric work could be stated as: find \(z\), \(W\), and a
negative supported quadratic \(q\in WW\).  This needed a named Lean predicate.

\paragraph{Lean progress.}
In \texttt{Support.lean}, added
\[
\texttt{HasRankThreeAnnihilatorSupportData}\ B\ p\ u.
\]
It packages \(z\in\texttt{linearAnnihilator}\), \(z\neq0\),
\(q\in\texttt{linProductSubmodule}\ W\ W\), and \(B(q^2,R)<0\).  Proved
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_rank\_three\_supportData},
\]
which combines this data with the rank-three hypotheses and returns the
negative syzygy-certificate existential required by the global rank split.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three branch is now reduced to proving
\texttt{HasRankThreeAnnihilatorSupportData}; this is exactly the support and
negative-square decomposition part of the blueprint.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Support codimension monotonicity}

\paragraph{Largest gap selected.}
The low-rank support theorem is naturally stated for rank at most \(k\), while
individual branches may consume weaker support bounds.  The formal support
interface needed monotonicity in \(k\).

\paragraph{Lean progress.}
Added
\[
\texttt{HasLinearAnnihilatorCodimAtMost.mono}.
\]
If \(k\le l\), then a codimension-at-most-\(k\) annihilator immediately gives
a codimension-at-most-\(l\) annihilator.  Also added the concrete convenience
lemmas
\[
\texttt{HasLinearAnnihilatorCodimAtMost.rank\_one\_to\_two},\qquad
\texttt{HasLinearAnnihilatorCodimAtMost.rank\_two\_to\_three}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The support interface can now be weakened between rank cases.  The substantive
support theorem proving the interface from the catalecticant rank remains
open.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Low-rank support interface}

\paragraph{Largest gap selected.}
The low-rank support theorem is still the largest structural blocker.  Its
output needs a precise Lean interface before the rank cases can consume it:
a large annihilator subspace and a complementary support subspace.

\paragraph{Lean progress.}
Created \texttt{QuaternaryQuarticProof/Support.lean}.  Defined
\[
\texttt{HasLinearAnnihilatorCodimAtMost}\ B\ p\ u\ k,
\]
meaning there is \(A\le\texttt{linearAnnihilator}\ B\ p\ u\) with
\[
4-k\le\dim A.
\]
Proved
\[
\texttt{exists\_support\_complement\_of\_hasLinearAnnihilatorCodimAtMost},
\]
which chooses a complement \(W\) with \(\Lin=A\oplus W\) and
\(\dim W\le k\).  Also added specialized rank-one, rank-two, and rank-three
versions with the concrete dimension bounds \(3,2,1\) for \(A\).
The new module is imported through \texttt{QuaternaryQuarticProof/Basic.lean}.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
This does not prove the support theorem itself, but it pins down the exact
finite-dimensional output needed by the rank cases.  The next structural step
is proving \texttt{HasLinearAnnihilatorCodimAtMost} from low catalecticant
rank.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Complement dimension bookkeeping}

\paragraph{Largest gap selected.}
The low-rank support theorem will produce an annihilator subspace
\(A\subseteq\Lin\), then choose a complementary support subspace \(W\) with
\(\Lin=W\oplus A\).  The Lean development needed reusable dimension
bookkeeping for such complements.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Dimension.lean} with
\[
\texttt{exists\_isCompl\_finrank\_eq\_sub}
\]
and
\[
\texttt{exists\_isCompl\_finrank\_add\_eq}.
\]
These lemmas use \texttt{Submodule.exists\_isCompl} and
\texttt{Submodule.finrank\_add\_eq\_of\_isCompl} to choose complements and
record the expected finrank equalities.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The complement bookkeeping for the support theorem is now available.  The
substantive missing part remains proving that a sufficiently large
annihilator subspace exists from catalecticant rank at most three.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Rank-three annihilator endpoint}

\paragraph{Largest gap selected.}
In the rank-three branch \(K=L\), so support-theorem annihilator data should
provide both \(zW\subseteq K\) and \(zW\subseteq L\).  This conversion was not
yet packaged.

\paragraph{Lean progress.}
In \texttt{Catalecticant.lean}, proved
\[
\texttt{mem\_spanUQuad\_of\_rank\_three\_of\_mem\_catalecticantKernel}
\]
and
\[
\texttt{linProduct\_mem\_spanUQuad\_of\_rank\_three\_of\_mem\_linearAnnihilator}.
\]
Also added \(\texttt{linProduct\_self\_ne\_zero}\), using that the polynomial
ring has no zero divisors.

In \texttt{Syzygy.lean}, added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_rank\_three\_annihilator\_support}.
\]
Under the rank-three hypotheses, \(z\in\texttt{linearAnnihilator}\),
\(z\neq0\), \(q\in WW\), and \(B(q^2,R)<0\), this returns the negative
syzygy-certificate existential for the global rank split.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The rank-three endpoint can now consume annihilator support data directly.
The missing structural pieces are the existence of such \(z,W\) and the
decomposition of a negative square into a supported \(WW\)-quadratic.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Certificate wrappers from annihilator data}

\paragraph{Largest gap selected.}
The support theorem will naturally provide annihilator data, such as
\(A\le\texttt{linearAnnihilator}\) or \(z\in\texttt{linearAnnihilator}\).
The rank-case certificate wrappers previously expected the already-converted
kernel inclusions \(xA\subseteq K\) and \(zW\subseteq K\).

\paragraph{Lean progress.}
In \texttt{Catalecticant.lean}, added commuted annihilator-to-kernel lemmas:
\[
\texttt{linProduct\_comm\_mem\_catalecticantKernel\_of\_mem\_linearAnnihilator}
\]
and
\[
\texttt{linProduct\_comm\_mem\_catalecticantKernel\_of\_le\_linearAnnihilator}.
\]
In \texttt{Syzygy.lean}, added wrappers
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimage\_product\_dimension\_of\_annihilator}
\]
and
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_support\_product\_decomposition\_of\_annihilator}.
\]
These versions consume annihilator hypotheses directly and internally produce
the catalecticant-kernel inclusions needed for the syzygy certificate.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Syzygy.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The eventual low-rank support theorem can now feed its annihilator output
directly into both the rank-one/rank-two and rank-three certificate
constructors.  The support theorem itself remains unformalized.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Named rank-case certificate obligation}

\paragraph{Largest gap selected.}
The proof had a rank-split theorem accepting a long anonymous hypothesis.
To make the remaining global gap explicit, Lean needed a named predicate for
the structural rank-case certificate family.

\paragraph{Lean progress.}
In \texttt{QuaternaryQuarticProof/Basic.lean}, defined
\[
\texttt{HasRankCaseNegativeCertificateFamily}\ B\ p\ u.
\]
It states that under \(R\neq0\) and full rank of the seven factor quadratics,
each catalecticant rank \(1,2,3\) returns a negative quadratic square together
with a \texttt{HasSyzygyCertificate}.  Proved
\[
\texttt{residual\_eq\_zero\_of\_hasRankCaseNegativeCertificateFamily}
\]
and
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_rankCaseNegativeCertificates}.
\]
The latter shows that a uniform proof of this named certificate family implies
the immutable target
\(\texttt{QuaternaryQuarticRankSevenNoSpuriousSOCP}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The final theorem is now reduced to a single named structural obligation.
The remaining work is to prove \(\texttt{HasRankCaseNegativeCertificateFamily}\)
from the low-rank support theorem, the binary negative-pure-square theorem,
and the rank-specific subspace instantiations already packaged in
\texttt{Syzygy.lean}.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Linear annihilator to polynomial kernel}

\paragraph{Largest gap selected.}
The low-rank support theorem is expected to produce subspaces
\(A\le\texttt{linearAnnihilator}\), while the syzygy certificate wrappers need
pointwise polynomial-kernel facts \(xA\subseteq K\).  The Lean development
needed this conversion as a reusable lemma.

\paragraph{Lean progress.}
Added
\[
\texttt{linProduct\_mem\_catalecticantKernel\_of\_mem\_linearAnnihilator}
\]
and
\[
\texttt{linProduct\_mem\_catalecticantKernel\_of\_le\_linearAnnihilator}.
\]
They convert membership in \(\texttt{linearAnnihilator}\) through
\texttt{mem\_ker\_catalecticantMap\_iff} into the polynomial kernel statement
used by the negative-curvature certificate constructors.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
Once a formal low-rank support theorem supplies \(A\le
\texttt{linearAnnihilator}\), these lemmas now feed its output directly into
the rank-one/rank-two certificate wrappers.  The existence proof for such an
\(A\) remains the major structural gap.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Catalecticant symmetry lemmas}

\paragraph{Largest gap selected.}
The binary normal-form theorem treats the degree-two catalecticant as a
symmetric Hankel form.  In Lean, this symmetry does not come from any symmetry
assumption on \(B\); it comes only from commutativity of polynomial
multiplication in the first argument of \(B\).

\paragraph{Lean progress.}
Added
\[
\texttt{catalecticantMap\_pair\_comm}
\]
for the restricted map
\(\texttt{catalecticantMap}\ B\ p\ u:\Quad\to\Quad^\ast\), and
\[
\texttt{catalecticantKernel\_pair\_comm}
\]
for the underlying polynomial product pairing.  These lemmas record
\[
B(qr,R)=B(rq,R)
\]
without requiring \(B\) itself to be symmetric.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The catalecticant symmetry fact is now explicit.  The remaining binary work is
to connect concrete Hankel kernel normal forms to these pairings and to the
scalar sign lemmas.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Explicit linear product generators}

\paragraph{Largest gap selected.}
The finite-dimensional inventory still lacks the formal statement that
products of affine-linear forms span all quadratics.  A necessary concrete
step is to expose the standard generators \(1\), \(X_i\), and \(X_iX_j\) as
elements of the product span.

\paragraph{Lean progress.}
Added the bundled affine-linear forms
\[
\texttt{linOne}:\Lin,\qquad \texttt{linVar}\ i:\Lin
\]
with simp lemmas for their polynomial values.  Proved membership of the
basic degree \(0\), \(1\), and \(2\) products in
\[
\texttt{linProductSubmodule}\ \top\ \top:
\]
\[
\texttt{linOne\_mul\_linOne\_mem\_linProductSubmodule\_top\_top},
\]
\[
\texttt{linVar\_mul\_linOne\_mem\_linProductSubmodule\_top\_top},
\qquad
\texttt{linVar\_mul\_linVar\_mem\_linProductSubmodule\_top\_top}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The standard product generators are now available as bundled elements.  The
remaining span proof must connect arbitrary degree-\(\le2\) monomials to
these generators and then use the restricted-support monomial basis to show
\(\texttt{linProductSubmodule}\ \top\ \top=\top\).

No Julia numerical claims were used in this round.

\section*{2026-05-04: Binary normal-form scalar cases}

\paragraph{Largest gap selected.}
Continuing the binary negative-pure-square theorem, the blueprint also uses
two scalar normal-form facts: in the diagonal case a negative value of
\(aX^2+eY^2\) forces one diagonal coefficient negative, and in the rank-one
case a negative scalar multiple of a fourth power gives a negative pure
fourth power.

\paragraph{Lean progress.}
Extended \texttt{QuaternaryQuarticProof/Binary.lean} with
\[
\texttt{diagonal\_form\_negative\_pure\_of\_negative},\qquad
\texttt{diagonal\_form\_exists\_pure\_negative},
\]
and
\[
\texttt{rank\_one\_negative\_value}.
\]
These lemmas formalize the scalar inequalities used in the \(xy\in\ker M\)
and rank-one Hankel normal-form branches.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
The binary theorem still needs the algebraic bridge from catalecticant kernel
normal forms to these scalar lemmas.  The real inequality pieces for the
normal-form case split are now mostly isolated.

No Julia numerical claims were used in this round.

\section*{2026-05-04: Binary negative-square sign lemmas}

\paragraph{Largest gap selected.}
One of the remaining structural blockers is the binary negative-pure-square
theorem.  The blueprint's proof reduces several binary kernel normal forms to
elementary real-variable sign claims, for example choosing \(t\) so that
\(a+4bt<0\), \(4b(t-t^3)<0\), or
\[
a(1-6t^2+t^4)+4b(t-t^3)<0.
\]

\paragraph{Lean progress.}
Created \texttt{QuaternaryQuarticProof/Binary.lean} and formalized these
real sign lemmas:
\[
\texttt{exists\_linear\_combination\_lt\_zero},\quad
\texttt{exists\_cubic\_tail\_lt\_zero},
\]
\[
\texttt{exists\_quartic\_tail\_lt\_zero\_of\_nonzero\_linear},\quad
\texttt{exists\_quartic\_tail\_lt\_zero\_of\_nonzero\_quadratic},
\]
and the combined
\[
\texttt{exists\_quartic\_tail\_lt\_zero}.
\]
Also recorded the exact values of the quartic tail at \(t=0\) and \(t=1\).
The new module is imported through \texttt{QuaternaryQuarticProof/Basic.lean}
so it is included in the editable proof build.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.

\paragraph{Remaining blockers.}
These lemmas cover the real sign choices in the binary rank-two normal-form
case split.  The remaining work for the binary theorem is to connect the
Hankel matrix/kernel normal forms to these scalar coefficients and to package
the resulting negative pure fourth power as a \(\texttt{linProduct}\)-square.

No Julia numerical claims were used in this round.

\section*{Quartic ambient dimension}

\paragraph{Gap targeted.}
The catalecticant rank and support arguments ultimately compare kernels and
images inside the quartic ambient space.  Earlier rounds had formal dimension
counts for linear and quadratic forms, but the quartic space itself was still
only named as a filtered submodule.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_quarticSubmodule\_eq\_thirtyFive}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  The proof identifies
degree-\(\leq 4\) monomials in four variables with bounded triples
\((a,b,c)\in\{0,\ldots,4\}^3\) satisfying \(a+b+c\leq 4\), then lets
\texttt{decide} compute the finite cardinality \(35\).  This is the
quaternary quartic analogue of the existing four-dimensional linear and
ten-dimensional quadratic counts.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run after this log entry; the only
expected remaining failure is the root theorem's placeholder axiom.

\paragraph{Remaining blockers.}
The largest gap remains the structural support theorem: deriving the rank-case
support data from the catalecticant/kernel hypotheses, then using it to replace
the root placeholder.

No Julia numerical claims were used in this round.

\section*{Preimage product support constructors}

\paragraph{Gap targeted.}
The rank-one and rank-two cases both need the same finite-dimensional
intersection step: a product preimage is nonzero because two subspaces inside a
fixed quadratic ambient space have dimensions whose sum exceeds the ambient
dimension.  This was already available near the syzygy proof, but
\(\texttt{HasPreimageProductSupportData}\) still required the nonzero
preimage as an explicit field.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_dimension}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It packages the intersection
dimension inequality into the support-data predicate by invoking
\[
\texttt{linProductLeftPreimageWithin\_ne\_bot\_of\_finrank\_lt\_add}.
\]
Also added rank-specialized arithmetic wrappers for the blueprint's two lower
rank cases:
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_dimensions}
\]
with dimensions \(4+2>5\), and
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_one\_dimensions}
\]
with dimensions \(4+3>6\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The next support gap is to instantiate these constructors from the actual
rank-one and rank-two decompositions supplied by the low-rank apolar support
theorem and the binary negative-square theorem.

No Julia numerical claims were used in this round.

\section*{Annihilator as a linear-map kernel}

\paragraph{Gap targeted.}
The low-rank apolar support theorem needs dimension control of the degree-one
annihilator.  The previous formalization defined the annihilator extensionally
as the set of linear forms whose products with all linear forms lie in the
catalecticant kernel, but this was not yet connected to rank-nullity.

\paragraph{Lean progress.}
Added
\[
\texttt{linearAnnihilatorMap}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  This map sends a
linear form \(a\) to the linear map \(e\mapsto ae\) modulo the degree-two
catalecticant kernel.  Proved
\[
\texttt{linearAnnihilator\_eq\_ker\_linearAnnihilatorMap}
\]
and the rank-nullity consequence
\[
\texttt{finrank\_range\_linearAnnihilatorMap\_add\_finrank\_linearAnnihilator}.
\]
The latter records the formal identity
\[
\dim(\operatorname{range})+\dim(\operatorname{Ann}_1)=4.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The support theorem still needs a bound on the range of
\(\texttt{linearAnnihilatorMap}\) from catalecticant rank at most three.  This
round supplies the exact rank-nullity object that such a bound will feed into.

No Julia numerical claims were used in this round.

\section*{Rank-nullity bridge for the apolar support predicate}

\paragraph{Gap targeted.}
After introducing the quotient-valued annihilator map, the support predicate
\(\texttt{HasLinearAnnihilatorCodimAtMost}\) still needed a direct way to use
a bound on that map's image dimension.

\paragraph{Lean progress.}
Added
\[
\texttt{hasLinearAnnihilatorCodimAtMost\_of\_annihilatorMap\_range}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It applies the rank-nullity
identity for \(\texttt{linearAnnihilatorMap}\) to show that if the image has
dimension at most \(k\le4\), then the annihilator has dimension at least
\(4-k\).  Also added the rank-specialized wrappers for \(k=1,2,3\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining structural step is now sharper: prove the annihilator-map image
bound from catalecticant rank at most \(1,2,3\), or supply an equivalent
formal version of the low-rank apolar support theorem.

No Julia numerical claims were used in this round.

\section*{Catalecticant quotient dimension}

\paragraph{Gap targeted.}
The low-rank support argument passes through the quotient of quadratics by the
catalecticant kernel.  To use catalecticant rank hypotheses inside this
quotient, the formalization needs a direct dimension identity.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_quotient\_ker\_catalecticantMap\_eq\_rank}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  It uses the standard
\(\texttt{quotKerEquivRange}\) isomorphism to identify
\[
\dim(\Quad/\ker C_\lambda)=\dim(\operatorname{range} C_\lambda).
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The next missing structural comparison is bounding the image of
\(\texttt{linearAnnihilatorMap}\) using this low-dimensional quotient and the
special inverse-system facts for rank at most three.

No Julia numerical claims were used in this round.

\section*{Binary normal-form evaluation lemmas}

\paragraph{Gap targeted.}
The binary rank-at-most-two theorem needs to turn scalar sign choices into
negative values of actual binary quartic evaluations.  The previous round had
the scalar inequalities but not the uniform quartic-evaluation packaging.

\paragraph{Lean progress.}
Added
\[
\texttt{binaryQuarticEval}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}, together with normal-form
existence lemmas matching the blueprint's case split:
\[
\texttt{diagonal\_binaryQuarticEval\_exists\_negative},
\]
\[
\texttt{y\_sq\_kernel\_binaryQuarticEval\_exists\_negative},
\]
and
\[
\texttt{elliptic\_kernel\_binaryQuarticEval\_exists\_negative}.
\]
These package the diagonal, \(y^2\)-kernel, and \(x^2+y^2\)-kernel cases as
statements that some pair \((x,y)\) gives a negative pure fourth-power value in
the corresponding binary normal form.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining binary work is to connect catalecticant/Hankel kernel normal
forms to these evaluation lemmas and then transport the resulting binary linear
form back to the quaternary support subspace.

No Julia numerical claims were used in this round.

\section*{Rank-one binary outer-product evaluation}

\paragraph{Gap targeted.}
The binary normal-form branch also includes the rank-one Hankel case, where the
catalecticant is a negative scalar times an outer product.  The scalar
negativity lemma was present, but it had not yet been packaged as a negative
binary quartic evaluation.

\paragraph{Lean progress.}
Added
\[
\texttt{rank\_one\_binaryQuarticEval\_exists\_negative}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  If
\(\rho<0\) and \((\alpha,\beta)\ne(0,0)\), the lemma chooses
\((x,y)=(\alpha,\beta)\) and proves the corresponding binary quartic value is
\[
\rho(\alpha^2+\beta^2)^4<0.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
All binary scalar normal forms now have quartic-evaluation wrappers.  The next
binary step is to derive those normal forms from an actual rank-one or rank-two
Hankel/catalecticant kernel hypothesis.

No Julia numerical claims were used in this round.

\section*{Rank-three annihilator extraction}

\paragraph{Gap targeted.}
In the rank-three branch, once the low-rank support theorem supplies an
annihilator of codimension at most three, the proof needs an explicit nonzero
linear form \(z\) in that annihilator to build the syzygy identity.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_nonzero\_mem\_linearAnnihilator\_of\_rank\_three\_support}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It extracts a nonzero
element from the annihilator subspace using the formal dimension lower bound
\(\dim A\ge1\).  Also added
\[
\texttt{hasRankThreeAnnihilatorSupportData\_of\_rank\_three\_support},
\]
which packages such a \(z\) together with a negative quadratic supported on a
chosen \(W\) into \(\texttt{HasRankThreeAnnihilatorSupportData}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-three branch still needs the support theorem and a formal way to
choose a negative quadratic inside the supported \(W\)-quadratic subspace.

No Julia numerical claims were used in this round.

\section*{Direct rank-three support certificate}

\paragraph{Gap targeted.}
The rank-three support branch had two separate formal steps: extract a nonzero
annihilator element, then invoke the rank-three syzygy construction.  The main
rank-case proof will be simpler if that composition is available as one lemma.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_rank\_three\_support}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It consumes
\(\texttt{HasLinearAnnihilatorCodimAtMost}\) at \(k=3\), the rank-three
catalecticant equality, and a negative quadratic in
\(\texttt{linProductSubmodule W W}\), then returns the negative syzygy
certificate required by the rank-case reduction.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-three case is now reduced to proving the low-rank support theorem and
placing a negative square inside the supported quadratic subspace.

No Julia numerical claims were used in this round.

\section*{Final-theorem bridge from rank-case support data}

\paragraph{Gap targeted.}
The root theorem still depends on the placeholder, but the proof architecture
had only a final constructor from rank-case negative certificates.  Since the
current work is focused on support data, the final theorem should also have a
direct support-data constructor.

\paragraph{Lean progress.}
Added
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_rankCaseSupportData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  It converts a uniform proof of
\(\texttt{HasRankCaseSupportData}\) for every positive-definite instance into
the target statement, by invoking
\[
\texttt{residual\_eq\_zero\_of\_rankCaseSupportData}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining top-level obligation is now concentrated in one statement:
construct \(\texttt{HasRankCaseSupportData}\) uniformly from the low-rank
support theorem, binary negative-square theorem, and the rank-three support
branch.

No Julia numerical claims were used in this round.

\section*{Kernel-product conclusion from support data}

\paragraph{Gap targeted.}
The low-rank support theorem in the blueprint is used through the concrete
geometric conclusion \(AE\subseteq K\).  The Lean support predicate already
stored \(A\le\operatorname{Ann}_1\), but downstream rank-case proofs still had
to recover the product-kernel inclusion separately.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_support\_complement\_kernel\_product\_of\_hasLinearAnnihilatorCodimAtMost}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It extracts a complement
\(E=A\oplus W\), the dimension bounds \(\dim W\le k\) and
\(\dim A\ge 4-k\), and the formal inclusion
\[
\texttt{linProductSubmodule A top <= ker(catalecticantMap B p u)}.
\]
Also added rank-one, rank-two, and rank-three specializations matching the
case split in the blueprint.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The apolar support theorem still has to produce
\(\texttt{HasLinearAnnihilatorCodimAtMost}\) from low catalecticant rank.  This
round packages the exact conclusion all later rank-case incidence arguments
need once that theorem is available.

No Julia numerical claims were used in this round.

\section*{Pointwise kernel membership from product support}

\paragraph{Gap targeted.}
The support extraction now provides the submodule-level inclusion
\(\texttt{linProductSubmodule A top <= ker(catalecticantMap)}\).  Several
syzygy constructions, however, consume the pointwise polynomial predicate
\(\texttt{catalecticantKernel}\).

\paragraph{Lean progress.}
Added
\[
\texttt{linProduct\_mem\_catalecticantKernel\_of\_product\_le\_ker}
\]
and its commuted form to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.
These lemmas turn the \(AE\subseteq K\) submodule inclusion into statements
that each individual product \(ae\) lies in the polynomial catalecticant
kernel.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one and rank-two incidence constructions still need to thread the
support decomposition through the preimage/intersection machinery.  This bridge
removes one translation step between support data and syzygy input.

No Julia numerical claims were used in this round.

\section*{Rank-three Macaulay arithmetic core}

\paragraph{Gap targeted.}
The apolar support theorem's rank-three case uses a short Hilbert-function
contradiction.  After choosing \(1\le b\le3\), Macaulay's degree-two bound would
force \(4-b\le 3-b\), which is impossible.

\paragraph{Lean progress.}
Added
\[
\texttt{rankThreeMacaulayNumericalContradiction}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}, with explicit case lemmas
for \(b=1,2,3\).  These isolate the finite arithmetic part of the
rank-three apolar-support proof from the still-missing Hilbert-function
formalization.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The surrounding apolar algebra is still missing: formal Hilbert functions,
Macaulay growth, and Gorenstein symmetry must be connected to this arithmetic
contradiction to prove the support theorem.

No Julia numerical claims were used in this round.

\section*{Binary Hankel-case wrappers}

\paragraph{Gap targeted.}
The binary rank-at-most-two proof needs to connect Hankel kernel normal forms
to the previously formalized negative pure-fourth-power evaluations.  The
scalar evaluation lemmas existed, but the coefficient hypotheses from the
individual kernel cases were not yet packaged.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Binary.lean}:
\[
\texttt{xy\_kernel\_binaryQuarticEval\_exists\_negative},
\]
\[
\texttt{y\_sq\_kernel\_binaryQuarticEval\_exists\_negative\_of\_rank\_two},
\]
and
\[
\texttt{elliptic\_kernel\_binaryQuarticEval\_exists\_negative\_of\_nonzero\_hankel}.
\]
These lemmas match the blueprint's \(xy\), \(y^2\), and \(x^2+y^2\) kernel
normal forms and dispatch to the already-proved real-variable sign choices.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining binary work is to derive the normal-form coefficient hypotheses
from the actual rank-one/rank-two catalecticant matrix and transport the
resulting binary linear form back through the quaternary support decomposition.

No Julia numerical claims were used in this round.

\section*{Preimage incidence with ambient dimension upper bounds}

\paragraph{Gap targeted.}
The rank-one and rank-two incidence arguments often produce an ambient
quadratic subspace with a dimension upper bound before proving or using an
exact equality.  The existing preimage nonzero lemmas required an exact
\(\texttt{finrank W = wdim}\), which made future applications unnecessarily
rigid.

\paragraph{Lean progress.}
Added upper-bound variants in
\texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{linProductLeftPreimageOn\_ne\_bot\_of\_finrank\_le\_lt\_add}
\]
and
\[
\texttt{linProductLeftPreimageWithin\_ne\_bot\_of\_finrank\_le\_lt\_add}.
\]
Then added
\[
\texttt{hasPreimageProductSupportData\_of\_dimension\_le}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, which stores the actual
ambient dimension but allows callers to prove only an upper bound
\(\dim W\le wdim\) together with \(wdim<sdim+adim\).

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The incidence framework is now less brittle, but the rank-one/rank-two cases
still need concrete ambient subspaces and dimension bounds from the support
decomposition and binary negative-square theorem.

No Julia numerical claims were used in this round.

\section*{Rank-specific ambient bound wrappers}

\paragraph{Gap targeted.}
After adding the general upper-bound support-data constructor, the rank-one
and rank-two cases still needed convenient wrappers for the blueprint's fixed
ambient bounds \(5\) and \(6\).

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_dimension\_bounds}
\]
and
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_one\_dimension\_bounds}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  They package the arithmetic
\(5<4+2\) and \(6<4+3\) while accepting upper bounds
\(\dim W\le5\) and \(\dim W\le6\), respectively.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining incidence work is now concentrated in proving the inputs to
these wrappers: the \(S\)-dimension lower bound, the ambient bound, and the
range/product inclusions from the rank-case support decomposition.

No Julia numerical claims were used in this round.

\section*{Grassmann lower bounds for incidence intersections}

\paragraph{Gap targeted.}
The rank-one and rank-two incidence arguments require lower bounds such as
\(\dim(L\cap U)\ge4\), derived from \(\dim L=7\), a support ambient dimension,
and the catalecticant kernel dimension.  The previous Grassmann lemmas mainly
produced nonzero intersections, not explicit lower bounds.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_inf\_ge\_add\_sub\_of\_le}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}.  It proves the general
finite-dimensional inequality
\[
a+b-c \le \dim(s\cap t)
\]
from \(s,t\le w\), lower bounds \(a\le\dim s\), \(b\le\dim t\), and an upper
bound \(\dim w\le c\).  Also added the blueprint-specific specializations
\[
\texttt{four\_le\_finrank\_inf\_of\_seven\_five\_eight}
\]
and
\[
\texttt{four\_le\_finrank\_inf\_of\_seven\_six\_nine}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The incidence proof still needs to define the rank-one/rank-two support
ambient subspaces \(U\) and prove their dimension upper bounds.  This round
supplies the Grassmann step once those subspaces are available.

No Julia numerical claims were used in this round.

\section*{Rank-case span--ambient intersection bounds}

\paragraph{Gap targeted.}
The lower rank incidence proof needs the subspace
\(P=L\cap U\), where \(L=\texttt{spanUQuad}\) and \(U\) is a support ambient
inside the catalecticant kernel, to have dimension at least four.  The generic
Grassmann bound was available after the previous round but not yet connected to
the catalecticant rank hypotheses.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{four\_le\_finrank\_spanUQuad\_inf\_of\_rank\_two\_ambient}
\]
and
\[
\texttt{four\_le\_finrank\_spanUQuad\_inf\_of\_rank\_one\_ambient}.
\]
They use \(\dim L=7\), the rank-two/rank-one kernel dimensions \(8\) and \(9\),
and an ambient lower bound \(\dim U\ge5\) or \(\dim U\ge6\) to prove
\[
4\le\dim(\texttt{spanUQuad hu} \cap U).
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one/rank-two branches still need the concrete definitions and
dimension bounds for their support ambient subspaces \(U\), but the
catalecticant-rank part of the \(P=L\cap U\) lower bound is now packaged.

No Julia numerical claims were used in this round.

\section*{Rank-one and rank-two ambient support constructors}

\paragraph{Gap targeted.}
The lower-rank cases choose \(S=L\cap U\), where
\(L=\texttt{spanUQuad}\) and \(U\) is the support ambient inside the
catalecticant kernel.  The support-data constructors still required callers to
assemble this choice and its dimension bound manually.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_ambient\_bounds}
\]
and
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_one\_ambient\_bounds}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  These lemmas set
\[
S=\texttt{spanUQuad hu} \cap U
\]
and use the rank-specific catalecticant intersection bounds to prove
\(\dim S\ge4\).  The caller now supplies the concrete ambient \(U\), the
kernel inclusion \(U\subseteq K\), lower and upper dimension bounds for \(U\),
and the range/product inclusions needed by the preimage construction.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one/rank-two proof now needs concrete construction of the ambient
subspaces \(U\) from the support decomposition and the binary negative pure
square.  The repetitive \(S=L\cap U\) bookkeeping is packaged.

No Julia numerical claims were used in this round.

\section*{Dimension bound for sums of subspaces}

\paragraph{Gap targeted.}
The rank-one and rank-two ambient subspaces are sums such as
\(xA+\operatorname{Sym}^2 A\).  Their dimension bounds require the elementary
inequality \(\dim(S+T)\le\dim S+\dim T\).

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_sup\_le\_add}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}, derived from
\(\texttt{Submodule.finrank\_sup\_add\_finrank\_inf\_eq}\).  This will be used
to bound the finite-dimensional support ambient spaces that appear in the
rank-one and rank-two cases.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The product-subspace dimension bounds for \(\operatorname{Sym}^2 A\) remain to
be formalized and then combined with this sum bound.

No Julia numerical claims were used in this round.

\section*{Bounded subspace-sum dimension estimates}

\paragraph{Gap targeted.}
The rank-one and rank-two ambient spaces are built as sums of smaller product
subspaces.  The previous round added \(\dim(S+T)\le\dim S+\dim T\), but callers
would still need to repeat the arithmetic for the specific blueprint bounds.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_sup\_le\_of\_le\_add}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}, plus the specializations
\[
\texttt{finrank\_sup\_le\_five\_of\_le\_two\_three},\quad
\texttt{finrank\_sup\_le\_six\_of\_le\_three\_three},\quad
\texttt{finrank\_sup\_le\_nine\_of\_le\_three\_six}.
\]
These package the finite-dimensional arithmetic needed to bound the support
ambient spaces in the lower-rank incidence arguments.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The product-subspace dimension bounds feeding these sum estimates remain to be
formalized for the concrete support subspaces.

No Julia numerical claims were used in this round.

\section*{Fixed-linear product range dimension bounds}

\paragraph{Gap targeted.}
The lower-rank ambient spaces contain components of the form \(xA\), the image
of multiplication by a fixed linear form.  Their dimension is bounded by
\(\dim A\), independent of whether \(x\) is known nonzero.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_range\_linProductLeftMapOn\_le}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}, with rank-specific
wrappers for upper bounds \(2\) and \(3\).  These lemmas use
\(\texttt{LinearMap.finrank\_range\_le}\) on
\(\texttt{linProductLeftMapOn}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The next product-dimension target is the symmetric square component
\(\operatorname{Sym}^2 A\), which needs a finite spanning argument rather than
just a single linear-map range bound.

No Julia numerical claims were used in this round.

\section*{Named support ambient subspace}

\paragraph{Gap targeted.}
The lower-rank incidence proofs use ambient subspaces built from a fixed
linear product component \(xA\) and a symmetric-square component
\(\operatorname{Sym}^2 A\).  These were not yet named in Lean, so later
applications had to work directly with a repeated supremum expression.

\paragraph{Lean progress.}
Added
\[
\texttt{supportAmbient x A}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}, defined as
\[
\operatorname{range}(\texttt{linProductLeftMapOn x A})\ \sqcup\
\texttt{symSquareSubmodule A}.
\]
Also added inclusion lemmas for both summands and dimension-bound wrappers:
\[
\texttt{finrank\_supportAmbient\_le\_of\_bounds},
\]
\[
\texttt{finrank\_supportAmbient\_le\_five},\quad
\texttt{finrank\_supportAmbient\_le\_nine}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining ambient-dimension input is an upper bound on
\(\texttt{symSquareSubmodule A}\), especially \(\le3\) for \(\dim A\le2\) and
\(\le6\) for \(\dim A\le3\).

No Julia numerical claims were used in this round.

\section*{Support ambient inclusion lemmas}

\paragraph{Gap targeted.}
The preimage support-data constructors require product inclusions of the form
\(\texttt{linProductSubmodule M A <= W}\).  When \(W\) is the named support
ambient \(xA+\operatorname{Sym}^2A\), these inclusions should follow
automatically from \(M\le A\).

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{linProductSubmodule\_leftPreimageWithin\_le\_symSquare}
\]
and
\[
\texttt{linProductSubmodule\_leftPreimageWithin\_le\_supportAmbient}.
\]
Also added
\[
\texttt{supportAmbient\_le\_ker\_of\_range\_and\_symSquare},
\]
which proves the whole ambient lies in the catalecticant kernel when both
summands do.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining ambient construction work is to prove the symmetric-square
summand's kernel inclusion and dimension bound from the support decomposition.

No Julia numerical claims were used in this round.

\section*{Rank-two support-ambient data wrapper}

\paragraph{Gap targeted.}
After naming \(\texttt{supportAmbient x A}\), the rank-two support-data
constructor still required explicit range and product inclusions.  Those
inclusions are automatic from the definition of the ambient.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_supportAmbient}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It specializes the rank-two
ambient-bound constructor to
\[
U=\texttt{supportAmbient x A},
\]
deriving the required range inclusion and the
\(\texttt{linProductSubmodule}\) inclusion from the ambient's two summands.
The caller supplies the kernel inclusion, the lower bound \(\dim U\ge5\), the
upper bounds on the fixed-linear range and symmetric-square summand, and the
negative square.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two branch still needs the concrete support theorem and binary
negative-square transport to produce \(x,A\), the kernel inclusion for
\(\texttt{supportAmbient x A}\), and the required dimension bounds.

No Julia numerical claims were used in this round.

\section*{Support ambient kernel inclusion from annihilator}

\paragraph{Gap targeted.}
The rank-two support-ambient wrapper still required a proof that
\(\texttt{supportAmbient x A}\) lies in the catalecticant kernel.  In the
blueprint this follows from \(AE\subseteq K\): both \(xA\) and
\(\operatorname{Sym}^2A\) contain a factor from \(A\).

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{range\_linProductLeftMapOn\_le\_ker\_of\_le\_linearAnnihilator},
\]
\[
\texttt{symSquareSubmodule\_le\_ker\_of\_le\_linearAnnihilator},
\]
and
\[
\texttt{supportAmbient\_le\_ker\_of\_le\_linearAnnihilator}.
\]
These prove the ambient kernel inclusion directly from
\(A\le\texttt{linearAnnihilator B p u}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
For the rank-two support-ambient wrapper, the kernel inclusion can now be
discharged automatically from support data.  The remaining inputs are the
ambient lower bound, symmetric-square dimension bound, and negative square.

No Julia numerical claims were used in this round.

\section*{Rank-two support ambient from annihilator inclusion}

\paragraph{Gap targeted.}
The rank-two support-ambient constructor previously required the caller to
prove \(\texttt{supportAmbient x A}\subseteq K\).  After proving that this
follows from \(A\le\operatorname{Ann}_1\), the rank-two wrapper could be
simplified.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_supportAmbient\_of\_annihilator}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It consumes
\(A\le\texttt{linearAnnihilator B p u}\) and automatically supplies the ambient
kernel inclusion using
\[
\texttt{supportAmbient\_le\_ker\_of\_le\_linearAnnihilator}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two case is now reduced to producing the support subspace \(A\), a
negative pure square \(x^2\), and the two ambient dimension bounds.

No Julia numerical claims were used in this round.

\section*{Rank-two support ambient with range dimension derived}

\paragraph{Gap targeted.}
The rank-two support-ambient wrapper still required a separate upper bound on
the fixed-linear product range \(xA\).  This bound follows from
\(\dim A\le2\).

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_supportAmbient\_of\_annihilator\_dim}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It derives
\[
\dim(\operatorname{range}(\texttt{linProductLeftMapOn x A}))\le2
\]
from \(\dim A\le2\), using
\(\texttt{finrank\_range\_linProductLeftMapOn\_le\_two}\).  The remaining
rank-two ambient dimension input is now the symmetric-square bound
\(\dim\operatorname{Sym}^2 A\le3\).

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The key finite-dimensional product-space gap for rank two is now precisely the
formal symmetric-square dimension bound for a two-dimensional support subspace.

No Julia numerical claims were used in this round.

\section*{Rank-two symmetric-square dimension bound}

\paragraph{Gap targeted.}
The rank-two support ambient requires the bound
\(\dim\operatorname{Sym}^2 A\le3\) when \(\dim A=2\).  This was the main
finite-dimensional product-space input still missing from the rank-two
ambient construction.

\paragraph{Lean progress.}
Added a bilinear map
\[
\texttt{linProductBilinOn}
\]
and proved
\[
\texttt{finrank\_symSquareSubmodule\_le\_three\_of\_finrank\_eq\_two}
\]
in \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  The proof chooses a
two-element basis of \(A\), spans \(\operatorname{Sym}^2 A\) by the three
products \(e_0e_0,e_0e_1,e_1e_1\), and uses commutativity to handle the
ordered product \(e_1e_0\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two ambient dimension upper bound can now be derived from
\(\dim A=2\).  The rank-two branch still needs the support theorem to provide
such an \(A\), plus binary transport of a negative pure square.

No Julia numerical claims were used in this round.

\section*{Rank-two support wrapper using \texorpdfstring{\(\dim A=2\)}{dim A=2}}

\paragraph{Gap targeted.}
After proving the symmetric-square bound for two-dimensional support
subspaces, the rank-two support-data wrapper no longer needs callers to pass
that bound separately.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_supportAmbient\_of\_annihilator\_finrank\_eq}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It consumes
\(\dim A=2\) and derives both the fixed-linear range bound and the
\(\operatorname{Sym}^2A\) bound internally, using
\[
\texttt{finrank\_symSquareSubmodule\_le\_three\_of\_finrank\_eq\_two}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two branch now needs the support theorem to provide a
two-dimensional annihilator subspace and the binary theorem to provide the
negative pure square and ambient lower bound.

No Julia numerical claims were used in this round.

\section*{Rank-one symmetric-square dimension bound}

\paragraph{Gap targeted.}
The rank-one incidence proof needs the bound
\(\dim\operatorname{Sym}^2 A\le6\) when the support complement has dimension
three.  This is the three-dimensional analogue of the previous rank-two
symmetric-square bound.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_symSquareSubmodule\_le\_six\_of\_finrank\_eq\_three}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  The proof spans
\(\operatorname{Sym}^2 A\) by the six unordered products of a three-element
basis and uses commutativity to cover the reversed ordered products.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The lower-rank ambient dimension bounds now have formal symmetric-square
dimension inputs for dimensions two and three.  The remaining rank-one/rank-two
work is to connect those ambient spaces to the exact binary/support normal
forms.

No Julia numerical claims were used in this round.

\section*{Rank-one support ambient upper bound}

\paragraph{Gap targeted.}
The rank-one normal-form branch uses the ambient \(xA+\operatorname{Sym}^2 A\)
inside a nine-dimensional catalecticant kernel.  After proving
\(\dim\operatorname{Sym}^2 A\le6\) for \(\dim A=3\), the corresponding ambient
upper bound should be packaged.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_supportAmbient\_le\_nine\_of\_finrank\_eq\_three}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  It combines the
fixed-linear range bound \(\dim(xA)\le3\) with the symmetric-square bound
\(\dim\operatorname{Sym}^2 A\le6\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one branch still needs its exact normal-form decomposition and
negative-square transport.  The ambient dimension bookkeeping is now available
for the natural \(xA+\operatorname{Sym}^2A\) space.

No Julia numerical claims were used in this round.

\section*{Exact-dimensional subspace extraction}

\paragraph{Gap targeted.}
The low-rank support theorem naturally gives lower bounds such as
\(\dim A\ge2\) or \(\dim A\ge3\).  The rank-one and rank-two normal-form
arguments often need to pass to a subspace of exact dimension two or three.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_submodule\_le\_finrank\_eq\_of\_le}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}.  From
\(n\le\dim s\), it constructs a submodule \(t\le s\) with \(\dim t=n\), by
choosing \(n\) linearly independent vectors in \(s\) and taking their span.
Also added the specialized wrappers for dimensions two and three.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Dimension.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
This supports replacing lower-bound annihilator data by exact-dimensional
support subspaces, but the full apolar support theorem and binary transport are
still required to instantiate the rank cases.

No Julia numerical claims were used in this round.

\section*{Exact-dimensional annihilator extraction}

\paragraph{Gap targeted.}
Rank-one and rank-two normal-form arguments need exact-dimensional
annihilator subspaces, while the low-rank support predicate gives only lower
bounds.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Support.lean}:
\[
\texttt{exists\_rank\_one\_exact\_annihilator},\quad
\texttt{exists\_rank\_two\_exact\_annihilator},\quad
\texttt{exists\_rank\_three\_exact\_annihilator}.
\]
These lemmas use the exact-subspace extraction from
\texttt{QuaternaryQuarticProof/Dimension.lean} to choose annihilator subspaces
of dimensions \(3,2,1\), respectively.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The lower-rank cases can now use exact-dimensional annihilator subspaces once
the low-rank apolar support theorem supplies the support predicate.

No Julia numerical claims were used in this round.

\section*{Rank-two exact support ambient extraction}

\paragraph{Gap targeted.}
The rank-two branch needs an exact two-dimensional annihilator subspace and
the routine ambient facts for \(xA+\operatorname{Sym}^2 A\): kernel inclusion
and dimension at most five.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_supportAmbient\_le\_five\_of\_finrank\_eq\_two}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  It combines the
fixed-linear range bound with the two-dimensional symmetric-square bound.

Added
\[
\texttt{exists\_rank\_two\_exact\_annihilator\_supportAmbient}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  From
\(\texttt{HasLinearAnnihilatorCodimAtMost}\) at rank two and a chosen \(x\), it
extracts an annihilator subspace \(A\) with \(\dim A=2\), proves
\(\texttt{supportAmbient x A}\subseteq K\), and proves
\(\dim(\texttt{supportAmbient x A})\le5\).

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two case now mainly needs the binary negative-square transport and
the lower bound \(\dim(\texttt{supportAmbient x A})\ge5\) for the selected
support data.

No Julia numerical claims were used in this round.

\section*{Rank-one symmetric-square ambient extraction}

\paragraph{Gap targeted.}
The rank-one branch uses the second-stage ambient
\(\operatorname{Sym}^2 A\), not the full \(xA+\operatorname{Sym}^2A\).  Once
the support predicate gives a large annihilator, the proof needs an exact
three-dimensional \(A\), kernel inclusion for \(\operatorname{Sym}^2A\), and
the dimension upper bound \(\le6\).

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_rank\_one\_exact\_annihilator\_symSquare}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It extracts
\(\dim A=3\) from rank-one support data and packages
\[
\texttt{symSquareSubmodule A <= ker(catalecticantMap B p u)}
\]
together with
\[
\dim(\texttt{symSquareSubmodule A})\le6.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
followed by
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one branch still needs the matching lower bound/equality for
\(\operatorname{Sym}^2A\), and the normal-form transport that supplies the
negative pure square.

No Julia numerical claims were used in this round.

\section*{Exact annihilator complements}

\paragraph{Gap targeted.}
The rank-one and rank-two normal-form arguments use a direct-sum support
decomposition \(E=A\oplus W\), with exact dimensions for both sides.  Previous
rounds extracted exact-dimensional annihilator subspaces \(A\), but did not
package a complementary \(W\).

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_rank\_one\_exact\_annihilator\_complement}
\]
and
\[
\texttt{exists\_rank\_two\_exact\_annihilator\_complement}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  They extract \(A\le
\operatorname{Ann}_1\), choose a complement \(W\), and prove the exact
dimension pairs \((\dim A,\dim W)=(3,1)\) and \((2,2)\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The support decomposition is now available in exact-dimensional form; the
remaining lower-rank work is the binary negative-square transport and the
stronger product-space lower/equality statements needed for the selected
ambient subspaces.

No Julia numerical claims were used in this round.

\section*{Rank-two support predicate to preimage data}

\paragraph{Gap targeted.}
The rank-two path had all the pieces to go from an exact two-dimensional
annihilator to \(\texttt{HasPreimageProductSupportData}\), but callers still
had to choose that exact annihilator manually from the support predicate.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_support}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It consumes
\(\texttt{HasLinearAnnihilatorCodimAtMost}\) at rank two, chooses an exact
two-dimensional annihilator subspace internally, and invokes the rank-two
support-ambient constructor.  The remaining caller obligations are now the
negative pure square, the nonzero condition on its linear factor, and the
ambient lower bound \(\dim(\texttt{supportAmbient x A})\ge5\) for exact
two-dimensional annihilator subspaces.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
This reduces the rank-two case to binary negative-square transport plus the
support-ambient lower bound for the selected pure-square direction.

No Julia numerical claims were used in this round.

\section*{Rank-two support to negative certificate}

\paragraph{Gap targeted.}
The rank-two support bridge produced
\(\texttt{HasPreimageProductSupportData}\), but the rank-case reducer consumes
an actual negative syzygy certificate.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_rank\_two\_support}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It composes the rank-two
support-data constructor with
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimageProductSupportData}.
\]
Thus the rank-two branch can now go directly from the support predicate, a
negative pure square \(x^2\), the nonzero condition on \(x\), and the remaining
ambient lower bound to the negative certificate required by the SOCP
contradiction.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two certificate branch is now structurally packaged.  The unresolved
inputs are the binary negative-square theorem and the ambient lower bound for
the selected support direction.

No Julia numerical claims were used in this round.

\section*{Rank-one ambient bounds to negative certificate}

\paragraph{Gap targeted.}
The rank-one incidence interface produced
\(\texttt{HasPreimageProductSupportData}\) from ambient bounds, but the
rank-case reducer needs a negative syzygy certificate.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_rank\_one\_ambient\_bounds}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It composes the existing
rank-one ambient-bound constructor with
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_preimageProductSupportData}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one branch still needs its normal-form transport to provide the
ambient \(U\), range/product inclusions, and negative pure square.  Once those
are available, this lemma returns the certificate directly.

No Julia numerical claims were used in this round.

\section*{Finite exponent-set instances}

\paragraph{Gap targeted.}
To prove that linear products span the full quadratic space, the proof needs
finite case analysis over degree-bounded exponent sets.  The cardinality
arguments already used equivalences with bounded triples, but the exponent
subtypes themselves did not have explicit local \(\texttt{Fintype}\)
instances.

\paragraph{Lean progress.}
Added private \(\texttt{Fintype}\) instances for
\[
\texttt{LinExponentSet},\quad \texttt{QuadExponentSet},\quad
\texttt{QuarticExponentSet}
\]
in \texttt{QuaternaryQuarticProof/Catalecticant.lean}, reusing the existing
equivalences with bounded finite triples.  This is preparatory infrastructure
for a future monomial-level proof that every degree-\(\le2\) monomial is a
product of degree-\(\le1\) monomials.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The actual monomial decomposition theorem still needs an explicit construction
rather than relying on opaque finite reduction.

No Julia numerical claims were used in this round.

\section*{Complement vectors from exact annihilators}

\paragraph{Gap targeted.}
The rank-one and rank-two support reductions had exact annihilator complements,
but later binary-normal-form arguments need an actual nonzero linear form in
the complementary support direction, together with a proof that it is not in
the annihilator slice.

\paragraph{Lean progress.}
Added general complement-vector lemmas to
\texttt{QuaternaryQuarticProof/Dimension.lean}: a positive-finrank submodule
contains a nonzero vector, and a nonzero vector lying in one side of an
\(\texttt{IsCompl}\) pair cannot lie in the other side.  Added support-specific
extractors
\[
\texttt{exists\_rank\_one\_exact\_annihilator\_complement\_vector}
\]
and
\[
\texttt{exists\_rank\_two\_exact\_annihilator\_complement\_vector}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  These refine the existing
exact complement lemmas by choosing \(x \in W\) with \((x:\texttt{Poly})\ne0\)
and \(x\notin A\), preserving the exact annihilator and complement dimensions.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The largest remaining mathematical gap is still the binary normal-form
transport: from the chosen complement direction, the proof must construct the
negative pure square and the rank-specific ambient lower-bound hypotheses used
by the certificate lemmas.

No Julia numerical claims were used in this round.

\section*{Rank-two ambient lower-bound criterion}

\paragraph{Gap targeted.}
The rank-two certificate constructor still required the lower bound
\[
5 \le \dim(\texttt{supportAmbient}\ x\ A),
\]
which is the formal version of the blueprint's direct-sum calculation
\(\dim(xA\oplus\Sym^2 A)=2+3\).  The previous interface left this as a
monolithic assumption.

\paragraph{Lean progress.}
Added \(\texttt{finrank\_supportAmbient\_eq\_add\_of\_disjoint}\) and the
rank-two specialization
\[
\texttt{five\_le\_finrank\_supportAmbient\_of\_rank\_two\_components}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  The latter proves the
needed lower bound from three component facts: \(x\ne0\), \(\dim A=2\), exact
\(\dim\Sym^2 A=3\), and
\[
\texttt{range (linProductLeftMapOn x A) }\cap\texttt{ symSquareSubmodule A}
=0.
\]
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_supportAmbient\_of\_components}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, so the rank-two branch can
now consume these component facts directly instead of an opaque ambient lower
bound.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
For rank two, the ambient-dimension gap is reduced to two algebraic component
facts: exact dimension of \(\Sym^2 A\) for a two-dimensional linear subspace
and disjointness of \(xA\) from \(\Sym^2A\) when \(x\notin A\).  The binary
normal-form transport must still supply the negative pure square.

No Julia numerical claims were used in this round.

\section*{Exact symmetric-square dimension from product independence}

\paragraph{Gap targeted.}
The rank-two ambient criterion reduced the lower bound to exact
\(\dim\Sym^2 A=3\) and a disjointness statement.  The exact symmetric-square
dimension should follow once the three products of a basis of \(A\) are known
to be linearly independent.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_symSquareSubmodule\_eq\_three\_of\_basis\_products\_linearIndependent}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  For a basis
\(\beta:\texttt{Fin 2}\to A\), it proves
\[
\dim(\texttt{symSquareSubmodule}\ A)=3
\]
from linear independence of
\[
\beta_0^2,\qquad \beta_0\beta_1,\qquad \beta_1^2.
\]
The proof combines the existing upper bound with a lower bound from the span
of these three independent generators inside \(\texttt{symSquareSubmodule}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The exact \(\Sym^2\) dimension gap is now concentrated in the polynomial
linear-independence theorem for products of two independent affine-linear
forms.  Rank two still also needs the disjointness of \(xA\) from
\(\Sym^2 A\), and the binary normal-form transport still needs to construct a
negative pure square.

No Julia numerical claims were used in this round.

\section*{Rank-one product-support certificate interface}

\paragraph{Gap targeted.}
The blueprint's rank-one case uses two incidence spaces,
\[
M=\{a\in A: xa\in L\},\qquad N=L\cap\Sym^2A,
\]
then finds a nonzero element of \(N\cap MA\).  This is not the same shape as
the rank-two preimage interface, so the formal proof needed a rank-one-specific
support-data bridge.

\paragraph{Lean progress.}
Added \(\texttt{HasRankOneProductSupportData}\) and
\[
\texttt{exists\_negative\_syzygyCertificate\_of\_rank\_one\_productSupportData}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  The new theorem proves the
negative syzygy certificate from the rank-one dimension package:
\(\dim N\ge4\), \(\dim A\ge3\), \(\dim\Sym^2A=6\), \(M\ne0\), and
\(MA\subseteq\Sym^2A\).  It uses the existing Grassmann/product intersection
lemma to choose \(0\ne s\in N\cap MA\), then feeds that product decomposition
to \(\texttt{hasSyzygyCertificate\_of\_mem\_linProductSubmodule}\).

Added \(\texttt{HasRefinedRankCaseSupportData}\) and its reducers to
\texttt{QuaternaryQuarticProof/Basic.lean}.  This root-level bridge uses the
new rank-one product data, the existing rank-two preimage data, and the
existing rank-three annihilator data to prove residual vanishing.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
For rank one, the formal certificate step is now available once the normal
form supplies the subspaces \(A,M,N\), the dimension bounds
\(\dim N\ge4\), \(\dim\Sym^2A=6\), and the negative pure square.  The remaining
work is the normal-form transport and the exact polynomial dimension facts.

No Julia numerical claims were used in this round.

\section*{Rank-one product data from annihilator normal form}

\paragraph{Gap targeted.}
After adding the rank-one product-support certificate interface, the next gap
was constructing its \(M\) and \(N\) spaces from the normal-form components.
The blueprint obtains \(M\ne0\) from \(L\cap xA\) inside the rank-one kernel
and obtains \(\dim N\ge4\) from \(L\cap\Sym^2A\).

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankOneProductSupportData\_of\_annihilator\_symSquare}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  Assuming rank one, an
annihilator subspace \(A\), \(\dim A=3\), exact
\(\dim\Sym^2A=6\), and a negative pure square for \(x\), it constructs
\(\texttt{HasRankOneProductSupportData}\).  The proof sets
\[
M=\texttt{linProductLeftPreimageWithin}\ x\ A\ L,\qquad
N=L\cap\Sym^2A,
\]
uses the rank-one kernel dimension \(9\) to prove \(M\ne0\), and uses the
existing Grassmann lemma
\(\texttt{four\_le\_finrank\_spanUQuad\_inf\_of\_rank\_one\_ambient}\) to prove
\(\dim N\ge4\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one case is now reduced to the normal-form obligations: choose the
three-dimensional annihilator/support subspace \(A\), prove
\(\dim\Sym^2A=6\), and produce \(x\) with \(\lambda(x^4)<0\).

No Julia numerical claims were used in this round.

\section*{Exact three-dimensional symmetric square}

\paragraph{Gap targeted.}
The rank-one product-data constructor needs exact
\(\dim\Sym^2A=6\) for a three-dimensional subspace \(A\).  As in the rank-two
case, this should follow from independence of the unordered products of a
basis.

\paragraph{Lean progress.}
Added
\[
\texttt{finrank\_symSquareSubmodule\_eq\_six\_of\_basis\_products\_linearIndependent}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  For a basis
\(\beta:\texttt{Fin 3}\to A\), it proves exact dimension \(6\) from linear
independence of the six products \(\beta_i\beta_j\) with \(i\le j\).  The proof
matches the rank-two exact-dimension lemma: the six independent products give
a six-dimensional subspace of \(\Sym^2A\), while the existing spanning lemma
gives the upper bound.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Both exact symmetric-square dimension requirements are now reduced to
polynomial independence of basis products.  The remaining rank-one and
rank-two work is to prove those independence facts, prove the rank-two
disjointness statement, and transport the binary negative-square normal forms.

No Julia numerical claims were used in this round.

\section*{Binary low-rank normal-form interface}

\paragraph{Gap targeted.}
The binary negative-square proof had individual Lean lemmas for each normal
form, but the later transport step needs one reusable interface that says the
restricted binary quartic is in one of the blueprint's low-rank normal forms.

\paragraph{Lean progress.}
Added \(\texttt{HasBinaryLowRankNegativeNormalForm}\) and
\[
\texttt{binaryQuarticEval\_exists\_negative\_of\_lowRankNegativeNormalForm}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  The normal-form predicate has
four cases: rank-one outer product with negative scalar, \(xy\)-kernel
diagonal form with an indefinite quadratic witness, \(y^2\)-kernel form with
nonzero linear coefficient, and elliptic \(x^2+y^2\)-kernel form.  The theorem
dispatches each case to the existing case-specific binary negativity lemma.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The binary branch is now packaged as a single target.  The unresolved transport
step is to derive \(\texttt{HasBinaryLowRankNegativeNormalForm}\) for the
quartic functional obtained by restricting the catalecticant to the chosen
two-dimensional support.

No Julia numerical claims were used in this round.

\section*{Transporting binary negativity to a support-plane square}

\paragraph{Gap targeted.}
After packaging the binary normal forms, the next bridge is turning a negative
binary value at coordinates \((X,Y)\) into a quaternary linear form
\(z=Xx+Yy\) whose pure square is negative under the residual functional.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_negative\_pure\_square\_of\_binaryLowRankNormalForm}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  Given
\(\texttt{HasBinaryLowRankNegativeNormalForm}\) and an explicit evaluation
identity identifying the quaternary residual value on
\((Xx+Yy)^4\) with \(\texttt{binaryQuarticEval}\), it produces
\[
z\in\operatorname{span}\{x,y\}
\quad\text{with}\quad
\lambda(z^4)<0.
\]
This isolates the remaining transport work to proving the coefficient
evaluation identity for the chosen support-plane basis.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The binary-to-quaternary negativity bridge is now available once the restricted
functional coefficients are related to
\(\texttt{binaryQuarticEval}\).  The proof still needs the actual restriction
normal-form theorem and the polynomial dimension/disjointness facts for the
rank-one and rank-two support spaces.

No Julia numerical claims were used in this round.

\section*{Rank-two binary normal form to support data}

\paragraph{Gap targeted.}
The rank-two branch had two separated pieces: the binary normal-form theorem
could produce a negative support-plane square, while the rank-two support
constructor needed a concrete negative direction plus exact component data for
\(\texttt{supportAmbient}\).

\paragraph{Lean progress.}
Changed \texttt{QuaternaryQuarticProof/Support.lean} to import the binary
interface and added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_binaryNormalForm}.
\]
This theorem composes
\[
\texttt{exists\_negative\_pure\_square\_of\_binaryLowRankNormalForm}
\]
with
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_supportAmbient\_of\_components}.
\]
It also proves the selected support direction is nonzero from the strict
negative value.  The remaining assumptions are now precisely the transport
inputs: the restricted binary normal form, the coefficient-evaluation identity,
exact \(\dim\Sym^2 A=3\), and disjointness of \(zA\) from \(\Sym^2A\) for
nonzero support-plane \(z\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two certificate path is now wired from binary normal form to support
data.  What remains is proving the normal form/evaluation identity and the
geometric component facts for the chosen complement.

No Julia numerical claims were used in this round.

\section*{Rank-one binary normal form to product data}

\paragraph{Gap targeted.}
The rank-one product-data constructor still expected a concrete linear form
\(x\) with negative pure square.  The binary interface can produce such a
linear form from a low-rank normal form plus the coefficient-evaluation
identity.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankOneProductSupportData\_of\_binaryNormalForm}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It composes
\(\texttt{exists\_negative\_pure\_square\_of\_binaryLowRankNormalForm}\) with
\(\texttt{hasRankOneProductSupportData\_of\_annihilator\_symSquare}\).  As in
the rank-two bridge, the selected support-plane direction is proved nonzero
from the strict negative value.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one and rank-two branches now both consume the same binary
normal-form/evaluation interface.  The remaining proof work is concentrated in
deriving that interface from the catalecticant rank cases and proving the
polynomial component facts for \(\Sym^2 A\) and the rank-two direct sum.

No Julia numerical claims were used in this round.

\section*{Rank-case reducer for binary normal-form data}

\paragraph{Gap targeted.}
The proof had reducers from abstract support data and from negative
certificates, but the largest remaining constructive work is now specifically
the rank-one/rank-two binary normal-form transport.  The root proof needed a
stable interface at exactly that level.

\paragraph{Lean progress.}
Added \(\texttt{HasRankCaseBinaryNormalFormData}\) to
\texttt{QuaternaryQuarticProof/Basic.lean}.  Its rank-one branch packages an
annihilator subspace \(A\), exact \(\dim A=3\), exact \(\dim\Sym^2A=6\), a
binary low-rank negative normal form, and the coefficient-evaluation identity.
Its rank-two branch packages the analogous \(\dim A=2\), exact
\(\dim\Sym^2A=3\), binary normal form, evaluation identity, and the direct-sum
disjointness condition for nonzero support-plane directions.  The rank-three
branch reuses \(\texttt{HasRankThreeAnnihilatorSupportData}\).

Added reducers
\[
\texttt{hasRankCaseNegativeCertificateFamily\_of\_binaryNormalFormData},
\quad
\texttt{residual\_eq\_zero\_of\_rankCaseBinaryNormalFormData},
\]
and
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_rankCaseBinaryNormalFormData}.
\]
These compose the new binary-data interface with the already formalized
rank-one/rank-two support bridges and the rank-three certificate bridge.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The global theorem is now reducible to proving
\(\texttt{HasRankCaseBinaryNormalFormData}\) for every SOCP instance.  That is
the current largest gap: deriving the binary normal forms, coefficient
evaluation identities, exact symmetric-square dimensions, and rank-two
disjointness from the catalecticant support structure.

No Julia numerical claims were used in this round.

\section*{Binary restriction coefficient evaluation}

\paragraph{Gap targeted.}
The binary normal-form rank-case data still required an opaque
coefficient-evaluation identity.  This identity should come from expanding
\((Xx+Yy)^4\) and applying linearity of the residual functional.

\paragraph{Lean progress.}
Added binary restriction coefficient definitions
\[
\texttt{binaryRestrictionCoeffA},\ldots,
\texttt{binaryRestrictionCoeffE}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  These are the five coefficients
\(\lambda(x^4),\lambda(x^3y),\lambda(x^2y^2),\lambda(xy^3),\lambda(y^4)\)
for the plane spanned by \(x,y\).

Added
\[
\texttt{binaryRestriction\_eval\_eq\_of\_pow\_expansion}.
\]
It proves the required \(\texttt{binaryQuarticEval}\) identity from the
polynomial expansion of \((Xx+Yy)^4\).  Thus the remaining evaluation-identity
work is reduced to a pure polynomial expansion lemma, separated from bilinear
functional linearity.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The coefficient-evaluation gap has been split: bilinear evaluation is done,
and the remaining subgoal is a standalone \(\texttt{MvPolynomial}\) expansion
for \((Xx+Yy)^4\).  The normal-form classification and the exact
dimension/disjointness facts remain open.

No Julia numerical claims were used in this round.

\section*{Canonical binary restriction support constructors}

\paragraph{Gap targeted.}
The rank-one and rank-two binary-normal-form constructors still accepted an
arbitrary five-coefficient evaluation identity.  After defining canonical
restriction coefficients, this can be reduced to a normal form for those
coefficients plus the pure polynomial expansion of \((Xx+Yy)^4\).

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_binaryRestriction}
\]
and
\[
\texttt{hasRankOneProductSupportData\_of\_binaryRestriction}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  These constructors take
\(\texttt{HasBinaryLowRankNegativeNormalForm}\) for the canonical coefficients
\(\texttt{binaryRestrictionCoeffA},\ldots,\texttt{binaryRestrictionCoeffE}\)
and use
\(\texttt{binaryRestriction\_eval\_eq\_of\_pow\_expansion}\) to discharge the
evaluation identity required by the previous binary-normal-form bridges.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one/rank-two binary transport now has canonical coefficients.  The
remaining evaluation obligation is only the polynomial expansion of
\((Xx+Yy)^4\), while the normal-form classification must be proved for those
canonical coefficients.

No Julia numerical claims were used in this round.

\section*{Polynomial expansion for binary restriction}

\paragraph{Gap targeted.}
The binary coefficient evaluation bridge still needs the algebraic expansion
of \((Xx+Yy)^4\).  Direct scalar-smul notation caused coercion friction, but
the same identity stated with \(\texttt{MvPolynomial.C}\) coefficients
normalizes cleanly.

\paragraph{Lean progress.}
Added
\[
\texttt{binaryRestriction\_pow\_eq\_C}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  It proves the explicit
polynomial identity
\[
(Xx+Yy)^4
=X^4x^4+4X^3Yx^3y+6X^2Y^2x^2y^2+4XY^3xy^3+Y^4y^4
\]
in the \(\texttt{MvPolynomial.C}\)-coefficient form accepted by
\(\texttt{ring\_nf}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The pure polynomial expansion is now formalized.  The next evaluation step is
to bridge this \(\texttt{MvPolynomial.C}\)-coefficient form to the scalar-smul
form consumed by
\(\texttt{binaryRestriction\_eval\_eq\_of\_pow\_expansion}\), or to add a
parallel evaluation lemma for the \(\texttt{MvPolynomial.C}\)-coefficient
statement.

No Julia numerical claims were used in this round.

\section*{Residual evaluation scalar lemmas}

\paragraph{Gap targeted.}
The \(\texttt{MvPolynomial.C}\)-coefficient expansion leaves scalar factors
inside the first argument of the bilinear residual functional.  The next
evaluation bridge needs reusable facts that expose those factors as real
scalars outside \(B\).

\paragraph{Lean progress.}
Added
\[
\texttt{residualEval\_C\_mul},\quad
\texttt{residualEval\_mul\_four},\quad
\texttt{residualEval\_mul\_six}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  The first lemma proves
\[
B((\texttt{MvPolynomial.C}\ r)q,s)=rB(q,s),
\]
and the numeric lemmas handle the polynomial numeral factors \(4\) and \(6\)
that appear in the binomial expansion.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining coefficient-evaluation task is now mostly rearrangement: use
these scalar-extraction lemmas on the five terms in
\(\texttt{binaryRestriction\_pow\_eq\_C}\) to derive the canonical
\(\texttt{binaryQuarticEval}\) identity without an external expansion
hypothesis.

No Julia numerical claims were used in this round.

\section*{Canonical binary evaluation and rank-case reducer}

\paragraph{Gap targeted.}
The binary-restriction bridge still carried an explicit evaluation identity.
After the polynomial expansion and scalar-extraction lemmas, this identity can
be proved directly for the canonical binary restriction coefficients.

\paragraph{Lean progress.}
Added
\[
\texttt{residualEval\_C\_mul\_mul\_four},\quad
\texttt{residualEval\_C\_mul\_mul\_six},
\quad
\texttt{binaryRestriction\_eval\_eq}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  The final theorem proves
directly that evaluating the residual functional on \((Xx+Yy)^4\) is exactly
\(\texttt{binaryQuarticEval}\) with the canonical coefficients
\(\texttt{binaryRestrictionCoeffA},\ldots,\texttt{binaryRestrictionCoeffE}\).

Added canonical-coefficient support constructors
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_binaryRestriction\_coefficients}
\]
and
\[
\texttt{hasRankOneProductSupportData\_of\_binaryRestriction\_coefficients}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  These no longer require an
external evaluation identity.

Added \(\texttt{HasRankCaseBinaryRestrictionData}\) and corresponding reducers
to \texttt{QuaternaryQuarticProof/Basic.lean}.  The global reduction now asks
for rank-one/rank-two normal forms of the canonical binary restriction
coefficients, exact symmetric-square dimensions, rank-two disjointness, and
the existing rank-three support data.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The binary evaluation identity is closed.  The largest remaining gap is now
the genuine normal-form/support theorem: derive the canonical binary
restriction normal forms and the exact component dimension/disjointness facts
from the rank-one and rank-two catalecticant cases.

No Julia numerical claims were used in this round.

\section*{Complement-based binary restriction component data}

\paragraph{Gap targeted.}
After closing the canonical binary evaluation identity, the remaining rank-one
and rank-two branches should be stated in terms of the actual complement
geometry supplied by the apolar support theorem: an exact annihilator subspace,
its complement, normal forms on complement directions, and the remaining
component dimension/disjointness facts.

\paragraph{Lean progress.}
Added \(\texttt{HasRankOneBinaryRestrictionComponentData}\) and
\(\texttt{HasRankTwoBinaryRestrictionComponentData}\) to
\texttt{QuaternaryQuarticProof/Support.lean}.  These package exact
annihilator-complement data together with canonical binary restriction normal
forms.  The rank-two package states disjointness for all nonzero directions in
the complement \(W\); the bridge proves the span-based disjointness needed by
the existing rank-two constructor.

Added
\[
\texttt{hasRankOneProductSupportData\_of\_binaryRestrictionComponentData}
\]
and
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_binaryRestrictionComponentData}.
\]
Added the root-level
\(\texttt{HasRankCaseBinaryRestrictionComponentData}\) and reducers in
\texttt{QuaternaryQuarticProof/Basic.lean}.  The global proof is now reducible
to proving complement-based binary restriction component data for ranks one
and two, plus the existing rank-three support data.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The largest remaining gap is now sharply localized: prove the complement-based
rank-one/rank-two component data from the catalecticant rank cases.  This
includes exact \(\Sym^2\) dimensions, rank-two direct-sum disjointness, and the
binary low-rank normal-form classification of the canonical restriction
coefficients.

No Julia numerical claims were used in this round.

\section*{Component data from apolar support complements}

\paragraph{Gap targeted.}
The complement-based binary restriction data still began with exact complement
data as an assumption.  The apolar support interface already extracts exact
annihilator complements from
\(\texttt{HasLinearAnnihilatorCodimAtMost}\); the normal-form theorem should
only have to supply the remaining algebraic facts after that extraction.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_rank\_one\_binaryRestrictionComponentData\_of\_support}
\]
and
\[
\texttt{exists\_rank\_two\_binaryRestrictionComponentData\_of\_support}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  These use the existing exact
annihilator-complement vector extractors for ranks one and two, then invoke a
rank-specific component hypothesis only for the remaining choices and facts:
the second support-plane direction, exact \(\Sym^2\) dimension, binary normal
form of the canonical coefficients, and rank-two disjointness.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The support-complement extraction is now connected to the component-data
target.  The remaining proof work is the rank-specific algebra inside the
chosen complement: exact product dimensions, direct-sum disjointness, and
binary low-rank normal-form classification.

No Julia numerical claims were used in this round.

\section*{Named apolar component-data reducer}

\paragraph{Gap targeted.}
The previous support-complement constructors used large anonymous hypotheses
for the remaining rank-one and rank-two algebra.  This made the next theorem
target hard to read and reuse.

\paragraph{Lean progress.}
Added named predicates
\[
\texttt{HasRankOneSupportComponentHypothesis},\qquad
\texttt{HasRankTwoSupportComponentHypothesis}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, and rewrote the
support-to-component constructors to consume these predicates.

Added \(\texttt{HasRankCaseApolarComponentData}\) and the reducers
\[
\texttt{hasRankCaseNegativeCertificateFamily\_of\_apolarComponentData},
\quad
\texttt{residual\_eq\_zero\_of\_rankCaseApolarComponentData},
\]
and
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_rankCaseApolarComponentData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  This reducer now starts from
rank-one/rank-two apolar support bounds plus the named component hypotheses,
then invokes the exact complement extraction and the canonical binary
restriction bridges.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The remaining proof target is now the apolar component data itself: derive
\(\texttt{HasLinearAnnihilatorCodimAtMost}\) in the rank-one and rank-two cases
and prove the named support component hypotheses, plus rank-three support data.

No Julia numerical claims were used in this round.

\section*{Rank-one self-direction normal form}

\paragraph{Gap targeted.}
The rank-one support component hypothesis still asked for a full binary
low-rank normal form.  In a one-dimensional support complement, the same
linear form can be used for both binary coordinates, so a negative pure fourth
power already gives a rank-one binary normal form.

\paragraph{Lean progress.}
Added
\[
\texttt{rankOneSelf\_binaryLowRankNegativeNormalForm}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  If
\(\lambda(x^4)<0\), then the binary restriction with coordinates \((x,x)\)
has the rank-one outer-product normal form with negative scalar.

Added
\[
\texttt{hasRankOneSupportComponentHypothesis\_of\_self\_negative}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  This reduces the rank-one
support component hypothesis to two smaller facts for every extracted
one-dimensional complement direction \(x\): exact
\(\dim\Sym^2A=6\), and negativity of \(\lambda(x^4)\).

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Binary}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one binary normal-form obligation is now reduced to a negative
pure-square theorem for the complement direction and exact symmetric-square
dimension.  Rank two still needs the full binary classification and
direct-sum disjointness.

No Julia numerical claims were used in this round.

\section*{Named binary normal-form constructors}

\paragraph{Gap targeted.}
The rank-two classification still needs to prove that the canonical binary
restriction lies in one of the low-rank normal forms.  The target disjunction
was usable but awkward to construct directly in later proofs.

\paragraph{Lean progress.}
Added named constructors to \texttt{QuaternaryQuarticProof/Binary.lean}:
\[
\texttt{HasBinaryLowRankNegativeNormalForm.rankOne},
\quad
\texttt{HasBinaryLowRankNegativeNormalForm.xyKernel},
\]
\[
\texttt{HasBinaryLowRankNegativeNormalForm.ySqKernel},
\quad
\texttt{HasBinaryLowRankNegativeNormalForm.ellipticKernel}.
\]
These correspond exactly to the four cases in the blueprint: rank-one outer
product, \(xy\)-kernel diagonal, \(y^2\)-kernel, and elliptic
\(x^2+y^2\)-kernel.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Future rank-two classification work can now target one of these named
constructors after deriving the corresponding coefficient equalities and
nondegeneracy/negativity facts from the catalecticant kernel.

No Julia numerical claims were used in this round.

\section*{Choosing a second rank-two complement direction}

\paragraph{Gap targeted.}
The rank-two binary normal-form classification needs a genuine two-dimensional
support plane.  After extracting a nonzero \(x\in W\) from the complement, the
proof still needs a second direction in \(W\) outside the line spanned by \(x\).

\paragraph{Lean progress.}
Added general dimension lemmas to \texttt{QuaternaryQuarticProof/Dimension.lean}:
\[
\texttt{exists\_mem\_notMem\_of\_finrank\_lt}
\]
and
\[
\texttt{exists\_mem\_notMem\_span\_singleton\_of\_finrank\_two}.
\]
The first extracts a vector in a larger subspace but outside a smaller one;
the second specializes this to a two-dimensional subspace and a nonzero vector.

Added the support-level wrapper
\[
\texttt{exists\_rank\_two\_complement\_second\_direction}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, specialized to
\(\texttt{linSubmodule}\).  This will let the rank-two component theorem choose
a second binary coordinate \(y\in W\) with \(y\notin\mathbb{R}x\).

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the only
remaining expected failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Rank two still needs the coefficient normal-form classification and the
direct-sum disjointness proof, but the basic choice of an independent second
support direction is now formalized.

No Julia numerical claims were used in this round.

\section*{Reducing rank-two support components to independent binary data}

\paragraph{Gap targeted.}
The previous round supplied the dimension-theoretic choice of a second
rank-two support direction \(y\in W\setminus \mathbb{R}x\), but the main
component hypothesis still treated the resulting \(y\) as part of one opaque
assumption.  The next useful reduction is to separate this into the actual
mathematical obligations: exact \(\Sym^2 A\) dimension, binary normal form for
an independent pair \((x,y)\), and product-disjointness for every nonzero
direction in \(W\).

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_independent\_binary\_data}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It proves
\[
\texttt{HasRankTwoSupportComponentHypothesis B p u}
\]
from three reusable hypotheses:
\begin{enumerate}
\item \(\dim \Sym^2 A=3\) for every exact rank-two annihilator complement;
\item the canonical binary restriction coefficients have
  \(\texttt{HasBinaryLowRankNegativeNormalForm}\) whenever
  \(y\in W\setminus \mathbb{R}x\);
\item every nonzero \(z\in W\) has
  \(zA\cap \Sym^2A=\{0\}\).
\end{enumerate}
The proof invokes
\(\texttt{exists\_rank\_two\_complement\_second\_direction}\), so the
independent second coordinate is now part of the formal support pipeline.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two case is now reduced to proving the three separated component
facts above from the apolar Hilbert-function/support analysis.  In particular,
the binary normal-form classification and the product-disjointness proof remain
the largest rank-two blockers.

No Julia numerical claims were used in this round.

\section*{Recording dimension of an independent binary support plane}

\paragraph{Gap targeted.}
The rank-two support component now chooses \(y\in W\setminus\mathbb{R}x\).
The binary normal-form and support-plane arguments also need the standard fact
that this pair spans a two-dimensional plane.  This should be available as a
small reusable dimension lemma rather than reproved inside later coefficient
classification code.

\paragraph{Lean progress.}
Added generic lemmas to \texttt{QuaternaryQuarticProof/Dimension.lean}:
\[
\texttt{linearIndependent\_pair\_of\_notMem\_span\_singleton}
\]
and
\[
\texttt{finrank\_span\_pair\_eq\_two\_of\_notMem\_span\_singleton}.
\]
They turn \(x\ne0\) and \(y\notin Kx\) into linear independence of
\((x,y)\), then into
\[
\dim_K\operatorname{span}\{x,y\}=2.
\]
Added the support-specialized wrapper
\[
\texttt{finrank\_span\_rank\_two\_support\_pair\_eq\_two}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, converting the existing
\(\texttt{(x : Poly) \(\ne\) 0}\) hypothesis into subtype nonzeroness.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the known
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two component proof still needs the apolar argument that produces the
normal-form coefficient alternatives and the disjointness
\(zA\cap\Sym^2A=0\).  The independent binary plane can now be passed to those
future lemmas as a concrete two-dimensional span.

No Julia numerical claims were used in this round.

\section*{Naming binary kernel-equation normal-form bridges}

\paragraph{Gap targeted.}
The rank-two binary classification in the blueprint splits according to a
kernel vector in \(\Sym^2W\): \(xy\), \(y^2\), or \(x^2+y^2\).  The Lean
normal-form predicate already had constructors for the resulting coefficient
families, but later apolar code needs named bridges from the actual coefficient
equations to those constructors.

\paragraph{Lean progress.}
Added three theorem wrappers to \texttt{QuaternaryQuarticProof/Binary.lean}:
\[
\texttt{HasBinaryLowRankNegativeNormalForm.of\_xyKernel\_equations},
\]
\[
\texttt{HasBinaryLowRankNegativeNormalForm.of\_ySqKernel\_equations},
\]
and
\[
\texttt{HasBinaryLowRankNegativeNormalForm.of\_ellipticKernel\_equations}.
\]
They convert the coefficient equations
\[
b=c=d=0,\qquad c=d=e=0,\qquad c=-a,\ d=-b,\ e=a
\]
respectively into
\(\texttt{HasBinaryLowRankNegativeNormalForm}\), with the corresponding
negativity or nonzero side condition required by the blueprint.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure remains the root placeholder axiom.

\paragraph{Remaining blockers.}
The missing binary step is now shifted to proving that the rank-two
catalecticant restriction actually has one of these kernel-equation patterns
after a suitable binary basis choice, plus the side conditions required in the
three cases.

No Julia numerical claims were used in this round.

\section*{Specializing binary normal-form bridges to restriction coefficients}

\paragraph{Gap targeted.}
The support component predicates use the canonical coefficients
\(\texttt{binaryRestrictionCoeffA}\) through
\(\texttt{binaryRestrictionCoeffE}\), not abstract scalars
\((a,b,c,d,e)\).  The previous kernel-equation bridges therefore still
required a manual specialization step in every later use.

\paragraph{Lean progress.}
Added the canonical coefficient wrappers
\[
\texttt{binaryRestriction\_lowRankNegativeNormalForm\_of\_xyKernel\_equations},
\]
\[
\texttt{binaryRestriction\_lowRankNegativeNormalForm\_of\_ySqKernel\_equations},
\]
and
\[
\texttt{binaryRestriction\_lowRankNegativeNormalForm\_of\_ellipticKernel\_equations}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}.  These produce the exact
\(\texttt{HasBinaryLowRankNegativeNormalForm}\) term required by
\(\texttt{HasRankTwoBinaryRestrictionComponentData}\) from coefficient
equations on the binary restriction.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Binary.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is unchanged, namely the root placeholder axiom.

\paragraph{Remaining blockers.}
The normal-form field can now be discharged directly once the apolar
rank-two restriction supplies one of the three coefficient-equation cases.
The proof still needs that case split and the geometric disjointness fact.

No Julia numerical claims were used in this round.

\section*{Packaging rank-two binary kernel-equation cases}

\paragraph{Gap targeted.}
The rank-two apolar proof should not need to build
\(\texttt{HasBinaryLowRankNegativeNormalForm}\) directly.  Its natural output
is a case split on the binary catalecticant kernel equations.  The formal
pipeline needed a single predicate for those alternatives and a support-level
constructor consuming it.

\paragraph{Lean progress.}
Added
\[
\texttt{HasBinaryRestrictionKernelEquationCase}
\]
to \texttt{QuaternaryQuarticProof/Binary.lean}, bundling the three canonical
coefficient alternatives corresponding to the \(xy\), \(y^2\), and
\(x^2+y^2\) kernel-vector cases.  Added
\[
\texttt{binaryRestriction\_lowRankNegativeNormalForm\_of\_kernelEquationCase}
\]
to eliminate this package into the exact normal-form predicate.

Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_independent\_binary\_cases}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It combines the existing
independent second-coordinate choice with exact \(\Sym^2A\) dimension,
kernel-equation cases, and product disjointness to produce
\(\texttt{HasRankTwoSupportComponentHypothesis}\).

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Binary}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two branch now asks for the apolar proof to supply three concrete
facts: exact \(\Sym^2A\) dimension, the packaged kernel-equation case, and
\(zA\cap\Sym^2A=0\) for nonzero \(z\in W\).  The coefficient normal-form
plumbing is no longer a separate blocker.

No Julia numerical claims were used in this round.

\section*{Identifying the chosen binary pair with the rank-two support plane}

\paragraph{Gap targeted.}
After choosing \(y\in W\setminus\mathbb{R}x\), later rank-two arguments need
to freely replace the abstract complement plane \(W\) by the binary coordinate
plane \(\operatorname{span}\{x,y\}\).  The proof needed this equality as a
separate dimension lemma.

\paragraph{Lean progress.}
Added the generic theorem
\[
\texttt{span\_pair\_eq\_of\_mem\_of\_notMem\_span\_singleton\_of\_finrank\_two}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}.  It proves that if
\(x,y\in W\), \(x\ne0\), \(y\notin Kx\), and \(\dim W=2\), then
\[
\operatorname{span}\{x,y\}=W.
\]
Added the support-level wrapper
\[
\texttt{span\_rank\_two\_support\_pair\_eq}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, again translating the
existing \(\texttt{(x : Poly) \(\ne\) 0}\) hypothesis into subtype
nonzeroness.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two proof can now move cleanly from the selected binary coordinate
pair to the whole complement plane.  The hard remaining facts are still the
apolar kernel-equation case split and the product-disjointness theorem.

No Julia numerical claims were used in this round.

\section*{Identifying a rank-one complement from a nonzero support vector}

\paragraph{Gap targeted.}
The rank-one normal-form branch uses a one-dimensional support complement.
Once a nonzero \(x\in W\) is extracted, later code should be able to replace
\(W\) by the line \(\mathbb{R}x\) without repeating a dimension argument.

\paragraph{Lean progress.}
Added
\[
\texttt{span\_singleton\_eq\_of\_mem\_of\_finrank\_one}
\]
to \texttt{QuaternaryQuarticProof/Dimension.lean}.  It proves that if
\(x\ne0\), \(x\in W\), and \(\dim W=1\), then \(Kx=W\).  Added the
quaternary support wrapper
\[
\texttt{span\_rank\_one\_support\_vector\_eq}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the known
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-one support branch can now identify the complement line from the
extracted vector.  It still needs the apolar normal-form proof giving exact
\(\dim\Sym^2A=6\) and the negative pure-square coefficient.

No Julia numerical claims were used in this round.

\section*{Assembling apolar rank-case data from explicit component obligations}

\paragraph{Gap targeted.}
The root proof path is blocked at
\(\texttt{HasRankCaseApolarComponentData}\).  Prior rounds reduced the
rank-one and rank-two component hypotheses, but \texttt{Basic.lean} still did
not expose a single theorem saying exactly which apolar facts remain to prove
for each catalecticant rank.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  It constructs
\[
\texttt{HasRankCaseApolarComponentData B p u hu}
\]
from the following rank-indexed obligations:
\begin{enumerate}
\item rank one: \(\dim\operatorname{range}(\texttt{linearAnnihilatorMap})\le1\),
  exact \(\dim\Sym^2A=6\), and negative pure-square coefficient;
\item rank two: \(\dim\operatorname{range}(\texttt{linearAnnihilatorMap})\le2\),
  exact \(\dim\Sym^2A=3\), the packaged binary kernel-equation case, and
  product-disjointness;
\item rank three: the existing rank-three annihilator support data.
\end{enumerate}
The theorem uses the existing support constructors, so it does not introduce a
new proof target or weaken the final statement.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The largest gap is now sharply localized: prove the listed apolar component
obligations from the low-rank catalecticant/Hilbert-function argument in the
blueprint.

No Julia numerical claims were used in this round.

\section*{Reducing the rank-three apolar branch to explicit negative support}

\paragraph{Gap targeted.}
The previous apolar assembler still accepted the rank-three branch as opaque
\(\texttt{HasRankThreeAnnihilatorSupportData}\).  In the blueprint, rank three
comes from an annihilator direction plus an explicitly negative quadratic
supported on the remaining variables, so this should be exposed in Lean.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_component\_obligations\_with\_rank\_three\_support}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  It extends the previous
assembler by replacing the opaque rank-three support-data input with:
\begin{enumerate}
\item \(\dim\operatorname{range}(\texttt{linearAnnihilatorMap})\le3\);
\item an explicit \(q\in\texttt{linProductSubmodule W W}\) with
  \(B(q^2,\texttt{residual p u})<0\).
\end{enumerate}
The proof constructs
\(\texttt{HasLinearAnnihilatorCodimAtMost B p u 3}\) from the annihilator-map
bound and then invokes the existing rank-three support constructor.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
All three rank branches are now exposed as concrete component obligations.
The largest remaining mathematical gap is proving those obligations from the
low-rank catalecticant support theorem and SOS negativity argument.

No Julia numerical claims were used in this round.

\section*{Separating positive catalecticant rank from residual zero}

\paragraph{Gap targeted.}
Several remaining apolar obligations, especially the negative-square inputs in
rank one, rank two, and rank three, are only meaningful when the residual is
nonzero.  The rank-case hypotheses provide positive catalecticant rank, so the
formal development needs a direct bridge from positive catalecticant rank to
\(\texttt{residual p u \(\ne\) 0}\).

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{catalecticantMap\_eq\_zero\_of\_residual\_eq\_zero},
\]
\[
\texttt{finrank\_range\_catalecticantMap\_eq\_zero\_of\_residual\_eq\_zero},
\]
\[
\texttt{residual\_ne\_zero\_of\_catalecticantMap\_rank\_pos},
\]
and the rank-one, rank-two, and rank-three specializations.  These lemmas
show that if the residual vanishes then the catalecticant map is zero and has
range rank zero; hence any rank \(1,2,3\) case implies nonzero residual.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The apolar branches can now obtain nonzero residual directly from their rank
hypotheses.  The main missing work is still deriving the annihilator-map
dimension bounds and support/normal-form facts from the low-rank
catalecticant support theorem.

No Julia numerical claims were used in this round.

\section*{Producing SOS negative summands from positive catalecticant rank}

\paragraph{Gap targeted.}
The remaining apolar branches need negative quadratic input from the SOS
certificate.  After proving that catalecticant rank \(1,2,3\) implies nonzero
residual, the next step is to make the SOS negativity lemma directly available
from each rank hypothesis.

\paragraph{Lean progress.}
Added rank-specific lemmas to
\texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{exists\_negative\_sos\_summand\_of\_catalecticantMap\_rank\_eq\_one},
\]
\[
\texttt{exists\_negative\_sos\_summand\_of\_catalecticantMap\_rank\_eq\_two},
\]
and
\[
\texttt{exists\_negative\_sos\_summand\_of\_catalecticantMap\_rank\_eq\_three}.
\]
Each lemma takes admissibility, SOS feasibility, FOCP, and the corresponding
catalecticant rank equation, then returns a quadratic \(q\) with
\[
B(q^2,\texttt{residual p u})<0.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
The negative input for each positive-rank branch is now available without a
separate residual assumption.  The remaining hard work is to transport this
negative quadratic through the low-rank apolar support decomposition to obtain
the rank-one pure square, rank-two binary kernel-equation case, and rank-three
supported negative quadratic.

No Julia numerical claims were used in this round.

\section*{Specializing catalecticant quotient dimensions by rank}

\paragraph{Gap targeted.}
The low-rank apolar support theorem works in the quotient of quadratics by the
catalecticant kernel.  The generic quotient/range rank equivalence was already
formalized, but later rank-one, rank-two, and rank-three proofs should not
need to repeat the conversion from catalecticant rank equations.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{finrank\_quotient\_ker\_catalecticantMap\_eq\_one\_of\_rank\_one},
\]
\[
\texttt{finrank\_quotient\_ker\_catalecticantMap\_eq\_two\_of\_rank\_two},
\]
and
\[
\texttt{finrank\_quotient\_ker\_catalecticantMap\_eq\_three\_of\_rank\_three}.
\]
These specialize
\(\texttt{finrank\_quotient\_ker\_catalecticantMap\_eq\_rank}\) to the three
rank cases used in the blueprint.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The quotient dimension bookkeeping is now ready for the apolar support proof.
The remaining nontrivial gap is still the finite-dimensional/inverse-system
argument forcing the annihilator and support decompositions.

No Julia numerical claims were used in this round.

\section*{Equating annihilator codimension and annihilator-map rank bounds}

\paragraph{Gap targeted.}
The low-rank support theorem in the blueprint can be used either as a large
linear annihilator subspace or as a rank bound for the multiplication map out
of affine-linear forms.  Lean already had the direction from the map rank bound
to \(\texttt{HasLinearAnnihilatorCodimAtMost}\); later structural lemmas also
need the reverse direction.

\paragraph{Lean progress.}
Added
\[
\texttt{annihilatorMap\_range\_le\_of\_hasLinearAnnihilatorCodimAtMost}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}, plus rank-one, rank-two, and
rank-three specializations.  The proof uses the rank-nullity identity
\[
\dim\operatorname{range}(\texttt{linearAnnihilatorMap})
+\dim\texttt{linearAnnihilator}=4
\]
and monotonicity of finrank for a subspace contained in the linear
annihilator.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The apolar support reduction can now be phrased in either of the two equivalent
forms used by the blueprint.  The remaining hard step is still proving such a
support reduction from low catalecticant rank.

No Julia numerical claims were used in this round.

\section*{Reducing rank-two disjointness to the selected binary plane}

\paragraph{Gap targeted.}
The rank-two component constructor still required product-disjointness for
every nonzero \(z\in W\), while the binary normal-form proof naturally works
inside the chosen coordinate plane \(\operatorname{span}\{x,y\}\).  Since the
previous rounds proved that an independent \(x,y\in W\) span \(W\), this
disjointness obligation can be stated on the binary plane instead.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_independent\_binary\_plane\_cases}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It chooses
\(y\in W\setminus\mathbb{R}x\), identifies
\[
\operatorname{span}\{x,y\}=W
\]
using \(\texttt{span\_rank\_two\_support\_pair\_eq}\), and then converts a
disjointness hypothesis over the binary span into the original
\(\texttt{HasRankTwoSupportComponentHypothesis}\) field.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two branch can now be proved using binary-plane data only: exact
\(\dim\Sym^2A\), a packaged kernel-equation case, and disjointness on
\(\operatorname{span}\{x,y\}\).  The difficult remaining part is deriving
those binary-plane facts from the apolar support theorem.

No Julia numerical claims were used in this round.

\section*{Threading binary-plane disjointness into apolar rank-case assembly}

\paragraph{Gap targeted.}
After reducing rank-two disjointness to the selected binary plane in
\texttt{Support.lean}, the rank-case apolar assembler in \texttt{Basic.lean}
still exposed the older \(z\in W\) obligation.  The top-level remaining
obligations should match the reduced binary-plane form.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_binary\_plane\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  It is the same rank-case
apolar assembler as before, except the rank-two disjointness input is now
stated for
\[
z\in\operatorname{span}\{x,y\}
\]
after choosing \(y\in W\setminus\mathbb{R}x\).  Internally it invokes
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_independent\_binary\_plane\_cases}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The top-level rank-two apolar obligation is now aligned with the binary
classification geometry.  The main missing theorem is still the structural
low-rank apolar support proof that supplies the exact dimensions,
kernel-equation cases, and binary-plane disjointness.

No Julia numerical claims were used in this round.

\section*{Reducing rank-two product disjointness to product linear independence}

\paragraph{Gap targeted.}
The rank-two branch still needs
\[
zA\cap\Sym^2A=0.
\]
Rather than keep this as a primitive assumption, it can be reduced to a
standard linear-independence statement for the combined family of mixed
products \(z a_i\) and symmetric products \(a_i a_j\) in a basis of \(A\).

\paragraph{Lean progress.}
Added general linear-algebra lemmas to
\texttt{QuaternaryQuarticProof/Dimension.lean}:
\[
\texttt{disjoint\_span\_ranges\_of\_linearIndependent\_sum}
\]
and
\[
\texttt{inf\_span\_ranges\_eq\_bot\_of\_linearIndependent\_sum}.
\]
They use \(\texttt{linearIndependent\_sum}\) to conclude that the spans of the
left and right summands of a linearly independent \(\texttt{Sum.elim}\) family
are disjoint.

Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{range\_linProductLeftMapOn\_eq\_span\_basis\_products}
\]
and
\[
\texttt{range\_linProductLeftMapOn\_inf\_symSquare\_eq\_bot\_of\_basis\_products\_linearIndependent}.
\]
The latter proves the rank-two product-disjointness goal from linear
independence of
\[
\{z a_i\}_{i=1}^2\cup\{a_i a_j: i\le j\}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Dimension}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The product-disjointness obligation is now reduced to a concrete product
linear-independence statement.  The remaining structural work is to prove that
linear independence from the direct-sum decomposition \(E=A\oplus W\), and to
derive the binary kernel-equation cases.

No Julia numerical claims were used in this round.

\section*{Threading product linear independence into rank-two support}

\paragraph{Gap targeted.}
The previous round reduced \(zA\cap\Sym^2A=0\) to linear independence of a
combined product family, but the rank-two support constructor still expected a
disjointness equality.  The support-level proof should accept the more
concrete product-linear-independence obligation directly.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_independent\_binary\_plane\_cases\_and\_productLI}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It takes the rank-two
binary-plane component data plus, for each relevant nonzero
\(z\in\operatorname{span}\{x,y\}\), a basis \(\beta\) of \(A\) such that the
family
\[
\{z\beta_i\}_{i=1}^2
\cup
\{\beta_i\beta_j:i\le j\}
\]
is linearly independent.  It converts this into the required disjointness using
\[
\texttt{range\_linProductLeftMapOn\_inf\_symSquare\_eq\_bot\_of\_basis\_products\_linearIndependent}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two support branch can now be supplied by product linear independence
rather than an abstract intersection proof.  The remaining structural task is
to prove that independence from \(E=A\oplus W\), and to derive the binary
kernel-equation alternatives.

No Julia numerical claims were used in this round.

\section*{Threading product linear independence into apolar assembly}

\paragraph{Gap targeted.}
The rank-two support constructor now accepts product linear independence, but
the top-level apolar rank-case assembler still exposed the older binary-plane
disjointness equality.  The remaining obligations should be stated in the most
concrete form available.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_productLI\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  Its rank-two branch asks for a
basis \(\beta\) of \(A\) such that the combined family
\[
\{z\beta_i\}_{i=1}^2
\cup
\{\beta_i\beta_j:i\le j\}
\]
is linearly independent for each relevant nonzero
\(z\in\operatorname{span}\{x,y\}\).  The theorem converts this to the
binary-plane disjointness input using the Catalecticant lemma from the previous
round.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two disjointness subgoal has been replaced by a concrete product
linear-independence statement.  Remaining structural work: prove that product
linear independence from the direct-sum decomposition, prove exact
\(\Sym^2A\) dimensions, and derive binary kernel-equation alternatives.

No Julia numerical claims were used in this round.

\section*{Reducing exact symmetric-square dimensions to product independence}

\paragraph{Gap targeted.}
The rank-one and rank-two component hypotheses still assumed exact dimensions
\(\dim\Sym^2A=6\) and \(\dim\Sym^2A=3\).  Existing Catalecticant lemmas prove
these exact dimensions from linear independence of the symmetric basis-product
families, which is the concrete algebraic fact expected from a direct-sum
coordinate argument.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Support.lean}:
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_basis\_product\_independence}
\]
and
\[
\texttt{hasRankOneSupportComponentHypothesis\_of\_basis\_product\_independence}.
\]
The rank-two constructor uses a basis \(\beta:\texttt{Fin 2}\to A\) and
linear independence of
\[
\{\beta_i\beta_j:i\le j\}
\]
to obtain \(\dim\Sym^2A=3\).  The rank-one constructor uses the analogous
\(\texttt{Fin 3}\) family to obtain \(\dim\Sym^2A=6\).  Both then invoke the
previous support constructors.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The exact symmetric-square dimension obligations are now reduced to concrete
basis-product linear independence.  Remaining structural work: prove these
linear-independence facts from \(E=A\oplus W\), prove the rank-two combined
product independence, and derive binary kernel-equation alternatives.

No Julia numerical claims were used in this round.

\section*{Threading symmetric product independence into apolar assembly}

\paragraph{Gap targeted.}
After reducing exact \(\Sym^2A\) dimensions in \texttt{Support.lean}, the
top-level apolar assembler in \texttt{Basic.lean} still exposed those exact
dimension equalities directly.  The remaining obligations should instead be
the concrete basis-product linear-independence facts.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_product\_independence\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  It replaces:
\[
\dim\Sym^2A=6,\qquad \dim\Sym^2A=3
\]
by linear independence of the symmetric product families for bases of the
rank-one and rank-two annihilator subspaces.  It also keeps the previous
rank-two combined product-linear-independence obligation for disjointness.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The main apolar-data target now asks for product linear independence,
rank-two binary kernel-equation alternatives, annihilator-map bounds, and
rank-three negative support.  The difficult missing proof is the structural
low-rank apolar theorem that supplies these from catalecticant rank.

No Julia numerical claims were used in this round.

\section*{Deriving rank-two symmetric-square dimension from combined product independence}

\paragraph{Gap targeted.}
The rank-two product-linear-independence hypothesis already includes the
symmetric products \(\beta_i\beta_j\).  Therefore rank two should not need a
separate exact \(\dim\Sym^2A=3\) or symmetric-product-independence input.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_binary\_cases\_and\_productLI}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  After choosing
\(y\in W\setminus\mathbb{R}x\), the proof applies the product-LI hypothesis at
\(z=x\).  The right half of the resulting \(\texttt{Sum.elim}\) linear
independence gives linear independence of the symmetric product family, hence
\[
\dim\Sym^2A=3.
\]
The same product-LI hypothesis supplies the disjointness field for arbitrary
nonzero \(z\in W\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-two support branch now needs only binary kernel-equation cases and
combined product linear independence.  The separate rank-two
\(\Sym^2A\)-dimension obligation has been removed.

No Julia numerical claims were used in this round.

\section*{Removing the separate rank-two symmetric-square obligation upstairs}

\paragraph{Gap targeted.}
The support-level rank-two constructor now derives \(\dim\Sym^2A=3\) from the
combined product-linear-independence hypothesis.  The top-level apolar
assembler should therefore no longer ask for a separate rank-two symmetric
product independence hypothesis.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_rank\_two\_productLI\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  Compared with the previous
product-independence assembler, the rank-two inputs are reduced to:
\begin{enumerate}
\item the annihilator-map rank bound;
\item the binary kernel-equation case;
\item the combined product-linear-independence family.
\end{enumerate}
The exact \(\Sym^2A\) dimension is derived internally by
\(\texttt{hasRankTwoSupportComponentHypothesis\_of\_binary\_cases\_and\_productLI}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Rank two now has only two structural obligations beyond the annihilator-map
bound: binary kernel equations and combined product linear independence.  Rank
one still separately needs symmetric product independence and the negative
pure-square coefficient.

No Julia numerical claims were used in this round.

\section*{Packaging reduced apolar data as a final proof interface}

\paragraph{Gap targeted.}
The latest reductions left the main remaining apolar work spread across many
arguments: annihilator-map bounds, rank-one product independence and
negativity, rank-two binary kernel equations and product independence, and
rank-three negative support.  This made the remaining gap less explicit at the
final theorem interface.

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankCaseProductIndependenceApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  This packages the currently
reduced rank-one, rank-two, and rank-three apolar obligations into one
rank-case data interface.  Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_productIndependenceApolarData},
\]
\[
\texttt{residual\_eq\_zero\_of\_productIndependenceApolarData},
\]
and
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_productIndependenceApolarData}.
\]
These wire the package through the existing certificate pipeline all the way
to the immutable target statement, without changing the final theorem
declaration.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is still the root placeholder axiom.

\paragraph{Remaining blockers.}
It is now enough to prove
\(\texttt{HasRankCaseProductIndependenceApolarData}\) for every positive
definite SOS SOCP instance.  That is exactly the structural low-rank apolar
support theorem plus binary kernel classification and rank-three negative
support transport.

No Julia numerical claims were used in this round.

\section*{Reducing rank-one symmetric-square data to combined product independence}

\paragraph{Gap targeted.}
Rank one still separately required symmetric product independence for
\(\Sym^2A\).  In the rank-one normal form, the natural product decomposition is
\[
xA\oplus\Sym^2A,
\]
so a single combined product-linear-independence statement should provide the
\(\Sym^2A\) dimension as a subfamily consequence.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankOneSupportComponentHypothesis\_of\_combined\_product\_independence}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It takes linear independence
of the combined family
\[
\{x\beta_i\}_{i=1}^3
\cup
\{\beta_i\beta_j:i\le j\}
\]
and derives the symmetric-product independence by projecting to the right half
of the \(\texttt{Sum.elim}\) family.

Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_combined\_productLI\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}, so the top-level rank-one and
rank-two branches can both be supplied using combined product-linear
independence.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Both rank-one and rank-two product-geometry obligations are now stated as
combined product-linear-independence facts.  The remaining hard step is proving
those facts, plus binary kernel equations and negative support transport, from
the low-rank apolar support theorem.

No Julia numerical claims were used in this round.

\section*{Tightening the product-independence apolar data package}

\paragraph{Gap targeted.}
After adding a rank-one constructor from combined product independence, the
packaged data interface still used the older rank-one symmetric-product
independence input.  The package should expose the strongest reduced form:
combined product independence in both rank one and rank two.

\paragraph{Lean progress.}
Updated
\[
\texttt{HasRankCaseProductIndependenceApolarData}
\]
in \texttt{QuaternaryQuarticProof/Basic.lean}.  Its rank-one branch now asks
for linear independence of
\[
\{x\beta_i\}_{i=1}^3\cup\{\beta_i\beta_j:i\le j\}
\]
rather than only the symmetric product subfamily.  The bridge theorem
\[
\texttt{hasRankCaseApolarComponentData\_of\_productIndependenceApolarData}
\]
now routes through
\[
\texttt{hasRankCaseApolarComponentData\_of\_combined\_productLI\_component\_obligations}.
\]

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The reduced apolar package now mirrors the direct-sum product decompositions in
the blueprint: rank one uses \(xA\oplus\Sym^2A\), and rank two uses the binary
plane together with \(zA\oplus\Sym^2A\).  The open structural task is proving
these combined product-independence facts from \(E=A\oplus W\).

No Julia numerical claims were used in this round.

\section*{Dropping catalecticant-kernel components from negative squares}

\paragraph{Gap targeted.}
In the rank-three blueprint, a negative quadratic \(q_0\) is decomposed as
\[
q_0=q_W+q_K
\]
with \(q_K\) in the catalecticant kernel.  The proof then uses
\(\lambda(q_W^2)=\lambda(q_0^2)\).  This kernel-invariance step was not yet
available as a Lean lemma.

\paragraph{Lean progress.}
Added
\[
\texttt{residualEval\_sq\_add\_eq\_of\_mem\_ker\_catalecticantMap}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  For quadratic
submodule elements \(q,k\), if \(k\) lies in
\(\ker(\texttt{catalecticantMap B p u})\), then
\[
B((q+k)^2,\texttt{residual p u})=B(q^2,\texttt{residual p u}).
\]
The proof expands the square and uses that \(k\) pairs to zero with both
\(q\) and \(k\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Rank-three negative support transport now has the algebraic kernel-dropping
step.  It still needs the actual decomposition of an arbitrary negative
quadratic into its supported and kernel components under the low-rank apolar
support theorem.

No Julia numerical claims were used in this round.

\section*{Adding negative-square transfer across kernel decompositions}

\paragraph{Gap targeted.}
The rank-three support argument needs not only equality after dropping a kernel
component, but also the direct inequality consequence: if the original
quadratic square is negative, then the supported component square is negative.

\paragraph{Lean progress.}
Added three follow-up lemmas to
\texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{residualEval\_sq\_lt\_of\_add\_mem\_ker\_catalecticantMap},
\]
\[
\texttt{residualEval\_sq\_eq\_of\_eq\_add\_mem\_ker\_catalecticantMap},
\]
and
\[
\texttt{residualEval\_sq\_lt\_of\_eq\_add\_mem\_ker\_catalecticantMap}.
\]
These package the previous kernel-dropping equality in the forms needed when a
quadratic is explicitly decomposed as \(q=q_W+q_K\) with \(q_K\) in the
catalecticant kernel.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-three branch now has the algebra needed to transfer negativity to the
supported component once the decomposition is supplied.  The remaining missing
piece is constructing that decomposition from the apolar support reduction.

No Julia numerical claims were used in this round.

\section*{Reducing rank-three negative support to kernel decomposition}

\paragraph{Gap targeted.}
Rank three still asked directly for a negative quadratic supported on a
subspace \(W\).  In the blueprint this is obtained by taking a negative SOS
summand and decomposing it into a supported component plus a catalecticant
kernel component.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankThreeAnnihilatorSupportData\_of\_rank\_three\_kernel\_decomposition}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  The theorem assumes
rank three, SOS feasibility, FOCP, the codimension-three annihilator support
bound, and a decomposition principle:
\[
q=q_W+q_K,\qquad q_W\in WW,\qquad q_K\in\ker C_\lambda
\]
for each negative quadratic \(q\).  It obtains a negative SOS summand from the
rank-three catalecticant hypothesis, applies the decomposition, and transfers
negativity to \(q_W\) using the kernel-decomposition lemmas from the previous
round.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\qquad
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
Rank three is now reduced to a precise decomposition theorem for negative
quadratics under the apolar support split.  This is closer to the blueprint’s
\(q_0=q_W+q_K\) step.

No Julia numerical claims were used in this round.

\section*{Packaging rank-three kernel decomposition in the final apolar interface}

\paragraph{Gap targeted.}
The reduced apolar package still asked directly for rank-three negative support,
even though the rank-three proof now reduces that support to decomposing a
negative SOS summand into supported and kernel pieces.

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankCaseKernelDecompositionApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  This package keeps the reduced
rank-one and rank-two combined product-linear-independence obligations, and
replaces the rank-three direct negative-support input by the decomposition
principle
\[
q=q_W+q_K,\qquad q_W\in WW,\qquad q_K\in\ker C_\lambda
\]
for every negative quadratic \(q\).

Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_kernelDecompositionApolarData},
\]
\[
\texttt{residual\_eq\_zero\_of\_kernelDecompositionApolarData},
\]
and
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_kernelDecompositionApolarData}.
\]
Thus proving this kernel-decomposition apolar package for every SOCP instance
is now sufficient to prove the immutable target.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The final formal gap is reduced to proving
\(\texttt{HasRankCaseKernelDecompositionApolarData}\): annihilator-map bounds,
combined product independence in ranks one and two, binary kernel equations in
rank two, and the rank-three kernel decomposition.

No Julia numerical claims were used in this round.

\section*{Direct rank-three kernel-to-factor-span bridge}

\paragraph{Gap targeted.}
In rank three, the blueprint uses \(\dim K=\dim L=7\) and \(L\subseteq K\) to
identify the catalecticant kernel with the span of the seven factor quadratics.
Lean already had the equality and a polynomial-kernel version, but not the
direct statement for a \(\texttt{quadSubmodule}\) element of
\(\ker(\texttt{catalecticantMap})\).

\paragraph{Lean progress.}
Added
\[
\texttt{mem\_spanUQuad\_of\_rank\_three\_of\_mem\_ker\_catalecticantMap}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  It states that under
FOCP, full rank of the relation map, and catalecticant rank three, every
quadratic in \(\ker C_\lambda\) lies in \(\texttt{spanUQuad hu}\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
This removes another small conversion step in the rank-three branch.  The main
remaining work is still the low-rank apolar support/decomposition theorem.

No Julia numerical claims were used in this round.

\section*{Support complement separation wrappers}

\paragraph{Gap targeted.}
The remaining combined product-linear-independence proofs rely on direct-sum
coordinates \(E=A\oplus W\).  A basic recurring fact is that a nonzero support
direction in \(W\) is not in \(A\), and conversely for a nonzero vector in
\(A\).  The generic complement lemmas existed, but the support code uses
nonzeroness after coercion to \(\texttt{Poly}\).

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Support.lean}:
\[
\texttt{not\_mem\_left\_of\_support\_complement\_mem\_ne\_zero}
\]
and
\[
\texttt{not\_mem\_right\_of\_annihilator\_complement\_mem\_ne\_zero}.
\]
These specialize the generic \(\texttt{IsCompl}\) separation lemmas to
\(\texttt{linSubmodule}\), converting \(\texttt{(z : Poly) \(\ne\) 0}\) into
subtype nonzeroness.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The direct-sum separation facts are now available in the form needed for the
remaining product-independence proofs.

No Julia numerical claims were used in this round.

\section*{Exact complement generator packages}

\paragraph{Gap targeted.}
The largest remaining formal gap is still the apolar support theorem producing
rank-case kernel-decomposition data.  Its rank-one and rank-two product
independence subgoals repeatedly need the complement \(W\) of an exact
annihilator \(A\) in coordinates: a generator when \(\dim W=1\), and a spanning
pair when \(\dim W=2\).  Lean had the exact complements and some local span
lemmas, but not packaged witnesses with all hypotheses carried together.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Support.lean}:
\[
\texttt{exists\_rank\_two\_exact\_annihilator\_complement\_pair}
\]
and
\[
\texttt{exists\_rank\_one\_exact\_annihilator\_complement\_generator}.
\]
The rank-two package returns \(A,W,x,y\) with \(A\le\Ann\), \(A\oplus W\),
\(x,y\in W\), nonzeroness after coercion to \(\texttt{Poly}\), \(x,y\notin A\),
\(y\notin\mathbb R x\), \(\Span\{x,y\}=W\), and the exact dimensions
\(\dim A=\dim W=2\).  The rank-one package similarly returns a nonzero
complement generator \(x\) with \(\mathbb R x=W\), \(x\notin A\),
\(\dim A=3\), and \(\dim W=1\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The exact complement witnesses are now in the shape needed to start proving
the direct-sum product-independence facts.  The unproved content remains the
actual product-independence/binary-kernel-equation construction and the
rank-three kernel decomposition for arbitrary SOCP input.

No Julia numerical claims were used in this round.

\section*{Rank-three annihilator generator package}

\paragraph{Gap targeted.}
The rank-three branch of the blueprint needs a nonzero annihilator linear form
and a complementary three-plane before the negative quadratic is decomposed
modulo the catalecticant kernel.  Lean had the exact one-dimensional
annihilator extraction, but not the separated complement package that records
the annihilator generator as a line.

\paragraph{Lean progress.}
Added
\[
\texttt{exists\_rank\_three\_exact\_annihilator\_complement\_generator}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  From
\(\texttt{HasLinearAnnihilatorCodimAtMost}\;3\), it returns
\(A,W,z\) with \(A\le\Ann\), \(A\oplus W\), \(z\in A\),
\((z:\texttt{Poly})\ne0\), \(z\notin W\), \(\mathbb Rz=A\),
\(\dim A=1\), and \(\dim W=3\).

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Support.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
All low-rank exact-complement witness packages now exist in Lean.  The main
remaining unformalized mathematics is the construction of product independence,
rank-two binary kernel equations, and the rank-three kernel decomposition for
arbitrary SOCP data.

No Julia numerical claims were used in this round.

\section*{Kernel decomposition to product-independence bridge}

\paragraph{Gap targeted.}
The proof hierarchy had a direct route from
\(\texttt{HasRankCaseKernelDecompositionApolarData}\) to the apolar component
data, but the earlier product-independence apolar interface was not connected
to the newer kernel-decomposition interface.  The rank-three component of the
newer interface is strictly stronger: it decomposes every negative quadratic
into a supported term plus a catalecticant-kernel term.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseProductIndependenceApolarData\_of\_kernelDecompositionApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  In ranks one and two this is
projection from the packaged data.  In rank three, it uses the existing
negative SOS summand theorem for rank-three catalecticants, applies the
kernel-decomposition hypothesis to that summand, and transfers strict
negativity across the catalecticant-kernel component.  The theorem
\[
\texttt{residual\_eq\_zero\_of\_kernelDecompositionApolarData}
\]
now routes through
\(\texttt{residual\_eq\_zero\_of\_productIndependenceApolarData}\) using this
bridge.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typechecks.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The bridge removes a hierarchy gap but not the main mathematical construction:
it remains to prove \(\texttt{HasRankCaseKernelDecompositionApolarData}\) for
arbitrary positive-definite SOS SOCP input.

No Julia numerical claims were used in this round.

\section*{Affine-linear products span quadratics}

\paragraph{Gap targeted.}
The blueprint repeatedly uses the affine-chart fact
\(\Lin\cdot\Lin=\Quad\): every quadratic polynomial in three affine variables
is a linear combination of products of affine-linear forms.  This is a core
algebraic input for the direct-sum product-independence and rank-three
decomposition arguments.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{map\_subtype\_linProductSubmodule\_top\_top},
\quad
\texttt{bounded\_totalDegree\_two\_eq\_add\_bounded\_totalDegree\_one},
\]
\[
\texttt{linSubmodule\_mul\_linSubmodule\_eq\_quadSubmodule},
\quad
\texttt{map\_subtype\_linProductSubmodule\_top\_top\_eq\_quadSubmodule},
\]
and
\[
\texttt{linProductSubmodule\_top\_top\_eq\_top}.
\]
The proof connects the internal \(\texttt{linProductSubmodule}\) to the
ordinary pointwise product of polynomial submodules, then uses mathlib's
\(\texttt{MvPolynomial.restrictSupport\_add}\) plus a finite
\(\texttt{Finsupp.degree}\) calculation showing that degree \(\le 2\)
exponents are exactly sums of two degree \(\le 1\) exponents.

\paragraph{Verification.}
\[
\texttt{lake env lean QuaternaryQuarticProof/Catalecticant.lean}
\]
and
\[
\texttt{lake env lean QuaternaryQuarticProof/Basic.lean}
\]
typecheck.  The full quaternary harness is run for the commit; the expected
remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The ambient product-spanning fact is now formalized.  The remaining product
independence obligations require the sharper direct-sum versions for
complements \(E=A\oplus W\), not just the ambient equality
\(\Lin\cdot\Lin=\Quad\).

No Julia numerical claims were used in this round.

\section*{Direct-sum product decomposition}

\paragraph{Gap targeted.}
After formalizing \(\Lin\cdot\Lin=\Quad\), the largest remaining rank-three
gap was the decomposition step: for an annihilator subspace \(A\) and
complement \(W\), write an arbitrary quadratic as a supported \(W^2\) term
plus a catalecticant-kernel term.  This is the decomposition required by
\(\texttt{HasRankCaseKernelDecompositionApolarData}\).

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{linProduct\_mem\_sup\_of\_isCompl},
\quad
\texttt{linProductSubmodule\_top\_top\_le\_sup\_of\_isCompl},
\]
\[
\texttt{sup\_linProductSubmodule\_eq\_top\_of\_isCompl},
\quad
\texttt{exists\_linProduct\_decomposition\_of\_isCompl},
\]
and
\[
\texttt{exists\_catalecticantKernel\_decomposition\_of\_annihilator\_complement}.
\]
The proof expands products after decomposing both affine-linear factors through
\(A\oplus W\), so all mixed terms land in \(A\cdot\top\) and the remaining term
lands in \(W^2\).  If \(A\le\Ann\), the \(A\cdot\top\) term lies in the
catalecticant kernel.

Added to \texttt{QuaternaryQuarticProof/Support.lean}:
\[
\texttt{exists\_rank\_three\_catalecticantKernel\_decomposition}
\]
and
\[
\texttt{exists\_rank\_three\_catalecticantKernel\_decomposition\_of\_annihilatorMap\_range}.
\]
These use the exact rank-three annihilator complement package to produce the
required decomposition for every quadratic.

Added to \texttt{QuaternaryQuarticProof/Basic.lean}:
\[
\texttt{hasRankCaseKernelDecompositionApolarData\_of\_productIndependenceApolarData}.
\]
Thus the rank-three kernel-decomposition obligation now follows from the
annihilator-map bound already present in product-independence apolar data.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\]
and
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
complete successfully.  The full quaternary harness is run for the commit; the
expected remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The rank-three decomposition part of the kernel-decomposition interface is no
longer an independent blocker.  The main remaining construction is now the
rank-one/rank-two product-independence and binary kernel-equation data,
together with the apolar annihilator-map bounds.

No Julia numerical claims were used in this round.

\section*{Full affine-linear basis product independence}

\paragraph{Gap targeted.}
The rank-one and rank-two product-independence obligations are direct-sum
versions of a finite-dimensional fact: symmetric products of a basis of
\(\Lin\) form a basis of \(\Quad\).  The previous round proved spanning; this
round adds the corresponding linear independence statement for a
four-element affine-linear basis.

\paragraph{Lean progress.}
Added a global bilinear product map
\[
\texttt{linProductBilin}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  Added:
\[
\texttt{linProductSubmodule\_top\_top\_le\_span\_basis\_products},
\quad
\texttt{span\_basis\_products\_eq\_top},
\]
and
\[
\texttt{linearIndependent\_basis\_products\_fin\_four}.
\]
For any basis \(\beta:\texttt{Fin 4}\to\Lin\), the ten products
\(\beta_i\beta_j\) with \(i\le j\) span \(\Quad\), and since
\(\dim\Quad=10\), they are linearly independent.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
and
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
complete successfully.  The full quaternary harness is run for the commit; the
expected remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The full-space basis product independence theorem is now formalized.  The next
step is transporting it to bases adapted to complements \(A\oplus W\), yielding
the rank-one/rank-two combined product-independence obligations.

No Julia numerical claims were used in this round.

\section*{Adapted complement bases}

\paragraph{Gap targeted.}
The full affine-linear product-independence theorem applies to a
\(\texttt{Fin 4}\)-indexed basis of \(\Lin\).  The remaining rank-one and
rank-two product-independence obligations use bases of an annihilator
subspace \(A\) together with complement directions in \(W\), so the missing
formal step was assembling these data into a full basis of \(\Lin\).

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{exists\_rank\_one\_adapted\_basis}
\]
and
\[
\texttt{exists\_rank\_two\_adapted\_basis}.
\]
The rank-one theorem takes \(A\oplus W\), a \(\texttt{Fin 3}\)-basis of
\(A\), and a generator \(x\) with \(\mathbb R x=W\), and returns a
\(\texttt{Fin 4}\)-basis whose first three vectors are the \(A\)-basis and
whose last vector is \(x\).  The rank-two theorem similarly takes a
\(\texttt{Fin 2}\)-basis of \(A\) and complement directions \(x,y\) with
\(\Span\{x,y\}=W\), and returns a \(\texttt{Fin 4}\)-basis with those four
prescribed vectors.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
and
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
complete successfully.  The full quaternary harness is run for the commit; the
expected remaining failure is the root placeholder axiom.

\paragraph{Remaining blockers.}
The basis-assembly step is now isolated.  The next proof step is extracting
the required rank-one and rank-two product subfamilies from
\(\texttt{linearIndependent\_basis\_products\_fin\_four}\) after rewriting by
these adapted-basis equalities.

No Julia numerical claims were used in this round.

\section*{Rank-one product-independence extraction}

\paragraph{Gap targeted.}
After constructing bases adapted to \(A\oplus W\), the next missing formal
step was to turn full \(\texttt{Fin 4}\)-basis product independence into the
rank-one family used by the support component hypotheses:
\[
\{x\beta_i\}_{i<3}\cup\{\beta_i\beta_j\}_{i\le j<3}.
\]

\paragraph{Lean progress.}
Added
\[
\texttt{linearIndependent\_rank\_one\_combined\_products\_of\_isCompl}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  The proof embeds the
rank-one index family into the ten products of an adapted \(\texttt{Fin 4}\)
basis, proves that embedding injective, and pulls linear independence back
from
\[
\texttt{linearIndependent\_basis\_products\_fin\_four}.
\]
Added
\[
\texttt{exists\_rank\_one\_combined\_product\_independence}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  This packages the extraction
for any rank-one complement \(W\), using
\(\mathbb R x=W\) from the existing one-dimensional span lemma.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
and
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
complete successfully.  The full quaternary verification harness reaches the
final axiom audit; the only remaining extra dependency is still
\[
\texttt{QuaternaryQuartic.quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_placeholder}.
\]

\paragraph{Remaining blockers.}
The rank-one product-independence obligation no longer needs to be carried as
a separate algebraic assumption once a one-dimensional complement generator is
available.  The analogous rank-two extraction remains the next largest
product-independence gap.

No Julia numerical claims were used in this round.

\section*{Rank-two product-independence extraction}

\paragraph{Gap targeted.}
The largest remaining product-independence gap was the rank-two family
\[
\{z\beta_i\}_{i<2}\cup\{\beta_i\beta_j\}_{i\le j<2},
\]
where \(z\) is any nonzero vector in the binary complement plane.

\paragraph{Lean progress.}
Added
\[
\texttt{linearIndependent\_rank\_two\_combined\_products\_of\_isCompl}
\]
to \texttt{QuaternaryQuarticProof/Catalecticant.lean}.  The proof constructs
an adapted \(\texttt{Fin 4}\)-basis whose first two vectors are a basis of
\(A\), whose third vector is \(z\), and whose fourth vector completes the
rank-two complement.  The desired five products are then pulled back from the
ten independent products of the full basis.

Added
\[
\texttt{exists\_rank\_two\_combined\_product\_independence}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  Given \(z\ne0\) in
\(\Span\{x,y\}=W\), it chooses a second direction \(y'\in W\) outside
\(\mathbb R z\), proves \(\Span\{z,y'\}=W\), and applies the Catalecticant
lemma.

Added
\[
\texttt{hasRankCaseApolarComponentData\_of\_product\_free\_component\_obligations}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  This bridge no longer asks for
rank-one or rank-two product-independence assumptions; it supplies them from
the two support-level existence lemmas.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Catalecticant}
\]
and
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
complete successfully.  The full quaternary verification harness again reaches
the final axiom audit; the only extra dependency remains the root placeholder
axiom.

\paragraph{Remaining blockers.}
The product-independence layer has been discharged for the rank-one and
rank-two component obligations.  The remaining non-placeholder mathematical
inputs are the rank-one negativity, rank-two binary kernel-equation cases, and
the rank-three negative/decomposition data that feed the product-free bridge.

No Julia numerical claims were used in this round.

\section*{Product-free apolar interface}

\paragraph{Gap targeted.}
After the rank-one and rank-two product-independence proofs, the old apolar
data interface still exposed those product-independence facts as assumptions.
The formal statement of the remaining gap therefore needed to be tightened.

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankCaseProductFreeApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  This package keeps only the
remaining rank-case inputs: the annihilator-map bounds, rank-one negativity,
rank-two binary kernel-equation cases, and rank-three negative product data.
Added the bridges
\[
\texttt{hasRankCaseApolarComponentData\_of\_productFreeApolarData},
\quad
\texttt{residual\_eq\_zero\_of\_productFreeApolarData},
\]
and
\[
\texttt{quaternaryQuartic\_rankSeven\_no\_spurious\_socp\_of\_productFreeApolarData}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The top-level scaffold can now target the product-free rank-case data rather
than the older product-independence package.  The remaining proof work is to
derive those rank-one, rank-two, and rank-three inputs from the SOCP and SOS
hypotheses.

No Julia numerical claims were used in this round.

\section*{Kernel decomposition through product-free data}

\paragraph{Gap targeted.}
The kernel-decomposition bridge still passed through the older
product-independence apolar data package, even though those product
independence facts are now derived internally.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseProductFreeApolarData\_of\_kernelDecompositionApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  Rank one and rank two discard
the now-redundant product-independence fields from the kernel-decomposition
package, keeping only the negativity and binary kernel-equation fields.  Rank
three reuses the existing negative SOS summand and decomposition argument.

Updated
\[
\texttt{residual\_eq\_zero\_of\_kernelDecompositionApolarData}
\]
to route through \texttt{residual\_eq\_zero\_of\_productFreeApolarData}.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
Kernel-decomposition data is still stronger than the product-free data in rank
one and rank two, but the main proof chain no longer depends on product
independence as an external input.

No Julia numerical claims were used in this round.

\section*{Rank-one negativity from decomposition}

\paragraph{Gap targeted.}
The product-free apolar interface still assumed the rank-one negativity
\(\lambda(x^4)<0\) for every nonzero complement generator \(x\).  In the
rank-one case this follows from the negative SOS summand and the
annihilator-complement decomposition, so it should not remain an external
input.

\paragraph{Lean progress.}
Added to \texttt{QuaternaryQuarticProof/Catalecticant.lean}:
\[
\texttt{linProductSubmodule\_le\_span\_square\_of\_span\_eq}
\]
and
\[
\texttt{residualEval\_sq\_smul}.
\]
The first says that if \(W=\mathbb R x\), then \(WW\) is contained in the line
spanned by \(x^2\).  The second records the quadratic scaling rule
\(\lambda((c q)^2)=c^2\lambda(q^2)\).

Added to \texttt{QuaternaryQuarticProof/Support.lean}:
\[
\texttt{rank\_one\_binaryRestrictionCoeffA\_neg\_of\_annihilator\_complement}.
\]
Given rank one, SOS, FOCP, an annihilator complement \(A\oplus W\), and a
nonzero \(x\in W\) with \(\dim W=1\), it obtains a negative SOS summand,
decomposes it as \(q_W+q_K\), rewrites \(q_W\in WW\) as a scalar multiple of
\(x^2\), and concludes \(\lambda(x^4)<0\).

Added to \texttt{QuaternaryQuarticProof/Basic.lean}:
\[
\texttt{HasRankCaseRankOneFreeApolarData}
\]
and bridges to the product-free interface and root reduction theorem.  This
new package no longer includes rank-one negativity as an assumption; it is
derived internally from the rank-one theorem above.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Support}
\]
and
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
complete successfully.

\paragraph{Remaining blockers.}
The remaining high-level apolar inputs are the annihilator-map bounds, the
rank-two binary kernel-equation cases, and the rank-three negative product
data.  Rank-one negativity is now discharged from SOS and decomposition.

No Julia numerical claims were used in this round.

\section*{Rank-two binary-form interface}

\paragraph{Gap targeted.}
The rank-two apolar interface still required one of the explicit binary
kernel-equation cases.  Downstream, those cases are only used to obtain a
binary low-rank negative normal form.  The case classification is therefore a
separate algebraic subproblem and should not be part of the main rank-case
interface.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_binary\_forms\_and\_productLI}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It mirrors the existing
kernel-case constructor but accepts
\[
\texttt{HasBinaryLowRankNegativeNormalForm}
\]
directly; the \(\Sym^2A\) dimension and disjointness requirements are supplied
from the combined product-independence lemma.

Added to \texttt{QuaternaryQuarticProof/Basic.lean}:
\[
\texttt{HasRankCaseBinaryFormApolarData},
\quad
\texttt{hasRankCaseApolarComponentData\_of\_binaryFormApolarData},
\]
and the corresponding residual/root reduction theorems.  This interface keeps
the annihilator-map bounds, rank-two binary normal-form input, and rank-three
negative product input, while deriving rank-one negativity internally.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The rank-two kernel-equation classification has been separated from the main
proof chain.  The remaining structural inputs are now the annihilator-map
bounds, the rank-two binary low-rank normal form, and rank-three negative
product data.

No Julia numerical claims were used in this round.

\section*{Rank-three negativity from annihilator data}

\paragraph{Gap targeted.}
The binary-form apolar interface still carried rank-three negative product
data as an external assumption.  With the annihilator-map bound in rank three,
the existing decomposition lemma already converts any negative SOS summand
into a negative product supported on a complement.

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankCaseBinaryFormAndAnnihilatorData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  In rank three this package
requires only
\[
\dim\operatorname{range}(\texttt{linearAnnihilatorMap})\le3.
\]
Added
\[
\texttt{hasRankCaseBinaryFormApolarData\_of\_binaryFormAndAnnihilatorData}
\]
which obtains a negative SOS summand in rank three, applies
\[
\texttt{exists\_rank\_three\_catalecticantKernel\_decomposition\_of\_annihilatorMap\_range},
\]
and transfers negativity across the kernel summand.

Added the corresponding residual and root reduction theorems.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The newest high-level data interface now asks only for the annihilator-map
bounds in ranks one, two, and three, plus the rank-two binary low-rank normal
form.  Rank-one negativity and rank-three negative product data are both
derived internally.

No Julia numerical claims were used in this round.

\section*{Kernel data projected to binary-form interface}

\paragraph{Gap targeted.}
The kernel-decomposition route still carried stronger rank-one and rank-three
fields than the newest binary-form-and-annihilator interface needs.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseBinaryFormAndAnnihilatorData\_of\_kernelDecompositionApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  It projects the kernel
decomposition package to the newest interface: rank one keeps only the
annihilator-map bound, rank two converts kernel-equation cases to binary
low-rank normal forms, and rank three keeps only the annihilator-map bound.

Updated
\[
\texttt{residual\_eq\_zero\_of\_kernelDecompositionApolarData}
\]
to route through
\[
\texttt{residual\_eq\_zero\_of\_binaryFormAndAnnihilatorData}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The cleanest current high-level target is
\[
\texttt{HasRankCaseBinaryFormAndAnnihilatorData}.
\]
It remains to prove the annihilator-map bounds and the rank-two binary
low-rank normal form from the catalecticant rank hypotheses.

No Julia numerical claims were used in this round.

\section*{Existential binary normal-form interface}

\paragraph{Gap targeted.}
The rank-two binary-form interface still required a low-rank normal form for
every ordered basis \(x,y\) of the two-dimensional complement.  The proof only
uses, for each nonzero \(x\), the existence of a second direction \(y\) giving
such a normal form.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankTwoSupportComponentHypothesis\_of\_exists\_binary\_form\_and\_productLI}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  It accepts an existential
rank-two binary normal-form hypothesis and obtains the needed
\(\Sym^2A\)-dimension and disjointness facts from the product-independence
lemma.

Added
\[
\texttt{HasRankCaseExistentialBinaryFormData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}, together with the corresponding
apolar-component, residual, and root reduction theorems.  In this interface,
rank two requires only that each nonzero complement vector can be completed to
a binary normal-form pair.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The remaining high-level mathematical inputs are now the rank-case
annihilator-map bounds and the existential rank-two binary low-rank normal
form.

No Julia numerical claims were used in this round.

\section*{Negative-square apolar interface}

\paragraph{Gap targeted.}
The existential binary normal-form interface was still stronger than the
certificate construction needs.  The rank-two syzygy argument only uses a
negative pure square \(x^4\) in the two-dimensional complement, not an explicit
normal form for the whole binary quartic.

\paragraph{Lean progress.}
Added
\[
\texttt{hasPreimageProductSupportData\_of\_rank\_two\_negative\_square}
\]
to \texttt{QuaternaryQuarticProof/Support.lean}.  For a rank-two annihilator
complement \(A\oplus W\), a nonzero \(x\in W\), and
\(\lambda(x^4)<0\), it derives the required preimage-product support data.
The \(\Sym^2A\) dimension and range/symmetric-square disjointness are obtained
from the rank-two combined product-independence theorem.

Added
\[
\texttt{HasRankCaseNegativeSquareApolarData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  The corresponding bridge
\[
\texttt{hasRankCaseNegativeCertificateFamily\_of\_negativeSquareApolarData}
\]
proves the rank-case negative certificate family directly: rank one derives
negativity internally, rank two uses the supplied negative pure square, and
rank three derives the negative product from the annihilator bound and a
negative SOS summand.  Added residual and root reduction theorems for this
interface.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The main remaining mathematical inputs now match the blueprint more closely:
the low-rank apolar support bounds and the binary rank-two negative-square
statement.

No Julia numerical claims were used in this round.

\section*{Binary forms imply negative-square data}

\paragraph{Gap targeted.}
The existential binary normal-form interface should now be subordinate to the
negative-square interface, rather than maintaining a separate proof route.

\paragraph{Lean progress.}
Added
\[
\texttt{hasRankCaseNegativeSquareApolarData\_of\_existentialBinaryFormData}
\]
to \texttt{QuaternaryQuarticProof/Basic.lean}.  In rank two, it chooses a
nonzero vector \(x\) in the two-dimensional complement, uses the existential
binary normal-form hypothesis to find a second direction \(y\), applies the
existing binary negative-square theorem, and transports the resulting vector
back into the complement plane.

Updated
\[
\texttt{residual\_eq\_zero\_of\_existentialBinaryFormData}
\]
to route through \texttt{residual\_eq\_zero\_of\_negativeSquareApolarData}.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The preferred current interface is the negative-square apolar data package:
rank-case annihilator-map bounds plus a rank-two negative pure square in the
binary support plane.

No Julia numerical claims were used in this round.

\section*{Separating apolar support from binary negative square}

\paragraph{Gap targeted.}
The negative-square apolar interface combined two conceptually distinct
blueprint inputs: the low-rank apolar support bounds and the binary rank-two
negative-square statement.

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankCaseAnnihilatorMapBounds}
\]
for the three rank-case annihilator-map bounds, and
\[
\texttt{HasRankTwoNegativeSquareData}
\]
for the rank-two negative pure-square input.  Proved
\[
\texttt{hasRankCaseNegativeSquareApolarData\_of\_annihilatorBounds\_and\_rankTwo}
\]
and added residual/root reduction theorems through these two separated inputs.

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The remaining proof is now explicitly split into the two structural blueprint
lemmas: low-rank support/annihilator bounds and the rank-two binary
negative-square theorem.

No Julia numerical claims were used in this round.

\section*{Separating binary forms from negative squares}

\paragraph{Gap targeted.}
The binary normal-form-to-negative-square implication was still embedded in a
larger rank-case bridge.  It is one of the remaining blueprint components and
should be exposed directly.

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankTwoExistentialBinaryFormData}
\]
and proved
\[
\texttt{hasRankTwoNegativeSquareData\_of\_existentialBinaryFormData}.
\]
This theorem chooses a nonzero vector in the two-dimensional support plane,
uses the existential binary normal form to complete it to a pair, applies the
binary negative-square lemma, and returns the negative pure square in the same
plane.

Added the combined bridge and root reduction theorem for
\[
\texttt{HasRankCaseAnnihilatorMapBounds}
\]
together with
\[
\texttt{HasRankTwoExistentialBinaryFormData}.
\]

\paragraph{Verification.}
\[
\texttt{lake build QuaternaryQuarticProof.Basic}
\]
completes successfully.

\paragraph{Remaining blockers.}
The formal proof is now organized around two named inputs matching the
blueprint: low-rank apolar support bounds and the rank-two binary normal-form
or negative-square theorem.

No Julia numerical claims were used in this round.

\section*{Aligning annihilator bounds with apolar support}

\paragraph{Gap targeted.}
The current root reduction used rank-case bounds on the image of the linear
annihilator map.  The blueprint's low-rank support lemma instead produces
codimension data for the linear annihilator itself, via a large subspace
\(\Ann_1(\lambda)\).

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankCaseApolarSupportBounds}
\]
for the rank-one, rank-two, and rank-three instances of
\[
\texttt{HasLinearAnnihilatorCodimAtMost}.
\]
Proved
\[
\texttt{hasRankCaseAnnihilatorMapBounds\_of\_apolarSupportBounds}
\]
using the existing finite-dimensional equivalence between annihilator
codimension and annihilator-map range dimension.  Added residual and root
reduction theorems showing that apolar support bounds plus
\[
\texttt{HasRankTwoExistentialBinaryFormData}
\]
imply the target theorem.

\paragraph{Strategy change.}
The remaining support obligation is now stated in the same direction as
Lemma~\ref{lem:low-rank-support}: prove codimension support bounds first, then
derive the map-rank bounds as a bookkeeping consequence.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The two remaining mathematical inputs are the low-rank apolar support bounds
and the rank-two existential binary normal-form theorem.

No Julia numerical claims were used in this round.

\section*{Reducing binary forms to kernel equation cases}

\paragraph{Gap targeted.}
The rank-two binary input still assumed the completed low-rank negative normal
form.  The blueprint's proof reaches that normal form by classifying the
one-dimensional Hankel kernel into the cases \(xy\), \(y^2\), and
\(x^2+y^2\).

\paragraph{Lean progress.}
Added
\[
\texttt{HasRankTwoExistentialKernelEquationData}
\]
for the rank-two statement that every nonzero support direction can be
completed to a second support direction satisfying one of the existing
\[
\texttt{HasBinaryRestrictionKernelEquationCase}
\]
branches.  Proved
\[
\texttt{hasRankTwoExistentialBinaryFormData\_of\_kernelEquationData}
\]
using the already-formalized implication from kernel equations to
\[
\texttt{HasBinaryLowRankNegativeNormalForm}.
\]
Added residual and root reduction theorems through apolar support bounds plus
these kernel-equation cases.

\paragraph{Strategy change.}
The binary blocker is now reduced to the actual Hankel-kernel classification
step: produce the three kernel-equation alternatives in a support basis.  The
downstream negative-square and syzygy machinery no longer needs to know how the
case was obtained.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The two remaining mathematical inputs are low-rank apolar support bounds and
the rank-two Hankel-kernel classification/existence of a support basis.

No Julia numerical claims were used in this round.

\section*{Rank-one Hankel sign branch}

\paragraph{Gap targeted.}
The binary low-rank lemma's rank-one branch requires showing that a rank-one
Hankel matrix with a negative quadratic value has a negative scalar
factorization coefficient.

\paragraph{Lean progress.}
Added the scalar Hankel quadratic form
\[
\texttt{binaryHankelQuad}
\]
and proved its rank-one factorization identity
\[
\texttt{binaryHankelQuad\_rankOne\_eq}.
\]
From this, proved
\[
\texttt{rankOne\_hankel\_scalar\_neg\_of\_negative}
\]
and the normal-form constructor
\[
\texttt{HasBinaryLowRankNegativeNormalForm.rankOne\_of\_negative\_hankel}.
\]

\paragraph{Strategy change.}
The rank-one binary case no longer needs the sign \(\rho<0\) as an external
input once a negative Hankel quadratic value is available.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The rank-two kernel classification and the low-rank apolar support theorem
remain the two main mathematical inputs.

No Julia numerical claims were used in this round.

\section*{Hankel negativity side conditions}

\paragraph{Gap targeted.}
The binary kernel cases still carried side conditions in different forms:
the \(xy\)-kernel branch needed a negative diagonal quadratic value, and the
elliptic branch needed \(a\ne0\) or \(b\ne0\).

\paragraph{Lean progress.}
Added
\[
\texttt{HasBinaryHankelNegativeValue}
\]
as a named predicate for a negative value of the \(3\times3\) Hankel quadratic
form.  Proved that this predicate supplies the diagonal negative value in the
\(xy\)-kernel case via
\[
\texttt{HasBinaryLowRankNegativeNormalForm.xyKernel\_of\_negative\_hankel},
\]
and supplies the nonzero side condition in the elliptic case via
\[
\texttt{ellipticKernel\_nonzero\_of\_negative\_hankel}
\]
and
\[
\texttt{HasBinaryLowRankNegativeNormalForm.ellipticKernel\_of\_negative\_hankel}.
\]

\paragraph{Strategy change.}
The eventual rank-two Hankel classification can now pass around a single
negative-Hankel-value hypothesis for the diagonal and elliptic kernel branches,
rather than separately deriving each downstream side condition.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The rank-two \(y^2\)-kernel branch still needs the rank-two/nonzero-minor
argument that forces \(b\ne0\).  The low-rank apolar support theorem is still
the other main blocker.

No Julia numerical claims were used in this round.

\section*{\(y^2\)-kernel nonzero column branch}

\paragraph{Gap targeted.}
The \(y^2\)-kernel branch of the binary classification requires \(b\ne0\).
In the blueprint this comes from rank two of the Hankel matrix.

\paragraph{Lean progress.}
Added a concrete column-independence lemma
\[
\texttt{ySqKernel\_b\_ne\_zero\_of\_column\_linearIndependent}
\]
showing that linear independence of the first two specialized Hankel columns
\[
(a,b,0),\qquad (b,0,0)
\]
forces \(b\ne0\).  Added the corresponding normal-form constructor
\[
\texttt{HasBinaryLowRankNegativeNormalForm.ySqKernel\_of\_column\_linearIndependent}.
\]

\paragraph{Strategy change.}
The \(y^2\)-case can now be closed from a local two-column independence
certificate, avoiding a dependency on general matrix-rank infrastructure at
this stage.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The binary theorem still needs the global classification that supplies one of
the three kernel cases and the relevant branch certificate.  The low-rank
apolar support theorem remains open.

No Julia numerical claims were used in this round.

\section*{Consolidated binary kernel branch certificate}

\paragraph{Gap targeted.}
The binary rank-two classification should hand downstream proof code one of
three kernel branches together with the minimal branch-specific side
certificate.  Previously those branches were split across several constructors.

\paragraph{Lean progress.}
Added
\[
\texttt{HasBinaryKernelBranchCertificate}
\]
with branches for the \(xy\)-kernel, \(y^2\)-kernel, and elliptic kernel cases.
Proved
\[
\texttt{binaryLowRankNegativeNormalForm\_of\_kernelBranchCertificate}
\]
and its binary-restriction specialization.  Added
\[
\texttt{HasRankTwoExistentialKernelBranchData}
\]
and proved that it implies
\[
\texttt{HasRankTwoExistentialBinaryFormData}.
\]
Added residual and root reductions through apolar support bounds plus these
rank-two branch certificates.

\paragraph{Strategy change.}
The remaining binary input is now a classification theorem that produces a
single branch certificate in a support basis.  All branch-specific conversion
to negative normal forms is formalized.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The binary Hankel-kernel classification and the low-rank apolar support theorem
remain open.

No Julia numerical claims were used in this round.

\section*{Canonical binary Hankel kernel vectors}

\paragraph{Gap targeted.}
The consolidated branch certificate still assumed the branch equations
directly.  The blueprint obtains those equations from the canonical generators
\[
xy,\qquad y^2,\qquad x^2+y^2
\]
of the one-dimensional Hankel kernel after a real binary change of basis.

\paragraph{Lean progress.}
Added
\[
\texttt{binaryHankelMul}
\]
for the action of the scalar Hankel matrix on a coefficient triple.  Proved
that the three canonical kernel vectors
\[
(0,1,0),\qquad (0,0,1),\qquad (1,0,1)
\]
give the expected branch equations via
\[
\texttt{xy\_kernel\_equations\_of\_binaryHankelMul\_eq\_zero},
\]
\[
\texttt{ySq\_kernel\_equations\_of\_binaryHankelMul\_eq\_zero},
\]
and
\[
\texttt{elliptic\_kernel\_equations\_of\_binaryHankelMul\_eq\_zero}.
\]
Added constructors from these canonical kernel equations to
\[
\texttt{HasBinaryKernelBranchCertificate}.
\]

\paragraph{Strategy change.}
The remaining binary classification can now target canonical kernel vectors
for the scalar Hankel matrix, instead of re-proving the downstream coefficient
equations in every branch.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The binary theorem still needs the real change-of-basis classification of a
one-dimensional binary quadratic kernel.  The low-rank apolar support theorem
remains open.

No Julia numerical claims were used in this round.

\section*{Low-rank support as a single monotone input}

\paragraph{Gap targeted.}
The apolar support side was represented by three exact-rank cases, while
Lemma~\ref{lem:low-rank-support} is naturally a single statement for
\(\rank C_\lambda\le k\), \(k\le3\).

\paragraph{Lean progress.}
Added
\[
\texttt{HasLowRankApolarSupportTheorem}
\]
as the direct Lean interface for the blueprint lemma:
\[
\texttt{finrank (range catalecticantMap) <= k}
\Rightarrow
\texttt{HasLinearAnnihilatorCodimAtMost ... k}.
\]
Proved
\[
\texttt{hasRankCaseApolarSupportBounds\_of\_lowRankApolarSupportTheorem}
\]
and added residual/root reductions through this single support theorem plus
the rank-two kernel branch data.

\paragraph{Strategy change.}
The support blocker is now stated in the same monotone form as the blueprint.
The exact-rank bookkeeping is isolated in a short Lean bridge.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The two mathematical inputs are now:
the monotone low-rank apolar support theorem and the binary Hankel
kernel-branch classification.

No Julia numerical claims were used in this round.

\section*{Decomposition-level apolar support input}

\paragraph{Gap targeted.}
The support interface now matched the blueprint's rank inequality, but it still
discarded the explicit decomposition \(E=W\oplus A\) produced by
Lemma~\ref{lem:low-rank-support}.

\paragraph{Lean progress.}
Added
\[
\texttt{HasLowRankApolarSupportDecomposition}
\]
for the direct decomposition form of the blueprint lemma: for each
\(k\le3\) and \(\rank C_\lambda\le k\), there are subspaces \(A,W\) with
\[
A\le\Ann_1(\lambda),\qquad A\oplus W=E,\qquad \dim W\le k,\qquad
\dim A\ge4-k.
\]
Proved
\[
\texttt{hasLowRankApolarSupportTheorem\_of\_decomposition}
\]
and routed residual/root reductions through this decomposition-level input
plus the rank-two kernel branch data.

\paragraph{Strategy change.}
The remaining support theorem is now stated essentially in the form used in
the proof blueprint.  The later proof only extracts the annihilator
codimension, but future formalization of the apolar lemma can keep the
complement \(W\) available.

\paragraph{Verification.}
Pending the next Lean harness run.

\paragraph{Remaining blockers.}
The two mathematical inputs remain the decomposition-level low-rank apolar
support theorem and the binary Hankel kernel-branch classification.

No Julia numerical claims were used in this round.

\end{document}
